@AGENTS.md

# Cards Center

Pokémon trading card marketplace — auctions, claimsales, listings, wallet, messaging, reviews, watchlist, notifications, disputes.

## CRITICAL RULES
- **NOOIT zomaar naar `main` op GitHub pushen.** `main` is gekoppeld aan Railway en **elke push naar `main` deployt automatisch naar de LIVE site** (https://cardscenter.up.railway.app). Daarom: bij élke wijziging aan de app EERST lokaal testen op de localhost-testserver (`npm run dev`, controleren in de browser / met `curl`), pas pushen als het daar werkt — en alleen na expliciete toestemming van de gebruiker. Werk op een feature-branch en push die desnoods; push naar `main` is een bewuste "we gaan live"-keuze, geen routine. Zie sectie **Deployment & Hosting (Fase 41)**.
- **NOOIT destructieve git commando's** (`git stash`, `git reset --hard`, `git checkout .`, `git clean`) gebruiken zonder EERST te committen. Op 2026-03-31 zijn alle unstaged wijzigingen verloren gegaan door een mislukte `git stash`.
- **Altijd committen** na elke grote wijziging zodat werk niet verloren gaat.
- **Nooit `npm run build` draaien vanuit achtergrond agents** terwijl de dev server draait — dit veroorzaakt database locks en frozen pages.
- **`"use server"` bestanden mogen alleen async exports hebben.** Constants (zoals `IBAN_COOLDOWN_DAYS`, `WITHDRAWAL_MIN_AMOUNT`) moeten in een apart `src/lib/*-config.ts` bestand. Anders breekt Next.js client-side imports met "module has no exports at all".
- **Schema-wijzigingen via `prisma db push`, NIET `prisma migrate dev`.** Het project heeft drift tussen migration-history (in `prisma/migrations/`) en de huidige schema. `migrate dev` wil de DB resetten — gebruik `db push --accept-data-loss` om data te behouden.
- **Voor `prisma db push`: kill eerst de dev server.** Anders DB-lock.
- **Admin-werk hoort thuis in het admin panel** (Fase 15). Elke nieuwe admin-feature, admin-page, admin-action of admin-only flow moet via `/dashboard/admin/*` gaan en de admin-conventies volgen — zie sectie **Admin panel (Fase 15)** verderop. Concreet: nooit een nieuwe `*/admin/page.tsx` ergens anders aanmaken, nooit een admin-action zonder `requireAdmin()` + `logAdminAction()` schrijven, nooit een nieuwe cron toevoegen zonder `CRON_JOBS` registry + `withCronLogging`.

## Tech Stack
- **Framework:** Next.js 16 (App Router) — **Read `node_modules/next/dist/docs/` before writing code**
- **Language:** TypeScript
- **Styling:** Tailwind CSS v4 (default config, no custom theme file)
- **Database:** SQLite via Prisma ORM (`prisma/schema.prisma`)
- **Auth:** Auth.js v5 (NextAuth) with credentials provider
- **Validation:** Zod v4
- **i18n:** next-intl (Dutch default + English) — `src/messages/nl.json` + `en.json`
- **Charts:** recharts v3.8.0

## Commands
- `npm run dev` — Start dev server
- `npm run build` — Production build (use to verify after changes)
- `npx prisma db push --accept-data-loss` — **gebruik dit voor schema-wijzigingen** (`migrate dev` werkt niet door drift)
- `npx prisma db seed` — Seed the database
- `npx prisma generate` — Regenerate Prisma client (na elke `db push`)

**Env-vars** (`.env`): `DATABASE_URL`, `AUTH_SECRET`, `AUTH_TRUST_HOST`, `POKEWALLET_API_KEY`, `ANTHROPIC_API_KEY` (image content-moderation — leeg = fail-open, uploads gaan ongechecked door).

### Schema-wijziging workflow
1. Edit `prisma/schema.prisma`
2. Kill dev server: `taskkill //PID <pid> //F` (vind PID via `netstat -ano | grep :3000`)
3. `npx prisma db push --accept-data-loss && npx prisma generate`
4. `rm -rf .next && npm run dev` (verse cache)
5. `curl http://localhost:3000/<gewijzigde-page>` om te compileren

## Deployment & Hosting (Fase 41 — live sinds 2026-05-23)
De testfase-site draait live. **Workflow: altijd eerst lokaal testen (`npm run dev`), dan pas pushen naar `main` (= live deploy), en alleen met toestemming.**

- **Live URL:** https://cardscenter.up.railway.app
- **Stack:** Railway (hosting, Node-container) ← auto-deploy vanaf GitHub `cardscenter/app` branch **`main`** · Turso (libSQL database) · Cloudflare R2 (geüploade foto's). Drie aparte accounts/dashboards.
- **Auto-deploy:** elke push naar `main` triggert een Railway build+deploy naar productie. Feature-branches deployen niet. → test lokaal vóór je naar `main` pusht.

### Railway-specifieke afspraken (valkuilen die we raakten — niet wijzigen zonder reden)
- **Node-versie gepind** op 24 via `.nvmrc` (`24`) + `engines.node` (`24.x`). Anders gebruikt Railway oudere npm die `@swc/helpers` anders resolved → `npm ci` faalt op lockfile-mismatch. Lokaal = Node 24 / npm 11. Houd lokaal en Railway op dezelfde major.
- **Poort = 8080.** Railway luistert op 8080 en negeert een zelf-gezette `PORT`. Het publieke domein moet target-poort **8080** hebben (niet 3000).
- **Env-vars staan op de SERVICE**, niet project-wide. Railway's project-/shared-variabelen werken NIET automatisch door naar een service — alles moet in de Variables-tab van de service zelf. Geen quotes en geen plaatshouders (`<...>`) in waarden. `NEXT_PUBLIC_*` is build-time → na wijziging een rebuild (redeploy) nodig.
- **`postinstall: prisma generate`** in package.json zorgt dat Railway de Prisma-client bouwt.
- **`src/app/sitemap.ts` is `force-dynamic`** (+ try/catch-fallback): bevroeg anders de DB tijdens `next build` → faalde omdat Turso op build-time niet bereikbaar is.

### Productie env-vars (Railway service Variables)
`DATABASE_URL` (libsql Turso-URL), `TURSO_AUTH_TOKEN`, `AUTH_SECRET`, `AUTH_TRUST_HOST=true`, `NEXTAUTH_URL` + `NEXT_PUBLIC_SITE_URL` (= live domein), `CRON_SECRET`, `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET`. Optioneel: `ANTHROPIC_API_KEY`, `POKEWALLET_API_KEY`. Sjabloon: `.env.example`.

### Schema naar Turso pushen (NIET `prisma db push` — die praat niet met remote libSQL)
`prisma.config.ts` heeft geen driver-adapter, dus de Prisma-CLI kan geen remote `libsql://` bereiken. Workflow voor schema-wijzigingen op Turso:
1. SQL genereren: `npx prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script > prisma/turso-schema.sql` (let op: vlag is `--to-schema`, niet `--to-schema-datamodel`).
2. Toepassen: `npx tsx scripts/push-to-turso.ts "libsql://<db>.turso.io"` (leest `TURSO_AUTH_TOKEN` uit `.env`, voert SQL uit via `@libsql/client.executeMultiple`).
   - Let op: `--from-empty` is voor een **lege** DB. Voor incrementele wijzigingen op een gevulde Turso-DB een diff genereren met `--from-schema` van de oude staat (of handmatige ALTER).

### Helper-scripts (`scripts/`)
- `push-to-turso.ts` — schema naar Turso (zie boven).
- `check-turso.ts` — test Turso-connectie + telt tabellen/User-rijen (`npx tsx scripts/check-turso.ts "<url>"`).
- `test-r2.ts` — round-trip R2-test (upload/ophalen/verwijderen) met `.env`-credentials.

### Crons in productie
In-process schedulers (auction-finalize/activate, claimsale-activate, expire-claims) draaien automatisch bij container-boot (`src/instrumentation.ts`). De overige tijdgestuurde crons (`/api/cron/*`, o.a. `cleanup-sold-images`, betaaltermijnen, dispute-auto-resolve) hebben een **externe scheduler** nodig die ze dagelijks aanroept met `Authorization: Bearer <CRON_SECRET>` — nog in te stellen (Railway cron of externe dienst).

### E-mail
Nog mock/console (Fase 16 niet af) — wachtwoord-reset/verificatie-mails worden alleen gelogd, niet verstuurd.

## Conventions
- Code (variables, functions, components) in **English**
- UI text via **next-intl** — never hardcode Dutch/English strings in components
- Filenames: **kebab-case** (e.g., `bid-section.tsx`)
- Import alias: `@/` → `src/`
- Server Actions in `src/actions/`, Zod schemas in `src/lib/validations/`
- Pages are Server Components; add `"use client"` only when needed (charts, forms with state)
- **Locale-aware navigation**: `Link`, `useRouter`, `usePathname`, `getPathname` ALTIJD uit `@/i18n/navigation` — niet uit `next/link` of `next/navigation`. **redirect**: voor stub-paden die naar een ander pad in dezelfde locale verwijzen (zoals de `/dashboard/geschillen/admin` → `/dashboard/admin/disputes` redirects) werkt `redirect({ href, locale })` uit `@/i18n/navigation`. Maar voor redirects vanuit een live stateful page (bv. admin → admin-overview) gaf de i18n-redirect "This page couldn't load" + hook-mismatches in Next 16 + next-intl 4 setup — gebruik daar `redirect` uit `next/navigation` met een **handmatig locale-prefixed pad**: `redirect(\`/${locale}/dashboard/admin\`)`. Locale komt uit `params` (`const { locale } = await params;`). `notFound()` mag uit `next/navigation` (geen locale relevant). Bij twijfel: vermijd server-side redirects en gebruik een client-side Link.
- **Surface-hiërarchie (Fase 26)**: light-mode gebruikt **3 surface-niveaus**: `bg-background` (#f5f6f8, page-frame) < `bg-muted` (#eef0f3, pills/secondary surfaces) < `bg-card` (#ffffff, primary content). Cards op page-bg krijgen `border border-border` + optioneel `shadow-card` (subtiele elevation). **Nooit hardcoded `bg-white` / `bg-slate-200` / `bg-gray-100`** — gebruik altijd semantic tokens (`bg-card`, `bg-muted`, `bg-secondary`, `border-border`, `divide-border`, `hover:bg-muted`). Dark-mode tokens blijven onveranderd. Glass-utilities (`.glass`, `.glass-subtle`, `.glass-nav`, `.glass-input`) zijn in light-mode geherdefinieerd als solide `bg-card` + border + shadow; in dark-mode behouden ze het echte glass-effect (rgba + backdrop-blur). Custom-shadow-tokens: `shadow-card` voor rust-state, `shadow-card-hover` voor lift-on-hover.
- **Layout-tiers (Fase 25)**: gebruik `<PageContainer width="narrow|default|wide">` uit `@/components/layout/page-container` als top-level wrap voor elke page. Géén losse `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`-strings meer in nieuwe pages — die zijn vervangen door één bron van waarheid. Tiers:
  - `narrow` = `max-w-3xl` (768px) — voorwaarden, instructies, single-column tekst-pages
  - `default` = `max-w-[1440px]` — detail-pages (auction/listing/claimsale/seller-profile), forms (`*/nieuw/`), customization, calculator-pages, winkelwagen
  - `wide` = `max-w-[1680px]` — grid-overzichten (marktplaats/veilingen/claimsales/zoeken/kaarten/pokedex/admin-tables), header, footer, dashboard-layout, homepage-secties
  - PageContainer bevat alleen horizontale padding (`px-4 sm:px-6 lg:px-8 xl:px-10`). Vertical padding (`py-8`/`py-10`) komt via `className`-prop omdat dat per page verschilt.
  - Header + footer zijn ALTIJD `wide` (vaste frame), zodat alleen de inner content-area per page-type schuift — geen volledige layout-sprong.
  - Grid-pages gebruiken `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5 [@media(min-width:1600px)]:grid-cols-6`. Sponsored-rows tonen tot 6 items met progressive disclosure per breakpoint.
  - Homepage-secties (`src/components/home/*.tsx`) gebruiken inline `mx-auto max-w-[1680px] px-4 sm:px-6 lg:px-8 xl:px-10` (geen PageContainer omdat ze in `<section>`-tags met eigen background zitten).

## Key Architecture
- **Pokémon only** — no multi-TCG, Category always Pokémon
- **Internal wallet** — balance per user, tier-based seller commission (FREE 3%, PRO 1.5%, Unlimited 0%)
- **Escrow system** — `heldBalance` on User; `escrowCredit`/`releaseEscrow`/`refundEscrow` in `wallet.ts`
- **Shipping bundling** — ShippingBundle groups items per seller; links to claimsale, auction, or listing. **Default-gedrag** sinds 2026-05-15: elke `checkout()` in cart.ts maakt een verse bundle per verkoper (geen findFirst-hergebruik meer over verschillende sale-types). Cross-checkout bundeling alleen via expliciete opt-in (zie Claimsale-merge hieronder).
- **Claimsale-merge (2026-05-15)** — Buyer kan bij een tweede claimsale-checkout items toevoegen aan z'n vorige PAID-claimsale-bundle bij dezelfde verkoper (bespaart verzendkosten). Voorwaarden: bundle is puur claimsale (geen listingId/auctionId/bundleProposalId), status `PAID`, `lockedForPackingAt IS NULL`, `paymentMode=PLATFORM`, `deliveryMethod=SHIP`. UI in /winkelwagen: radio per seller-groep met *"Voeg toe aan #X (bespaar verzending)"* (default) vs *"Nieuwe aparte bestelling"* — alleen zichtbaar als `getCombinableClaimsaleBundles(buyerId)` een match levert. Bij merge: `shippingCost=0` voor die seller, shipping-picker verborgen, group-total toont "Inbegrepen in vorige bestelling". `cart.ts checkout()` accepteert `mergeIntoBundles?: Record<sellerId, bundleId>` — upfront-verify + race-safe claim via conditional `updateMany WHERE lockedForPackingAt:null AND status:PAID`. Faalt vóór side-effects met user-friendly error als bundle in tussentijd gelockt. Schrijft `ShippingBundle.appendHistory` (JSON-array `[{at,itemNames,itemCount,itemTotal}]`) + stuurt "Bestelling uitgebreid"-notificatie ipv "Nieuwe bestelling". **Seller-controle**: `ShippingBundle.lockedForPackingAt DateTime?` + nieuwe action `lockBundleForPacking(bundleId)` (purchase.ts, idempotent, race-safe). Amber "Klaar om in te pakken?"-banner in `<OrderDetailModal>` op claimsale-PAID-bundles met **Vergrendel voor inpakken**-knop; slate-pill "Vergrendeld op {datum}" zodra gelockt; tijdlijn met append-events (datum-pill + N items + totaal + itemnamen). `markAsShipped` lockt defensief mee. Beperking tot claimsales is bewust: veilingen en marktplaats-bestellingen hebben hun eigen status-machine en zouden bij merge breken (de bundle behoudt z'n `listingId`/`auctionId`-link).
- **Static shipping catalog (Fase 33)** — vooraf gedefinieerde tariff-catalog (`src/lib/shipping/tariffs.ts`) met 3 service-types (MAILBOX_PARCEL/PARCEL_STANDARD/PARCEL_SIGNED) × 3 zones (DOMESTIC/EU_NEAR/EU_FAR). Sellers krijgen automatisch (via `setupStaticShippingMethods`) een vaste set slots op basis van `User.country` + scope. Per slot kunnen ze carrier kiezen (uit `getCarriersForCountry`) en prijs **±50%** van basisprijs aanpassen via `priceOverride` (popover-edit met UX-tip "houd verzendkosten laag voor aantrekkelijkheid"). Geen custom-create meer. Briefpost (untracked) is geschrapt. EU_NEAR-mapping uit `src/lib/shipping/zones.ts` is symmetrisch (NL↔BE, DE↔AT, etc.). MT/CY zitten in SOUTHERN tier-fallback (geen aparte eiland-zone meer). Cross-border-altijd-signed-regel is vervallen — PARCEL_STANDARD mag in EU_NEAR/EU_FAR onder €150. **`PARCEL_SIGNED` is verplicht actief in elke in-scope zone** (fallback voor orders ≥€150) — `isRequiredSlot` helper, server-side guard in `toggleShippingMethod` + UI-lock-pill. Out-of-scope zones zijn volledig verborgen in de manager-UI én server weigert toggle-activatie buiten scope. `User.sellingCountries` is nu een SellingScope-enum (`DOMESTIC_ONLY | DOMESTIC_AND_NEAR | ALL_EU`) met origin-aware labels op `/dashboard/verzending` via `<SellingScopeToggle>` (label "Nederland en België" voor NL+1 buurland, langere lijst-formattering voor meerdere). Manager toont slots gegroepeerd per zone met headers ("Binnenland" / "Naar [buurlanden]" / "Overige EU-landen"). Snapshots in `Listing/Auction/ClaimsaleShippingMethod.price` blijven werken (`effectivePrice = priceOverride ?? basePrice` op snapshot-moment).
- **Address management** — buyer address snapshotted on ShippingBundle at checkout
- **Auction bidding** — 40% of bid amount reserved on `reservedBalance`; full amount deducted at auction end. If winner has <100% but >=40%: 5-day payment deadline (`paymentStatus: AWAITING_PAYMENT`). `completeAuctionPayment()` lets winner pay after topping up.
- **Auction runner-up offer-flow (post-Fase-33)** — als de winnaar de 5d-betaaltermijn mist, schuift de cron `api/cron/auction-payment-deadline` NIET meer automatisch door naar de volgende bieder. In plaats daarvan: (a) wanbetaler krijgt strike + 2,9%-fee + €200-borg (≥€2000), niet-inbare deel als `PendingPlatformFee`; (b) helper `processRunnerUpDecision(auctionId, previousBidderId)` (in `src/lib/cron-jobs.ts`) maakt een `AuctionRunnerUpOffer` aan voor de volgende eligible bieder met `status="AWAITING_DECISION"` + 72u-window, en flipt `Auction.paymentStatus` naar `AWAITING_RUNNER_UP_DECISION`. **Geen PENDING-bundle, geen reserve op runner-up tijdens 72u-window** — het is een offer, geen bid. Runner-up reageert via `acceptRunnerUpOffer(offerId)` (5d-payment-deadline + bundle + reserve, identiek aan originele winnaar) of `declineRunnerUpOffer(offerId)` (geen straf, doorschuiven naar volgende). Niet reageren binnen 72u → cron `auction-runner-up-decision-deadline` zet status EXPIRED en triggert volgende kandidaat. Cap via `User.maxRunnerUpAttempts` (0–3, default 2, slider 0-3 op /dashboard/profiel) — hard-clamped op `MAX_RUNNER_UP_ATTEMPTS_CAP=3` in cron-jobs.ts. Velden: `AuctionRunnerUpOffer { auctionId, bidderId, bidAmount, premiumAmount, deliveryChoice, status, decisionDeadline, decidedAt }`. `Auction.failedBidderIds` blijft het JSON-array van gepasseerde bidders (wanbetaler + decliners + expired). Eindstatus: alle attempts uitgeput of geen kandidaat → `Auction.status = PAYMENT_FAILED`. Adres-check: SHIP-veiling met runner-up zonder geldig adres → offer blijft staan, accept-knop disabled met "Vul je adres in"-banner; user kan adres invullen binnen 72u en alsnog accepteren. `completeAuctionPayment` muteert geen status meer bij verlopen deadline.
- **PendingPlatformFee (post-Fase-33)** — niet-inbare boetes/forfaits worden geregistreerd als schuld in `PendingPlatformFee { userId, type, amount, originalAmount, settledAt }` en bij elke balance-credit (deposit, sale-payout, refund) FIFO afgeroomd via `settlePendingFees(userId, incomingAmount)` in `src/lib/pending-fees.ts`. Helpers `recordPendingFeeInTx` (binnen tx) + `getOpenPendingFees`. Wallet-paths `creditBalance`/`releaseEscrow`/`partialRefundEscrow`/`refundEscrow`/`adminDeposit`/`confirmBankTransfer` zijn aangepast: `remaining = settle eerst, dan balance-credit met remaining`. Transaction-rij voor settlement type `PLATFORM_FEE_SETTLEMENT` (geen balance-mutatie, alleen audit-trail). UI: `<PendingFeesBanner>` op `/dashboard/saldo`, detail-page `/dashboard/saldo/openstaande-kosten`. Anti-griefing: wanbetaler kan saldo niet meer leeg houden om boete te ontwijken.
- **Proposal payment-deadline** — chat-proposals met partial balance (>=40% maar <100%) krijgen ook 5 dagen via `Proposal.paymentDeadline`. Cron `api/cron/proposal-payment-deadline` zet PAYMENT_FAILED, listing terug op ACTIVE, PENDING bundle weg, beide partijen genotificeerd. Geen runner-up (proposals hebben geen bod-volgorde).
- **Pending shipping-bundles** — bij AWAITING_PAYMENT (auction én listing-proposal) wordt direct een PENDING `ShippingBundle` aangemaakt via `src/lib/shipping-bundle.ts createPendingBundle()`. `completeAuctionPayment` / `completeProposalPayment` promoveren PENDING → PAID i.p.v. een tweede bundle aan te maken (auctionId/listingId zijn @unique). Address mag null zijn bij PENDING — wordt bij PAID-promotie ingevuld. Sales/purchases dashboards filteren PENDING uit hun lijsten; auction-pending heeft een eigen `pendingAuctions`-sectie op `/dashboard/verkopen`, listing-pending wordt afgehandeld via chat.
- **Reserved balance** — `reservedBalance` on User tracks 10% of (bid + 2,9% premium) for active auction bids (Fase 30 verlaagde van 15% naar 10%). `availableBalance = balance - reservedBalance`. All purchase actions check available balance. Per auction: reserves on max(highestBid, autobidMaxAmount). Released when outbid (no autobid) or auction ends. Bij win wordt de reserve "omgezet" naar de echte betaling — geen aparte vrijgave.
- **Account verification (Fase 32)** — Drie types naast elkaar, alle drie als trust-signal (ID-verificatie blijft de gate voor bids ≥€2000):
  1. **ID** — paspoort/ID/rijbewijs upload → admin review → `User.isVerified` + `verificationStatus`. Bestaand sinds project-start.
  2. **IBAN** — automatische match: user vult `User.iban` in op profiel, doet bankstorting, admin vult sender-IBAN in bij `confirmBankTransfer`. Bij genormaliseerde match → `User.isIbanVerified` true. Geen aparte status-veld want geen review-fase.
  3. **Adres** — officieel document (TAX_LETTER/UTILITY_BILL/BANK_STATEMENT/MUNICIPAL_LETTER) upload → admin vergelijkt naam+adres met profiel → `User.isAddressVerified` + `addressVerificationStatus`.
  `VerificationRequest` heeft een `type`-veld (ID|ADDRESS) + `addressDocumentType` voor de adres-flow. Actions in `verification.ts` (`submitVerification`, `submitAddressVerification`, `adminReviewVerification` flipt het juiste user-veld op basis van `request.type`, `revokeVerification(userId, type)` accepteert alle 3 types). Pages: `dashboard/verificatie/` (3-secties layout), `dashboard/admin/verifications/` (lijst met type-pill ID/Adres en profiel-adres-vergelijking voor ADDRESS-aanvragen). Trust-badges: `components/ui/trust-badges.tsx` (alleen geverifieerde types tonen, kleuren ID=sky/IBAN=emerald/Adres=violet) gebruikt op `SellerInfoBlock` + `SellerReputationCard`. `VerifiedBadge` legacy-component blijft voor compacte kaartjes (listing-card, sponsored-row) die alleen ID tonen.
- **Balance top-up** — Bank transfer with unique `bankTransferReference` per user (format: `[username][10digits]`, generated at registration, regenerated on username change). Admin confirms via `confirmBankTransfer()`. iDEAL: placeholder UI only.
- **IBAN / bankgegevens** — `User.iban` + `User.accountHolderName` voor uitbetalingen. Validatie via `src/lib/validations/iban.ts`: 37 EU/EER/CH/UK landen met exacte lengte per land + ISO 13616 mod-97 checksum. 30-dagen anti-fraude cooldown bij IBAN-wijziging via `User.lastIbanChange` (eerste keer is gratis). UI op `/dashboard/profiel` (BankDetailsForm). Helpers: `normalizeIban`, `isValidIbanFormat`, `formatIbanForDisplay`, `maskIban`.
- **Adresgegevens cooldown (Fase 27.108)** — `User.lastAddressChange DateTime?` + `ADDRESS_COOLDOWN_DAYS = 30` in `src/lib/address-cooldown.ts`. `getAddressCooldownInfo(lastAddressChange)` returnt `{canEdit, daysRemaining, availableAt}`. Server-side guard in `updateAddress` action — eerste wijziging gratis (lastAddressChange null), daarna 30d geblokkeerd. Apart bestand zodat client-componenten de constants mogen importeren (use-server bestanden mogen geen non-async exports). UI: AddressForm display met laatst-gewijzigd-datum + lock-icoon ipv pencil als cooldown actief is + amber waarschuwingsblok met "weer mogelijk over X dagen". Toegepast op `/dashboard/profiel` Adresgegevens-sectie + `/dashboard/verzending` (zelfde AddressForm component).
- **Distance display op item-cards (Fase 27.103-27.104)** — `src/lib/distance.ts` haversine + same-country check (NL ↔ NL only). Postcode-dataset `src/lib/data/postcodes-nl.json` (PC4 → [lat, lng] centroïden, ~150-entry seed; vervangen door volledige PDOK-set voor productie). `SellerLocationLine` gedeelde component toont "📍 Plaats · 12 km" onder seller-naam. Cross-border geeft vlaggetje (🇧🇪 / 🇩🇪) zonder afstand — je rijdt geen 600km voor een kaart. <1 km wordt "<1 km" (privacy-floor zodat seller's adres niet narrow-baar is via meerdere listings). PostalCode komt altijd van seller (Listing heeft geen eigen postcode-veld; pickupCity wordt auto-fill uit User.city). `getBuyerLocation()` in `shipping/filter.ts` levert `{country, postalCode}` voor buyer; ListingCard / AuctionCard / ClaimsaleCard / ListingListRow accepteren een optioneel `buyer` prop. Toegepast op marktplaats / veilingen / claimsales / homepage hoofdgrid + sponsored-rows.
- **Marktplaats list-view + filter sidebar (Fase 27.105-27.107)** — `/marktplaats` heeft nu een twee-koloms layout: links sticky `MarktplaatsFilterSidebar`, rechts toolbar (mobile-filter + view-toggle + sort) met lijst-rows of grid-cards. Default = lijst-view. `MarktplaatsMobileFilters` opent een slide-over drawer met counter-badge op <lg. ViewToggle is `hidden lg:inline-flex` — mobile users krijgen altijd lijst (grid was niet nuttig op klein scherm). Filter-state in URL voor shareable links + browser-back: `view`, `price_min/max`, `type`, `condition`, `pricing` (DIRECT/OFFER), `delivery` (SHIP/PICKUP), `free_shipping`, `radius` (5/10/25/50/100), `verified`, `since` (today/week/month/all). `parseListingFilters` + `buildListingFilterWhere` in `src/lib/listing-filters.ts`. Radius-filter doet post-filter in JS want SQLite kent geen haversine — fetcht eerst tot 500 candidates dan haversine + paginate. Sponsored-row alleen op page 1 zonder filters. Watchlist-IDs batch-loaded zodat hartjes correct vullen. Pricing-mode label: **"Direct kopen"** voor FIXED+allowDirectBuy, **"Voorstel doen"** voor NEGOTIABLE/acceptsOffers (geen "Bieden" — dat is veiling-jargon). Prijs-fallback (NEGOTIABLE zonder suggestedPrice) toont **"n.o.t.k."** (nader overeen te komen, NL-classifieds standaard). Delivery-badges sky-blue (verzenden) + amber (ophalen) — BOTH-listings tonen beide pills.
- **Uitbetaalsysteem** — `WithdrawalRequest` model, status `PENDING → APPROVED → PAID` of `REJECTED`. User-flow: `requestWithdrawal()` deduct `User.balance` direct, IBAN+naam wordt gesnapshot. Eén actieve aanvraag tegelijk per user. Admin keurt goed (`approveWithdrawal`), doet handmatige overboeking en markeert PAID (`markWithdrawalPaid`). `rejectWithdrawal` met verplichte reden refundt balance terug. Min €10, geen fee, max = available balance. UI: `/dashboard/uitbetalingen` (user) + `/dashboard/uitbetalingen/admin`. Config-constante `WITHDRAWAL_MIN_AMOUNT` in `src/lib/withdrawal-config.ts` (apart bestand zodat client-componenten het kunnen importeren — actions.ts mag geen non-async exports).
- **Account-suspension** — `User.suspendedUntil` + `User.suspensionType` (TEMPORARY/PERMANENT) + `User.suspensionReason`. Helper `src/lib/suspension.ts isUserSuspended()` + `requireNotSuspended(userId)` async guard. Toegepast op alle write-paden: `createAuction`, `placeBid`, `buyNow`, `setAutoBid`, `createListing`, `buyListing`, `createClaimsale`, `claimItem`, `checkout`, `createProposal`, `respondToProposal`, `sendMessage`. Toegestaan tijdens suspension: payout-aanvragen, dispute-reacties, `markAsShipped` (lopende verzendingen). Banner in dashboard layout. Admin suspend/lift via `suspendUser`/`liftSuspension` (knoppen op `/dashboard/rapporten/admin`).
- **Blokkeren + rapporteren** — `UserBlock { blockerId, blockedId, reason? }` symmetrisch (A blokkeert B → niemand ziet elkaar's content). `UserReport` met reasons SCAM/SPAM/HARASSMENT/INAPPROPRIATE/FAKE_LISTING/OTHER, status `OPEN → REVIEWING → DISMISSED of ACTION_TAKEN`. Helper `getBlockedUserIds(userId)` retourneert beide-richtingen Set. Filtering: `marktplaats/page.tsx`, `veilingen/page.tsx`, `claimsales/page.tsx`, `berichten/page.tsx` (NOT participants in blocked set), `sendMessage` (bidirectional check). Search/homepage/recommendations zijn NIET gefilterd (follow-up). UI: `BlockReportButtons` component op `/verkoper/[userId]`. User-side `/dashboard/blokkeerlijst`. Admin `/dashboard/rapporten/admin` gegroepeerd per gerapporteerde user met inline suspend-knop.
- **Annuleringsverzoek** — `CancellationRequest` op PAID `ShippingBundle` (werkt voor alle 3 sale-types). Reasons: BUYER_CHANGED_MIND/SELLER_OUT_OF_STOCK/DAMAGED/UNRESPONSIVE/OTHER. 7-dagen-deadline, één actief verzoek per bundle. ACCEPT door wederpartij refundt escrow + bundle CANCELLED + claimsale items terug naar AVAILABLE + listing terug op ACTIVE + multi-listing bundles flippen alle `BundleListing.listingId`s SOLD→ACTIVE. Cron `api/cron/cancellation-expiry` markeert verlopen als EXPIRED (verkoper blijft leveringsplichtig). UI: `CancellationsSection` bovenaan `/dashboard/aankopen` en `/dashboard/verkopen` (gehouden buiten bestaande Purchases/SalesContent voor regressie-veiligheid). Niet bedoeld voor SHIPPED bundles — daar treedt het bestaande dispute-systeem in. Niet voor EXTERNAL pickup-bundles — gebruik `cancelExternalReservation` (1-klik, geen wederzijds-akkoord).
- **Listing-statussen (Fase 27)** — `ACTIVE | PAUSED | RESERVED | PARTIALLY_SOLD | SOLD | DELETED`. PAUSED (tijdelijk verbergen via `pauseListing`/`resumeListing` — automatische cascade rejecteert PENDING enkel-listing proposals én PENDING multi-listing bundle-offers met systeembericht, zelfde patroon als DELETED-cascade), RESERVED (gekoppeld aan een ACCEPTED bundle-offer of partial-balance proposal), PARTIALLY_SOLD (Fase 27.13 — MULTI_CARD partial-sale + stocked SEALED/OTHER waar nog AVAILABLE-rijen zijn). Account-limits cap is op `status="ACTIVE"` — PAUSED/PARTIALLY_SOLD tellen niet mee. Pickup-locatie via `pickupCity` (auto-fill uit User.city; alleen plaats publiek). `closePartiallySoldListing` (Fase 27.34) sluit een PARTIALLY_SOLD listing zonder buyer-koppeling: status → SOLD, items blijven historisch op AVAILABLE, cascade-rejects PENDING. **Fase 27.83**: DRAFT verwijderd — concept-opslaan-en-later-publiceren gaf in de praktijk weinig waarde; verkopers vullen het form in één keer in. `saveDraft`/`publishDraft`/`deleteDraft` actions + `draftListingSchema` zijn weg. Bestaande DRAFT-rows zijn naar DELETED geflipt.
- **Koop-toggles + vraagprijs (Fase 27.31)** — `Listing.allowDirectBuy` (Boolean default true) + `Listing.acceptsOffers` (Boolean default true) + `Listing.suggestedPrice` (Float? voor NEGOTIABLE). Step-pricing-form heeft toggles voor FIXED-listings ("Direct Kopen toestaan", "Biedingen accepteren") en een suggested-price input voor NEGOTIABLE. `tradeable` is overwogen maar bewust niet geïmplementeerd: een ruil-toggle zonder echte ruil-flow zou kopers misleiden. COLLECTION krijgt een `conditionRange`-UI met van/tot selects in step-details die tot één string worden samengevoegd ("Lightly Played – Near Mint").
- **Direct Kopen voor non-stocked FIXED (Fase 27.33)** — `BuyNowButton` component op listing-detail page voor SINGLE_CARD/MULTI_CARD/COLLECTION met FIXED + allowDirectBuy + deliveryMethod !== "PICKUP". Hergebruikt `PaymentMethodModal`-patroon van BuyQuantityForm (single-quantity variant). NEGOTIABLE en FIXED+acceptsOffers krijgen "Bod doen"-knop (chat-route). Hint "verkoper accepteert alleen biedingen via chat" als Direct Kopen + biedingen beide uit staan. ContactSellerButton accepteert nu een optionele `label` + `variant` ("primary" | "outline") prop voor contextuele overrides.
- **Pickup payment-modes (Fase 27.39)** — `Listing.allowPlatformPickup` + `Listing.allowExternalPickup` (Boolean default true) bepalen welke betaal-modi koper mag kiezen voor PICKUP/BOTH listings. Server-validatie: minstens één moet aan staan. Form-toggles in step-shipping (alleen zichtbaar bij PICKUP/BOTH). `ShippingBundle.deliveryMethod` (SHIP|PICKUP) snapshot voor pickup-flow-routing. `DeliveryChoice` type (`SHIP | PICKUP_PLATFORM | PICKUP_EXTERNAL`) in `src/lib/listing-types.ts` (apart bestand omdat `use server` files geen type-exports mogen hebben). `buyListing` accepteert vierde param `deliveryChoice`: SHIP=PLATFORM-escrow met verzending; PICKUP_PLATFORM=PLATFORM-escrow zonder verzending, code-confirm bij ophaal; PICKUP_EXTERNAL=geen wallet, listing→RESERVED, bundle PENDING+EXTERNAL met 14d-cron-timeout. Helpers `reserveListingForExternalPickup` + `reserveStockedListingForExternalPickup` voor de EXTERNAL-paths. Detail-page rendert tot 4 koop-routes per listing afhankelijk van deliveryMethod + seller-toggles.
- **Bundle-offer 3-way (Fase 27.43)** — `createBundleOffer` accepteert nu `deliveryChoice` (SHIP|PICKUP_PLATFORM|PICKUP_EXTERNAL) ipv legacy `deliveryMethod`. Form rendert 3-way radio met disabled-state per knop op basis van per-listing seller-toggles (allowPlatformPickup/allowExternalPickup). Backend valideert dat alle gekozen listings de gekozen modus toestaan. PICKUP_PLATFORM bundle gaat via bestaande PLATFORM-branch in `respondToBundleOffer` (escrow + listings RESERVED→SOLD bij confirmPickup), PICKUP_EXTERNAL ongewijzigd.
- **EXTERNAL pickup 1-knops confirm (Fase 27.42)** — `confirmExternalPickup(bundleId)` action: koper-only, geen code, 1-klik, bundle SCHEDULED→COMPLETED, listings RESERVED→SOLD. Symmetrisch met `confirmDelivery` voor SHIP-bundles. Anti-self-inflation blijft (alleen koper kan bevestigen, niet seller). PickupActions component toont voor EXTERNAL bundles geen code-input meer maar groene "Bevestig ophaal"-knop. ActivePickupsSection in /aankopen toont voor EXTERNAL bundles `ExternalPickupConfirmButton` ipv prominent code-blok. Code-confirm met 4-cijfer + 1-letter code blijft alleen voor PLATFORM-bundles (escrow-release rechtvaardigt extra security).
- **Markeer-als-verkocht weg (Fase 27.40)** — Knop verwijderd uit `ListingActions` voor ACTIVE-state. Alle echte verkoop-flows (Direct Kopen, proposals, bundle-offers, pickup-confirm) zetten SOLD automatisch met cascade-rejection van pendings. Manuele flip miste cascade én vervuilde seller-stats. `updateListingStatus(id, "DELETED")` is de enige overgebleven handmatige status-flip — cascade is altijd actief.
- **Bundle-offer (Fase 27)** — Buyer-initieerd multi-listing voorstel via chat. Modal met multi-select uit `getRecentSellerListingsForBuyer`, intersectie van shipping-methods die alle gekozen listings ondersteunen, totaalbedrag (verzending inclusief). Listings blijven ACTIVE tijdens PENDING (geen DOS-vector). Bij seller-accept atomic flip ACTIVE→SOLD/RESERVED met `count===N` gate. SHIP-bundle met 100% saldo: bundle PAID + escrow direct. SHIP-bundle met partial saldo: 5d paymentDeadline, listings RESERVED, PENDING bundle pre-aangemaakt. PICKUP-bundle: paymentMode=EXTERNAL, geen wallet-impact, listings RESERVED, 14d pickup-reservation-timeout. Cron `bundle-offer-expiry` (3d) en `bundle-offer-payment-deadline` (5d). Multi-listing bundles op `ShippingBundle` via `BundleListing[]` join (listingId blijft @unique voor backward-compat — multi-listing bundles laten `listingId=null`). Counter-bod: schema heeft `parentProposalId`, action niet geïmplementeerd (follow-up).
- **Pickup uitgebreid (Fase 27)** — Off-platform betaling default (Tikkie/contant bij ophalen, geen escrow, geen commission). `paymentMode: "EXTERNAL"` op ShippingBundle + BundleProposal markeert dit. Status-flow voor EXTERNAL: PENDING (RESERVED-fase) → SCHEDULED (na pickup-moment-lock via `respondToPickup(ACCEPT)`) → COMPLETED (na 4-cijfer code-confirm via `confirmPickup`). PAID/SHIPPED-status overgeslagen. PickupSchedule per bundle (`shippingBundleId @unique`) bevat datum + tijdvenster + 4-cijfer code (server-generated, alleen buyer ziet hem). Code-confirm: `pickupCodeAttempts < 5`-gate, lockout 1u via `pickupLockedUntil` na 5e foute attempt. Bij PLATFORM-bundle: confirmPickup doet `releaseEscrow` naar seller. Bij EXTERNAL: alleen state-flip, geen geldverschuiving. Beide partijen kunnen ophaalmoment voorstellen en cancellen via `cancelExternalReservation` (1-klik op EXTERNAL-bundles). Cron `pickup-reservation-timeout` (14d) en `pickup-reminder` (24u voor afspraak). UI in `PickupActions` ingebed in `BundleOfferMessage` chat-bubble.
- **Claimsales immutable** once LIVE (DRAFT → LIVE, no edits). `cardSetId` optional on ClaimsaleItem. Form uses front/back image uploads per card (stored as JSON array in `imageUrls`). `coverImage` on Claimsale for thumbnail. `reference` field stores card number.
- **Mobile card layout** — All 3 main pages (veilingen, marktplaats, claimsales) use horizontal cards on mobile (image left, info right) with fixed `Image` width/height (not `fill`+`aspect-ratio` — that breaks in flex containers on mobile). Desktop uses vertical cards with `fill`+`aspect-square`.
- **Page color theming** — Create buttons on main pages: Veilingen=blue (`bg-primary`), Marktplaats=green (`bg-emerald-600`), Claimsales=dark-yellow (`bg-amber-600`). Only on overview pages.
- **`upload.ts` is server-only** — contains `fs/promises`. Client components must NOT import from it. Use inline `parseImageUrls` instead. **Type-only import van `UploadContext` mag wel** (via `import type`, wordt door bundler ge-erased).
- **Bestandsopslag: R2 in prod, schijf lokaal (Fase 41)** — geüploade foto's gaan in productie naar **Cloudflare R2** (object-storage; `src/lib/r2.ts` S3-client met `isR2Configured()`-schakelaar). Lokaal (geen `R2_*`-env-vars) valt `saveUploadedFile` + de serve-route `/api/uploads/[filename]` terug op `public/uploads/` op schijf. **Het URL-contract `/api/uploads/{filename}` blijft in beide gevallen identiek** — de DB bewaart alleen dat tekstpad, nooit de foto zelf, dus geen DB-URL-migratie en de UI/cron hoeven niet te weten waar de bytes staan. Reden: Railway's containerschijf wordt bij elke redeploy gewist. `deleteUploadedFile(url)` (niet-throwend, no-op op externe/seed-URLs via strikte `extractUploadFilename`-regex) verwijdert van R2/schijf — gebruikt door de `cleanup-sold-images` cron die 30d na voltooide verkoop de foto-bestanden opruimt en de DB-foto-velden leegt (tekst blijft). Serve-route is `runtime="nodejs"`. Follow-up voor schaal/mobiel: publieke R2-URL of custom domain i.p.v. proxy via de serve-route (vereist URL-contract-wijziging + `next.config`).
- **Image content-moderation** — elke upload via `saveUploadedFile(file, {context, userId})` voor de contexten `listing | auction | avatar | chat` wordt sync door Claude Haiku 4.5 vision gecheckt (`src/lib/moderation.ts`, `@anthropic-ai/sdk`, forced tool-use voor structured `{safe, reason}`). 9 afkeur-categorieën (NSFW/geweld/haat/politiek/illegaal/obscene-gebaren/grove-tekst/QR-codes/spam) elk met vaste NL-reden; officiële TCG-branding (Pokémon/Magic/Yu-Gi-Oh!-logo's) is expliciet vrijgesteld van de spam-categorie. **Hard-reject** bij hit (`saveUploadedFile` throwt `MODERATION_BLOCKED:<reason>`, route/action vertaalt naar user-message). **Fail-open** bij API-down/timeout (8s `AbortSignal.timeout`) of ontbrekende `ANTHROPIC_API_KEY` — upload gaat door, gelogd als `skipped_*`. Elke check schrijft een `ImageModerationLog`-rij. Contexten `claimsale` + `verification` worden bewust NIET gecheckt (legitiem persoonlijke documenten); dispute/ship-bundle-uploads geven geen context mee. `ImageUploader` heeft optionele `context`-prop; directe `/api/upload`-fetchers geven `context` via FormData mee.
- **Anti-snipe** — bids in last 2 min → +2 min extension
- **Bid increments** — per price tier (`src/lib/auction/bid-increments.ts`)
- **Account tiers (Fase 31)** — `FREE` / `PRO` / `UNLIMITED` / `ENTERPRISE` (config in `src/lib/subscription-tiers.ts`). ADMIN is a role, mapt nu naar **ENTERPRISE** perks (was UNLIMITED). Prijzen + commissies: FREE €0 / 3% · PRO €14,95 of €149/jr / 2,5% · Unlimited €39,95 of €399/jr / 2% · Enterprise €749/m custom-mogelijk / 0%. Enterprise is geen self-serve, alleen admin-grant via `/dashboard/admin/enterprise-requests`. Break-evens in `TIER_BREAK_EVENS_MONTHLY`. Tier-features: bulk-upload (PRO+), vanity shop-slug (Unlimited+), search-boost (Unlimited+), account-manager (Enterprise), N gratis HOMEPAGE_SPOTLIGHTs/maand (PRO=1, Unlimited=5, Enterprise=999). `User.tierRank` denormalisatie (1-4) gevuld bij `upgradeToTier` + downgrade-cron voor secondary-sort search-boost in marktplaats/veilingen/claimsales/zoeken (tier-key altijd SECONDARY achter primary sort, geen page-1-dominance). `Subscription.legacyPricing`-veld gereserveerd voor toekomstige prijswijziging zodra er echte users zijn. **Mollie recurring-billing is uit deze fase getild** (open Fase 17): hooks gereserveerd op `User.mollieCustomerId` + `Subscription.mollieSubscriptionId/paymentStatus/gracePeriodEnd/nextBillingAt`, knoppen "Upgrade naar PRO/Unlimited" disabled met "Binnenkort beschikbaar". Enterprise-aanvraag-flow werkt wel (handmatige admin-grant via `upgradeToTier(userId, "ENTERPRISE", cycle, customMonthlyPrice)`).
- **Auction buyer's premium (Fase 31)** — 3% over winnend bod, geldt voor ALLE auction-paden (gewone bid, Buy Now mét bids, Buy Now zonder bids). Geen tier-discount op de premium — Enterprise heeft al 0% seller-commissie als incentive. Premium gaat 100% naar platform via `Transaction { type: "AUCTION_PREMIUM" }`, seller krijgt z'n bod minus tier-commissie via `releaseEscrow` zoals voorheen. Pure helper `calculateBidFees(bid)` returnt `{bid, premium, total}` in `src/lib/auction/fees.ts`. **Reserve gaat over `total` (bid + premium)**, niet alleen bid — `calculateReserveAmount` in `balance-check.ts` is intern aangepast om de premium op te tellen, alle bestaande callers werken automatisch correct. Wallet-helper `deductBidPayment(userId, bid, premium, desc, auctionId)` doet één $transaction met balance-decrement + 2 Transaction-rijen (PURCHASE + AUCTION_PREMIUM) zodat de financiele trail klopt. **Publiek zichtbaar bod = zonder fee** — `AuctionBid.amount` slaat alleen het bod op, premium wordt on-the-fly berekend voor de winnaar. Bid-confirmation modal (`src/components/auction/bid-confirmation-modal.tsx`) opent bij eerste bod per veiling (per-veiling localStorage-tracking + globale `User.skipBidConfirmation` toggle in /dashboard/profiel) met breakdown bod/fee/totaal/reserve. AuctionFeeExplainer component op auction-detail page met collapsible "ⓘ 3% veilingkosten" + 4 bullets (bescherming/anti-fraude/platform/support).
- **Seller levels** — 14-tier brand-neutral collector progression (`src/lib/seller-levels.ts`). XP earned via: 1 XP/day account age, 1 XP/€ sold, 1 XP/€ bought, 20 XP per 5-star review received, 5 XP per review given, 10 XP per completed transaction. Tiers: Beginner(0)→Rookie(100)→Scout(300)→Collector(750)→Hunter(1.5k)→Trader(3k)→Veteran(5k)→Curator(8k)→Elite(12k)→Appraiser(17k)→Master(23k)→Grandmaster(32k)→Legend(50k)→Champion(100k XP). `nameKey` field stays stable across renames so existing `User.profileBanner` references keep working. Banner image assets removed; level banners now render as branded gradients with the level icon. Data fetching sums revenue amounts, not counts.
- **Customization & Ember** — Earn-only currency (`User.emberBalance`); paid Ember purchases removed (gambling-risk pivot). Earn paths: completed purchase/sale (15), review given (10), review received (5), bid (3), listing (5), daily login (10–500 via `LOGIN_STREAK_REWARDS`). Daily caps per tier in `MAX_DAILY_EMBER_BY_TIER`. `EmberTransaction` logs every flow. Cosmetic items (`CosmeticItem`) live in named **Chapters** (`CosmeticBundle`); rarity display labels are TCG slang (Pull/Hit/Chase/Grail/One-of-One/Rainbow) but DB keys stay UNCOMMON–SHINY. Equipped via `User.profileBanner/profileEmblem/profileBackground` keys.
- **Achievements (tiered, CoD-style)** — `Achievement` (parent) + `AchievementTier` (1..N tiers per achievement). Engine in `src/lib/achievements.ts` defines 9 achievements (days-online, purchases-completed, total-spent, sales-completed, total-earned, reviews-given, five-stars-received, login-streak, founder-member) with up to 5 tiers each. `checkAchievements(userId)` recomputes raw progress and grants rewards (Ember + XP + optional cosmetic) for **every** newly-crossed tier in one transaction — handles back-fills cleanly. Triggered on purchase complete, sale complete (auto-confirm too), review submit, login-streak claim. Per-user state in `UserAchievement` (currentTier, acknowledgedTier, progress). Unshown unlocks (currentTier > acknowledgedTier) surface as Trophy-styled `sonner` toasts via `<AchievementUnlockListener>` in the locale layout — fires on mount + after each navigation. Catalog seeded via `prisma/seed-achievements.ts`.
- **Disputes** — buyer opens on SHIPPED bundle (10–30 days after shipment); seller responds; buyer accepts/rejects; mutual proposals; escalation (both parties agree) → admin resolves. Event-sourced timeline via `DisputeEvent` model. Auto-resolve cron for unresponded disputes. Actions in `dispute.ts`, UI in `dispute-detail-content.tsx`, page in `dashboard/geschillen/[disputeId]/page.tsx`
- **Chat proposals** — Buyer/seller can make buy/sell price proposals in chat linked to a listing. Proposal model with PENDING/ACCEPTED/REJECTED/WITHDRAWN status. 40% minimum balance rule applies. Accepted proposals: mark listing SOLD, create ShippingBundle, escrow flow. `withdrawProposal()` zet status WITHDRAWN (proposer trekt zelf in — onderscheidt van REJECTED waar wederpartij afwijst). UI toont andere tekst per status.
- **Listing.DELETED cascade** — `updateListingStatus(id, "DELETED")` rejecteert alle PENDING proposals automatisch met systeembericht in chat + notificatie naar de proposer.
- **Claimsale CLOSED** — auto-close via `closeClaimsaleIfDepleted()` zodra alle items SOLD/DELETED zijn (aangeroepen vanuit `deleteClaimsaleItem` en `cart.checkout` per affected claimsale). Manuele close via `closeClaimsale()` action ("Sluit claimsale"-knop in `claimsale-actions.tsx`) — race-safe transactie met re-check van CLAIMED items + CAS op `claimsale.status="LIVE"→"CLOSED"`. CLAIMED items (= items in iemands cart) blokkeren close. Belt-and-braces: cartItems opruimen na close. CLOSED claimsales worden uit publieke views gefilterd (alle bestaande queries gebruikten al `status: "LIVE"`).
- **Buy Now price** — Optional on auctions. Removed when bids reach 75% of buy now price (not after first bid). Check in both `placeBid()` and `resolveAutoBids()`.
- **Auction upsells** — `AuctionUpsell` model (same structure as `ListingUpsell`). Higher pricing than listings: Homepage Spotlight €0.75/day, Category Highlight €0.40/day, Urgent Label €0.25/day. Config in `upsell-config.ts` (`AUCTION_UPSELL_PRICING`).
- **Auction types** — `SINGLE_CARD | MULTI_CARD | COLLECTION | SEALED_PRODUCT | OTHER` (same as listing types). Type-specific fields: `cardItems`, `estimatedCardCount`, `conditionRange`, `productType`, `itemCategory`.
- **Auction durations** — 3, 5, 7, or 14 days (no 1-day).
- **Auction-tijdvenster (Fase 34)** — Sellers kiezen op de aanmaakpagina drie dingen: `startDate` (vandaag t/m vandaag+5d), `duration` (3/5/7/14d) en `endTimeOfDay` ("HH:MM" op de eind-dag). Server berekent definitief via `deriveAuctionWindow` in [src/lib/auction/timing.ts](src/lib/auction/timing.ts) — DST-correct via `Intl.DateTimeFormat` met `timeZone: "Europe/Amsterdam"`, geen externe deps. **Marktplaats-conventie = vaste tijdzone Europe/Amsterdam** (label "🕒 NL-tijd"); buyers zien countdown in eigen lokale tijd. Bij `startTime > now + SCHEDULED_THRESHOLD_MS (5 min)` krijgt de auction nieuwe status `SCHEDULED` ipv `ACTIVE`; daaronder is het instant-publish (gedraagt zich identiek aan oud "live-from-creation"). Bid-guards in `placeBid`/`buyNow`/`setAutoBid` weigeren SCHEDULED met error "Deze veiling is nog niet gestart". Account-limits (`checkAuctionLimit`) tellen ACTIVE+SCHEDULED zodat de cap niet ontwijkbaar is. Sweet-spot-hint "💡 Tussen 19:00 en 21:00 zijn de meeste kopers actief" in de form (highlight bij `isSweetSpot`).
- **Auction-activator scheduler (Fase 34)** — Mirror van `auction-scheduler.ts` voor de SCHEDULED→ACTIVE flip. [src/lib/auction-activator-scheduler.ts](src/lib/auction-activator-scheduler.ts) doet sub-seconde-precieze activatie via in-process setTimeout op `MIN(startTime)`; bij fire flipt 'ie alle `startTime ≤ now`-veilingen naar ACTIVE (race-safe via conditional `updateMany`), publisht `auction-started` realtime event op `auctionChannel(id)`, bumpt vervolgens de finalize-scheduler. Cron `auction-activate` (elke minuut) is safety-net. Boot-init in [src/instrumentation.ts](src/instrumentation.ts) start drie in-process schedulers: auction-scheduler (finalize-flip), auction-activator-scheduler (SCHEDULED→ACTIVE), en expire-claims-scheduler (Fase 35 — claimsale-items na 15min uit cart releasen). Detail-page (`live-auction-content.tsx`) verbergt bid-form bij SCHEDULED en toont countdown naar `startTime` ipv `endTime` + listent op `auction-started` event om automatisch te switchen wanneer activator flipt.
- **Auction-form-redesign (Fase 34)** — `multi-step-auction-form.tsx` heeft nu een sticky `<AuctionFormProgress>` (6 stappen: Type · Foto's · Details · Prijzen · Tijd · Bezorging) met `IntersectionObserver` voor active-state, smooth-scroll-on-click. Op `lg+` 2-koloms layout met sticky `<AuctionFormPreview>` rechts (live-mock van auction-card). Looptijd is uit Pricing gehaald en in een eigen "Tijdvenster"-sectie geplaatst (`step-timing.tsx`) met date/time-pickers (HTML5 native, geen extra dep) + sweet-spot-hint + live-preview-string. Sticky bottom-bar gebruikt chip-array voor missing-fields ipv een single-string-warning. Bezorging + Promotie-opties zijn samengevoegd in één sectie voor minder visuele fragmentatie.
- **Sponsored listings & auctions** — Items with active `CATEGORY_HIGHLIGHT` upsell appear in "Gesponsord" row on marketplace/veilingen page with tooltip explanation.
- **Marketplace/Auction pagination** — 40 items per page, sponsored excluded from main grid, 4-column layout.
- **Real-time bid updates** — Polling via `/api/auctions/[auctionId]/bids` endpoint. 5-second interval normally, 2-second in last 30 minutes. Client component: `live-auction-content.tsx`. Shows "Je bent de hoogste bieder" banner + hides bid form when user is highest bidder.
- **SQLite date handling** — no DATE_TRUNC; fetch raw + group in JS
- **Admin panel (Fase 15)** — gecentraliseerde backoffice op `/dashboard/admin/*`. Eigen sub-nav (`src/components/admin/admin-nav.tsx`) met 4 secties: Overview · Queues (disputes/verifications/withdrawals/buybacks/reports/bank-transfers) · Users & Content (users/moderation/catalog) · System (config/crons/audit). Toegang via header shield-icon (`AdminShield`, alleen zichtbaar bij `session.user.accountType === "ADMIN"`) + 1 sidebar-link in `dashboard-nav.tsx`. Layout-guard: `dashboard/admin/layout.tsx` roept `requireAdminPage()` aan. **Elke admin-write-action moet door 2 hekken: `requireAdmin()` van `@/lib/admin` (of `requireAdminPage()` voor pages) ÉN `logAdminAction()` van `@/lib/admin-audit` na succes.** Audit log is forward-only — `AdminAuditLog`-model, geen retro-backfill. Alle bestaande admin-actions in `admin-suspension.ts`, `withdrawal.ts`, `dispute.ts` (`adminResolveDispute`), `verification.ts` (`adminReviewVerification`), `buyback.ts` (`updateBuybackStatus`), `wallet.ts` (`confirmBankTransfer`), `block-report.ts` (`reviewReport`) zijn geïnstrumenteerd. JWT-callback in `auth.ts` exposeert `accountType` op `session.user` zodat client-componenten admin-status kunnen detecteren. Oude admin-paden (`/dashboard/geschillen/admin`, `/dashboard/uitbetalingen/admin`, `/dashboard/inkoop/admin[/id]`, `/dashboard/rapporten/admin`) zijn redirect-stubs naar het nieuwe `/dashboard/admin/*`-pad — nieuwe admin-flows nooit ergens anders dan onder `/dashboard/admin/*` plaatsen.
- **Cron-logging & manual run (Fase 15)** — alle cron-routes onder `src/app/api/cron/` zijn gewrapt met `withCronLogging(jobName, fn)` uit `@/lib/cron-logging`, wat per executie een `CronRun`-record produceert (status RUNNING → SUCCESS/FAILED, items processed, errorMessage, triggeredBy). Auth via `resolveCronTrigger(request)` uit `@/lib/cron-auth` — accepteert `Bearer CRON_SECRET` (scheduler) of een ingelogde admin-sessie (manual). Centrale registry in `src/lib/cron-jobs.ts`: `CRON_JOBS` (job-runners) + `CRON_JOB_META` (description, schedule, `allowManualRun`, optionele `runWarning`). De admin-page `/dashboard/admin/crons` toont status + "Run nu"-knop alleen voor jobs met `allowManualRun: true`. **Niet manueel runbaar:** `cleanup-archived-chats` (destructief, hard-delete). **Met waarschuwing:** `sync-pokewallet` (heavy: ~600 PokeWallet API-calls). `runCronManually` (in `src/actions/admin/crons.ts`) weigert een job als de meta `allowManualRun: false` heeft — backend-guard, niet alleen UI.
- **Real-time SSE-infrastructuur (Fase 30)** — Eén EventSource per browser-tab via `<RealtimeProvider>` in locale-layout, gevoed door `/api/events/stream` (Node-runtime, heartbeat 25s). Server-side publishen via `publish(channel, event)` uit `@/lib/realtime`; channels-Map staat op `globalThis` zodat HMR-reloads in dev mode niet leiden tot 0-subscriber-bug. **Channel-naming**: `user:${userId}` voor private events (auto-subscribed via session-id), `auction:${id}`/`claimsale:${id}`/`listing:${id}` voor publieke broadcasts (opt-in via `?channels=` query, whitelisted op prefix om DoS-vector tegen user-channel-sniffing af te vangen). **Event-types** gedefinieerd als type-union in `realtime.ts`: outbid/bid-placed/auction-won/auction-ended/balance-changed/achievement-unlocked/notification-created/new-message/claimsale-item-claimed/cart-changed/listing-changed/bundle-changed/dispute-changed/withdrawal-changed/verification-changed/suspension-changed. **Client-side**: components subscriben via `useRealtime()` context — voor pages met SSR-state gebruik `useRefreshOnRealtime([types], filter?)` of drop-in `<RealtimePageRefresh events={[...]}>` in server-rendered layouts; voor resource-channels (auction/claimsale/listing) gebruik `<ListingRealtimeWatcher listingId>` of inline `registerChannels([...])`. **Polling blijft als fallback** voor disconnected SSE: notification-bell/message-icon/cart-icon op 30s, live-auction op 15s/2s laatste 30 min, live-claimsale op 15s, chat op 5s/15s adaptive. **Bell-based notification UX**: alle attention-grabbing events lopen via `<NotificationBell>` + `<MessageIcon>` met hover-popover + shake-animatie + auto-flash bij nieuwe events; klik navigeert naar overzichtspagina. Toasts zijn alleen behouden voor `<AchievementUnlockListener>` (rich Trophy-celebration). **Hosting-aanname**: Railway single-instance Node-container — voor multi-instance (Vercel/replicas) zou alleen `realtime.ts` swappen naar Redis pub/sub via ioredis (~30 regels), interface ongewijzigd.

---

## Codebase Map

### Server Actions (`src/actions/`)
| File | Purpose |
|------|---------|
| `auth.ts` | Register, login |
| `profile.ts` | Update profile (avatar, bio, display name), username change (€3.99, cooldown) |
| `wallet.ts` | Balance, deposit, withdrawal, escrow (credit/release/refund), `getBalanceSummary`, `confirmBankTransfer` (admin). Alle inkomende-credit-paden (`creditBalance`/`releaseEscrow`/`partialRefundEscrow`/`refundEscrow`/`adminDeposit`/`confirmBankTransfer`/`refundAuctionPremium`) doen FIFO `settlePendingFees()` vóór de balance-credit (post-Fase-33) |
| `auction.ts` | Create auction (force `runnerUpEnabled=false` als seller `maxRunnerUpAttempts=0` heeft), place bid (10% reserve + adres-check voor SHIP), end auction, buyNow, `completeAuctionPayment` (atomic flip naar PAID, race-safe vs cron), `finalizeAuction`, `acceptRunnerUpOffer`/`declineRunnerUpOffer`/`getActiveRunnerUpOffersForUser` (post-Fase-33 72u-window flow) |
| `verification.ts` | Submit/review/revoke ID verification, admin approve/reject |
| `claimsale.ts` | Create/manage claimsales with items |
| `listing.ts` | CRUD listings + `buyListing` (escrow) + `pauseListing`/`resumeListing`/`closePartiallySoldListing` (Fase 27) + cascade naar PENDING proposals én bundle-offers |
| `bundle-offer.ts` | (Fase 27) `createBundleOffer` (buyer), `respondToBundleOffer` (seller), `withdrawBundleOffer`, `completeBundleOfferPayment`, `getRecentSellerListingsForBuyer` |
| `pickup.ts` | (Fase 27) `proposePickup`, `respondToPickup`, `confirmPickup` (4-cijfer code + 5-attempts lockout), `cancelExternalReservation`, `getPickupCode` |
| `cart.ts` | Cart checkout with shipping + address snapshot |
| `purchase.ts` | Purchase queries (bundles, status) |
| `dispute.ts` | Open/respond/escalate/resolve disputes |
| `proposal.ts` | Create/respond/withdraw/complete proposals via chat |
| `message.ts` | Conversations, send messages (with photo), archive/delete/restore |
| `watchlist.ts` | Add/remove watchlist items |
| `notification.ts` | Create notifications, mark read, unread count |
| `review.ts` | Submit seller reviews, `getSellerStats` (revenue-based XP + username history) |
| `search.ts` | Global search across auctions/listings/claimsales |
| `address.ts` | Update user address |
| `shipping-method.ts` | CRUD seller shipping methods |
| `subscription.ts` | Upgrade/cancel/downgrade tier, subscription info |
| `customization.ts` | Ember balance, login-streak claim, owned items, equip/unequip cosmetic slots, list active chapters/bundles |
| `achievements.ts` | `getPendingUnlocks` + `acknowledgeAllUnlocks` for the celebration listener |
| `withdrawal.ts` | `requestWithdrawal`, admin `approveWithdrawal`/`rejectWithdrawal`/`markWithdrawalPaid`, `getMyWithdrawals`, `getActiveWithdrawal`, `getAllWithdrawals` |
| `block-report.ts` | `blockUser`/`unblockUser`/`isUserBlocked`, `reportUser`, admin `getReports`/`reviewReport` |
| `admin-suspension.ts` | Admin `suspendUser` (TEMPORARY 1-365 dagen of PERMANENT) + `liftSuspension` + `getSuspendedUsers` |
| `cancellation.ts` | `requestCancellation`, `respondToCancellation` (ACCEPT refund + bundle CANCELLED, REJECT met reden), `getActiveCancellationRequest`. Reasons-enum geëxporteerd voor UI |
| `admin/users.ts` | `resetIbanCooldown`, `forceUsernameReset` (Fase 15) — **alle nieuwe admin user-management actions hier toevoegen** |
| `admin/catalog.ts` | `createSeries`/`updateSeries`/`createCardSet`/`updateCardSet`/`updateCardMeta` (Fase 15) |
| `admin/app-config.ts` | `upsertAppConfig`/`deleteAppConfig` (Fase 15) — JSON-validatie server-side |
| `admin/crons.ts` | `runCronManually(jobName)`, `getCronStatus()` (Fase 15) — guard via `CRON_JOB_META.allowManualRun` |
| `admin/moderation.ts` | `bulkRemoveListings`/`bulkRemoveAuctions`/`bulkRemoveClaimsales` met reden + notificaties (Fase 15). Auctions → CANCELLED, Claimsales → CLOSED, Listings → DELETED (geen DELETED-status in Auction/Claimsale schema) |
| `enterprise.ts` | (Fase 31) `submitEnterpriseRequest` (gebruiker, zod-validatie, notify ADMINs), `approveEnterpriseRequest`/`rejectEnterpriseRequest` (admin via `requireAdmin`), `getMyPendingEnterpriseRequest`. Approve roept `upgradeToTier(...,"ENTERPRISE", cycle, customMonthlyPrice)` aan |

### Pages (`src/app/[locale]/`)
| Route | Page |
|-------|------|
| `/` | Homepage |
| `/veilingen/*` | Auction list (Fase 35: marktplaats-stijl two-column layout met filter-sidebar + list/grid view-toggle, status: { in: ["ACTIVE", "SCHEDULED"] }, ScheduledStartPill indigo voor SCHEDULED), create (Fase 34/35: 7-step met Promotie als eigen sectie), detail + bidding |
| `/claimsales/*` | Claimsale list (Fase 35: marktplaats-stijl two-column layout met filter-sidebar + list/grid view-toggle, claimsale-list-row met 7 thumbnails preview), create, detail + claim |
| `/marktplaats/*` | Listing list (sponsored row + pagination), create, detail |
| `/winkelwagen` | Shopping cart + checkout |
| `/zoeken` | Search results |
| `/berichten/*` | Messages/conversations (photo upload, proposals) |
| `/verkoper/[userId]` | Public seller profile |
| **Dashboard** `/dashboard/` | |
| `…/` | Home: 4 essentials-widgets voor IEDEREEN (action-items, saldo, lopende activiteit, recente bundles); CTA naar `/dashboard/statistieken` alleen voor PRO/UNLIMITED |
| `…/statistieken` | Premium statistieken (PRO/UNLIMITED): sales/seller-performance/buyer/XP/commission-savings, periode-filter incl. month/ytd quick-ranges, CSV-export per sectie |
| `…/profiel` | Edit profile |
| `…/saldo` | Wallet/balance + PendingFeesBanner (post-Fase-33) |
| `…/saldo/openstaande-kosten` | (post-Fase-33) Detail-page met open + afgeloste `PendingPlatformFee`-rijen |
| `…/veilingen` | My auctions |
| `…/claimsales` | My claimsales |
| `…/marktplaats` | My listings |
| `…/aankopen` | My purchases |
| `…/verkopen` | My sales |
| `…/biedingen` | My bids |
| `…/volglijst` | My watchlist |
| `…/meldingen` | Notifications |
| `…/reviews` | My reviews |
| `…/verzending` | Shipping address + methods |
| `…/geschillen` | Disputes overview + detail |
| `…/verificatie` | Account verification upload/status |
| `…/abonnement` | Subscription tier management |
| `…/uitbetalingen` | Saldo + IBAN + uitbetalingsformulier + history |
| `…/uitbetalingen/admin` | Redirect-stub → `/dashboard/admin/withdrawals` (Fase 15) |
| `…/blokkeerlijst` | "Block-list" — geblokkeerde gebruikers met unblock-actie |
| `…/rapporten/admin` | Redirect-stub → `/dashboard/admin/reports` (Fase 15) |
| `…/geschillen/admin` | Redirect-stub → `/dashboard/admin/disputes` (Fase 15) |
| `…/geschillen/admin/verificaties` | Redirect-stub → `/dashboard/admin/verifications` (Fase 15) |
| `…/inkoop/admin` (+ `[id]`) | Redirect-stubs → `/dashboard/admin/buybacks(/[id])` (Fase 15) |
| **Admin Panel** `/dashboard/admin/` | **Admin-only**, eigen layout met `requireAdminPage()` + `AdminNav`-sidebar |
| `…/admin` | Overzicht: KPI-tegels per queue, financiële totalen, recent audit-feed, quick-actions |
| `…/admin/disputes` | Escalated disputes (verplaatst van `/dashboard/geschillen/admin`) |
| `…/admin/verifications` | PENDING ID-verificaties (verplaatst van `/dashboard/geschillen/admin/verificaties`) |
| `…/admin/withdrawals` | Withdrawals queue: pending/approved/paid/rejected |
| `…/admin/buybacks` (+ `[id]`) | Buyback-aanvragen overzicht + detail |
| `…/admin/reports` | User-reports gegroepeerd per gerapporteerde user, inline suspend-knop |
| `…/admin/seller-warnings` | (Fase 28) Auto-geannuleerde PAID-bundles (14d zonder shipping). Repeat-offenders-tabel + chronologische event-lijst, accent op sellers die cancel-aanvraag afwezen vóór ghosting |
| `…/admin/bank-transfers` | Search + confirm-form voor `confirmBankTransfer`, recente confirmaties uit audit log |
| `…/admin/users` (+ `[userId]`) | User-search met filters + 360° detail-page met 11 tabs (profile/wallet/sales/purchases/disputes/withdrawals/reports/suspensions/reviews/ember/audit) |
| `…/admin/catalog` | Series/CardSets/Cards CRUD via sub-tabs |
| `…/admin/config` | AppConfig key/value editor met live JSON-validatie |
| `…/admin/crons` | Cron-status + description per job + "Run nu" (alleen voor jobs met `allowManualRun: true`) |
| `…/admin/moderation` | Listings/auctions/claimsales tabs met "alleen verkopers met open rapport"-filter + bulk-delete-met-reden |
| `…/admin/audit` | AdminAuditLog feed met filters (action, targetType, targetId-search) en paginatie |
| `…/admin/enterprise-requests` | (Fase 31) Enterprise-aanvraag queue met PENDING/APPROVED/REJECTED filter-tabs + counts. Inline approve-form (custom monthlyPrice + billingCycle) + reject-form (verplichte reden) |
| `…/abonnement/enterprise-aanvraag` | (Fase 31) User-aanvraag-form voor Enterprise-tier (shopName, geschatte maandomzet, telefoon, motivatie). Toont pending-status als er al een aanvraag loopt |
| `/winkel/[slug]` | (Fase 31) Vanity-redirect: lookup `User.shopSlug` waar `accountType in [UNLIMITED,ENTERPRISE,ADMIN]` → server-side redirect naar `/verkoper/<userId>`. notFound() bij downgrade |
| `/marktplaats/bulk-upload` | (Fase 31) PRO+ feature. 3-staps wizard (upload→preview→result) voor CSV-import van listings. Tier-gate met upgrade-CTA voor FREE |
| **Customization** `/customization/` | Hub: Ember balance, login streak, nav cards |
| `…/achievements` | Tiered achievements overview, progress per category |
| `…/packs` | Chapters overview (cosmetic collections) |
| `…/inventory` | Owned cosmetic items grid, filter/sort |
| `…/equip` | Equip banner / emblem / background slots |

### Components (`src/components/`)
| Folder | Key files |
|--------|-----------|
| `auction/` | `auction-card` (Fase 35: status+startTime props, ScheduledStartPill indigo voor SCHEDULED, `<AuctionLabels />` render onder titel, "Kopen nu" inline pill verwijderd), `multi-step-auction-form` (Fase 35: 7-step met Promotie als eigen sectie + Megaphone-icoon, scrollTo top op preview-klik, mobile-bottom-bar flex-col), `auction-form-progress` (Fase 35: 7e chip "Promotie"), `auction-form-preview` (Fase 35: live-mock met labels), `steps/` (type [Fase 35: RectangleVertical icoon ipv CreditCard], photos, details, pricing, timing [Fase 34], upsells [Fase 35: vertikale stack + dual-handle slider + LabelsBlock met stale-state-fix + kleur-popover via Pencil], review [Fase 35: PromotionCostBlock]), `autobid-form`, `bid-section`, `quick-bid-buttons`, `countdown-timer`, `live-auction-content` (Fase 35: `<AuctionLabels size="md" />` in hero), `sponsored-row`, `auction-labels` (Fase 35 NIEUW — render-component met COLOR_CLASSES + LABEL_TEXT-mapping, formatPrice voor DIRECT_KOPEN, size sm/md), `auction-list-row` (Fase 35 NIEUW — countdown-overlay links, indigo voor SCHEDULED, "Geen reserve"-pill subtiel, labels mt-2, "Bied nu"/"Bekijk" conditioneel), `auction-view-toggle` (Fase 35 NIEUW), `veilingen-filter-sidebar` (Fase 35 NIEUW — kopie van marktplaats-sidebar met auction-filters: looptijd/buy-now/geen-reserve/heeft-biedingen), `veilingen-mobile-filters` (Fase 35 NIEUW), `auction-owner-actions` (Fase 35: cancel-confirm-dialog vermeldt refund-bedrag + sonner-toast) |
| `claimsale/` | `claimsale-form`, `claimsale-card`, `claimsale-actions`, `claimsale-items-filter`, `claim-button`, `add-to-cart-button`, `claimsale-list-row` (Fase 35 NIEUW — cover-image met "X kaarten" amber-pill + amber voortgang-bar + 7 thumbnails 96×128 rounded-sm sizes=256px quality=90 + "+N meer" dashed-tile, prijsrange + amber "Bekijk items"-knop), `claimsale-view-toggle` (Fase 35 NIEUW), `claimsales-filter-sidebar` (Fase 35 NIEUW — filters: prijsrange, itemType, hasShipping, itemCountMin, radius, verifiedOnly, since), `claimsales-mobile-filters` (Fase 35 NIEUW) |
| `listing/` | `listing-form`, `multi-step-listing-form` (delivery-method-toggle + pickup-locatie velden + koop-toggles + sectie-status-pills + checklist-bar, Fase 27/27.81/27.83), `steps/` (incl. conditionRange UI voor COLLECTION + buy-options + suggested-price, Fase 27.31), `listing-card` (stock-badge "X op voorraad" voor SEALED/OTHER, Fase 27.36; n.o.t.k. price-fallback Fase 27.109), `listing-actions` (status-aware: ACTIVE/PAUSED/RESERVED/PARTIALLY_SOLD knoppen incl. "Advertentie afsluiten", Fase 27.34/27.83), `listing-status-badge` (Fase 27), `buy-quantity-form` (stocked, Fase 27.23), `buy-now-button` (non-stocked FIXED Direct Kopen, Fase 27.33), `sponsored-row`, `listing-list-row` (Fase 27.105 — Marktplaats-stijl horizontale rij: image + title/condition-pill/desc + delivery-badges + bottom-aligned seller-row + rechter actie-kolom met Direct/Voorstel pills onder elkaar + watchlist + Bekijk; mobile heeft prijs in body + watchlist-overlay op image), `listing-view-toggle` (Fase 27.105 — Lijst/Foto's, hidden op <lg), `marktplaats-filter-sidebar` (Fase 27.105 — sticky sidebar met collapsible sections + filter-counters per sectie + "Wis filters"-knop), `marktplaats-mobile-filters` (Fase 27.105 — slide-over drawer met active-counter badge) |
| `cart/` | `cart-checkout` (Fase 35: dispatcht `cart-checkout-locked`/`unlocked` DOM-events op modal-open/close, `checkoutLockMinutes={5}` doorgeven aan PaymentMethodModal), `cart-content`, `cart-item-row` (Fase 35: listent op checkout-lock events, pauseert countdown-interval + skipt auto-refresh-on-expired, toont "Vastgezet"-pill met ShieldCheck-icoon ipv aftellende klok tijdens lock) |
| `checkout/` | `shipping-method-picker`, `payment-method-modal` (Fase 35: optionele `checkoutLockMinutes`-prop voor groene ShieldCheck-info-banner — alleen voor claimsale-checkout, niet direct-buy) |
| `message/` | `chat-layout`, `conversation-list`, `message-thread`, `contact-seller-button`, `chat-actions`, `proposal-button`, `proposal-message`, `bundle-offer-form`, `bundle-offer-message`, `pickup-actions` (Fase 27) |
| `search/` | `search-bar`, `search-filters`, `search-result-card`, `search-sort-bar` |
| `dashboard/` | `dashboard-nav`, `dashboard-stats`, `dashboard-stats-locked`, `profile-form`, `address-form` (cooldown-aware met lock-icoon, Fase 27.108), `purchases-content`, `sales-content`, `shipping-methods-manager`, `shipping-method-form`, `ship-bundle-form`, `buyer-shipping-info`, `disputes-overview`, `dispute-detail-content`, `open-dispute-form`, `admin-disputes-list`, `balance-summary`, `deposit-methods`, `pending-auction-payments`, `verification-form`, `admin-verification-list`, `banner-selector` (alleen in /customization, niet meer in /profiel — Fase 27.108), `bank-details-form` (Fase 5), `runner-up-settings` (Cluster A.followup), `withdrawal-form` / `withdrawal-history` / `admin-withdrawals-list` (Fase 6), `blocked-list-content` / `admin-reports-list` (Fase 7), `suspension-banner` (Fase 8), `cancellation-actions` / `cancellations-section` (Fase 9), `active-pickups-section` (prominente pickup-code voor SCHEDULED bundles in /aankopen, Fase 27.35), `order-detail-modal` (Fase 28 — status-tijdlijn + sort-dropdown + inline SellerRefundForm + refund-historie + carrier-aware verzendblok + sortering-aware pakbon-print), `seller-refund-form` (item-toggle of custom-amount, hergebruikt door order-detail-modal en sales-content) |
| `customization/` | `ember-balance`, `ember-icon`, `login-streak`, `equip-selector`, `rarity-badge`, `cosmetic-banner-image`, `achievement-unlock-listener` |
| `home/` | `hero-email-form`, `pricing-section` |
| `layout/` | `header`, `footer`, `language-switcher`, `user-balance`, `page-container` (Fase 25 — narrow/default/wide tiers) |
| `ui/` | `button`, `card`, `cart-icon`, `chart`, `checkbox`, `input`, `select`, `slider`, `label`, `switch`, `image-gallery`, `image-uploader`, `rich-text-editor`, `item-carousel`, `breadcrumbs`, `social-share`, `notification-bell`, `notification-list`, `pagination`, `review-form`, `review-list`, `seller-level-badge`, `seller-reputation-card`, `seller-info-block`, `username-history-tooltip`, `shipping-method-selector`, `star-rating`, `watchlist-button`, `verified-badge`, `block-report-buttons` (Fase 7), `seller-location-line` (Fase 27.103 — gedeelde "📍 Plaats · X km" component voor item-cards, gebruikt door listing-card / auction-card / claimsale-card / listing-list-row) |
| `admin/` | (Fase 15) `admin-nav` (sidebar met badges), `bank-transfer-form`, `user-action-bar` (suspend/lift/IBAN-reset modal), `catalog-edit-form` (generic CRUD), `app-config-editor` (JSON-validatie), `cron-run-now-button` (respects `allowManualRun`), `moderation-table` (bulk-select + delete-met-reden) |
| `layout/` (admin) | (Fase 15) `admin-shield` — header-icon, alleen zichtbaar voor `accountType === "ADMIN"` |
| `providers/` | `theme-provider` |

### Library (`src/lib/`)
| File | Purpose |
|------|---------|
| `auth.ts` | NextAuth config, JWT callbacks |
| `prisma.ts` | Prisma client singleton |
| `utils.ts` | `cn()` for Tailwind class merging |
| `upload.ts` | Image upload handling |
| `subscription-tiers.ts` | Central tier config (limits, commission, prices, features) + helpers |
| `account-limits.ts` | Tier-based limit checks (auctions, claimsales, listings) |
| `seller-levels.ts` | 20-tier seller level definitions, XP calculation (revenue-based), styling |
| `seller-info.ts` | `getSellerInfo()` — fetches seller data for info blocks on detail pages |
| `recommendations.ts` | Seller other items & similar items queries |
| `upsell-config.ts` | Tier-based upsell pricing for listings (`UPSELL_PRICING`) and auctions (`AUCTION_UPSELL_PRICING`, higher rates) |
| `auction/bid-increments.ts` | Min bid increment per price tier |
| `balance-check.ts` | Available balance, reserve calculation (40%), per-auction reserve tracking |
| `auction/autobid.ts` | Autobid resolution (with 40% reserve on each bid step) |
| `shipping/countries.ts` | EU country list (code, name NL, name EN) |
| `shipping/carriers.ts` | Carrier suggestions per country |
| `validations/` | Zod schemas: `auth`, `auction`, `listing`, `address`, `shipping-method`, `cart`, `customization` |
| `cosmetic-config.ts` | Rarity table (display labels = TCG slang, schema keys stable), activity rewards, login-streak rewards |
| `achievements.ts` | Tiered achievement definitions, `checkAchievements`, `getUserAchievements`, `getPendingUnlocks`, `acknowledgeAllUnlocks`, `syncAchievementCatalog` |
| `shipping-bundle.ts` | `createPendingBundle()` voor AWAITING_PAYMENT pre-creation (auction én proposal-partial-balance) |
| `validations/iban.ts` | `IBAN_COOLDOWN_DAYS` constante, 37-land lengtetabel, mod-97 checksum (`isValidIbanFormat`), `normalizeIban`, `formatIbanForDisplay`, `maskIban` |
| `validations/bundle-offer.ts` | (Fase 27) `createBundleOfferSchema` — listingIds.length ≥ 2, totalAmount > 0, SHIP vereist shippingMethodId |
| `validations/pickup.ts` | (Fase 27) `proposePickupSchema` (datum ≥ vandaag, HH:MM tijdformaat, end > start), `confirmPickupSchema` (4-digit numerieke code) |
| `bundle-offer-config.ts` | (Fase 27) `BUNDLE_OFFER_EXPIRY_DAYS` (3), `MIN/MAX_LISTINGS_PER_BUNDLE` (2/20), `BUNDLE_PAYMENT_DEADLINE_DAYS_SHIP` (5), `PICKUP_RESERVATION_DAYS` (14). Apart bestand zodat client-componenten de constants mogen importeren |
| `pickup-config.ts` | (Fase 27.31) `PICKUP_CODE_DIGITS` (4) + `PICKUP_CODE_LETTERS` (24 hoofdletters zonder O/I), `PICKUP_CODE_LENGTH` (5), `PICKUP_CODE_REGEX` (`^\d{4}[A-HJ-NP-Z]$`), `PICKUP_CODE_MAX_ATTEMPTS` (5), `PICKUP_LOCKOUT_HOURS` (1), `PICKUP_REMINDER_HOURS` (24). `generatePickupCode()` gebruikt `crypto.getRandomValues` (geen modulo-bias) en levert format "4837K". |
| `distance.ts` | (Fase 27.103) `distanceKm({buyerCountry, buyerPostalCode, sellerCountry, sellerPostalCode})` — haversine, same-country only (NL ↔ NL), null bij verschillende landen of postcode niet in dataset. `formatDistance(km)` met "<1 km" privacy-floor + verder exact afgerond. `countryFlag(seller, buyer)` voor cross-border vlaggetje. Dataset in `data/postcodes-nl.json` (PC4 → [lat, lng]) — werkbare seed (~150 entries), drop-in vervangbaar met volledige PDOK-set (4000 entries) |
| `data/postcodes-nl.json` | (Fase 27.103) NL 4-cijfer postcodes naar [lat, lng] centroïden — werkbare seed voor distance-calculaties; voor productie vervangen door volledige PDOK PC4-set |
| `listing-filters.ts` | (Fase 27.105) `parseListingFilters(searchParams)` + `buildListingFilterWhere(filters)` Prisma-where builder + `countActiveFilters` voor counter-badge. URL-params: price_min/max, type, condition, pricing (DIRECT/OFFER), delivery (SHIP/PICKUP), free_shipping, radius, verified, since (today/week/month/all), view (list/grid). Radius-filter NIET in DB-where want SQLite kent geen haversine — page doet post-filter |
| `address-cooldown.ts` | (Fase 27.108) `ADDRESS_COOLDOWN_DAYS = 30` + `getAddressCooldownInfo(lastAddressChange)` returnt `{canEdit, daysRemaining, availableAt}`. Apart bestand zodat client-componenten het mogen importeren (actions.ts mag geen non-async exports). Eerste keer gratis (lastAddressChange null), daarna 30d geblokkeerd |
| `withdrawal-config.ts` | `WITHDRAWAL_MIN_AMOUNT` (apart bestand zodat client-componenten het kunnen importeren) |
| `moderation.ts` | `moderateImage(buffer, mimeType)` — Claude Haiku 4.5 vision-check via `@anthropic-ai/sdk`, forced tool-use voor `{safe, reason}`, 8s timeout, fail-open op alle errors. `server-only`. Module-level gecachede client + één-keer-per-process missing-key-warn |
| `blocking.ts` | `getBlockedUserIds(userId)` symmetrisch (beide richtingen), `sellerNotInBlockedFilter` helper voor Prisma where-clauses, `REPORT_REASONS` const-tuple |
| `suspension.ts` | `isUserSuspended(user)` (PERMANENT of `suspendedUntil` in toekomst), `requireNotSuspended(userId)` async guard voor write-actions |
| `admin.ts` | (Fase 15) `requireAdmin()` (action-variant: throws) en `requireAdminPage()` (page-variant: redirect). Returnt `{ adminId }`. **Vervangt inline `accountType !== "ADMIN"`-checks** |
| `admin-audit.ts` | (Fase 15) `logAdminAction({ adminId, action, targetType, targetId?, metadata? })` schrijft `AdminAuditLog`. `AdminAction`/`AdminTargetType` zijn const-unions — bij nieuwe actie eerst hier toevoegen |
| `cron-logging.ts` | (Fase 15) `withCronLogging(jobName, fn, triggeredBy?)` wrapper rond cron-werk. Maakt `CronRun` met RUNNING → SUCCESS/FAILED, vangt errors, schrijft itemsProcessed |
| `cron-jobs.ts` | (Fase 15) Centrale registry: `CRON_JOBS` (job-runners) + `CRON_JOB_META` (description/schedule/`allowManualRun`/runWarning) + `CRON_JOB_NAMES`. **Bij nieuwe cron toevoegen: registry + meta + route + withCronLogging — alle vier**. Bevat ook helper `processRunnerUpDecision(auctionId, previousBidderId)` (post-Fase-33) — race-safe via optimistic concurrency op `runnerUpAttempts`, suspended/blocked-bidder filter |
| `pending-fees.ts` | (post-Fase-33) `recordPendingFeeInTx(tx, args)` om niet-inbare boete als schuld te boeken; `settlePendingFees(userId, incomingAmount)` om FIFO af te romen (binnen één `$transaction(callback)` met conditional `updateMany WHERE settledAt=null AND amount>=settle` voor race-safety); `getOpenPendingFees`/`getOpenPendingFeesTotal` voor UI |
| `address-validation.ts` | (post-Fase-33) `hasValidShippingAddress(userId)` — checkt street/houseNumber/postalCode/city/country allen non-empty. Aangeroepen in `placeBid`/`setAutoBid`/`acceptRunnerUpOffer` voor SHIP-flow |
| `cron-auth.ts` | (Fase 15) `resolveCronTrigger(request)` returnt `"cron"` (Bearer secret), `<adminId>` (admin sessie) of `null` (unauthorized). Gebruikt door alle cron-routes |
| `realtime.ts` | (Fase 30) **SSE pub/sub** — `subscribe(channel, sub)` + `publish(channel, event)` + `publishMany`. Channels-Map staat op `globalThis.__realtimeChannels` zodat HMR-reloads in dev mode niet leiden tot twee aparte instances. Helpers: `userChannel(id)` / `auctionChannel(id)` / `claimsaleChannel(id)` / `listingChannel(id)`. `RealtimeEvent` type-union dekt alle 16 event-types (outbid/bid-placed/auction-won/auction-ended/balance-changed/achievement-unlocked/notification-created/new-message/claimsale-item-claimed/cart-changed/listing-changed/bundle-changed/dispute-changed/withdrawal-changed/verification-changed/suspension-changed). Bij multi-instance hosting (Vercel/replicas): swap naar Redis pub/sub via ioredis — interface ongewijzigd (~30 regels) |
| `auction/fees.ts` | (Fase 31) `AUCTION_BUYER_PREMIUM_RATE = 0.03` + pure helper `calculateBidFees(bid)` returnt `{bid, premium, total}`. `calculateBidTotalFromBid(bid)` als convenience. Geen tier-discount-logica — premium is universeel 3% |
| `listing-bulk-import.ts` | (Fase 31) `BulkRowSchema` Zod + `parseCsvText(text)` met papaparse + `BULK_CSV_TEMPLATE` seed-string. Velden: title/listingType/condition/deliveryMethod/price/pricingType/description/shippingMethodIds. Pure parser — DB-validatie en account-limit-check zit in `bulkImportListings` action |
| `subscription-tiers.ts` | (Fase 31) **Centrale tier-config**: ENTERPRISE toegevoegd, prijzen+commissies+features+limits+tierRank per tier. Helpers `getTierConfig`/`getCommissionRate`/`getUpsellDiscount`/`getTierRank`/`getMonthlyFreeUpsells`/`hasFeature`/`getTierPrice`. Constants `TIER_BREAK_EVENS_MONTHLY` + `ENTERPRISE_MIN_MONTHLY_REVENUE = 25000`. ADMIN-fallback nu naar ENTERPRISE-config |
| `auction/labels.ts` | (Fase 35) **NIEUW** — Label-systeem voor auctions. `LABEL_TYPES` (9 stuks), `LABEL_COLORS` (10-kleuren palet), `COLOR_CLASSES` Tailwind-mapping, `COLOR_HEX` voor preview, `MAX_LABELS_PER_AUCTION = 2`, `calculateLabelCost(count)` (1=€0,99 / 2=€1,69), `availableLabelsFor({reservePrice, buyNowPrice, condition, auctionType})` voor conditional availability + anti-tamper, `SPOTLIGHT_CTR_MULTIPLIER=15` / `CATEGORY_HIGHLIGHT_CTR_MULTIPLIER=8` / `LABELS_CTR_MULTIPLIER=3` |
| `auction-filters.ts` | (Fase 35) **NIEUW** — Parallel aan `listing-filters.ts`. `parseAuctionFilters`/`buildAuctionFilterWhere`/`countActiveAuctionFilters`. Filter-velden: priceMin/Max (op `currentBid`), types, conditions, durations (3/5/7/14), hasBuyNow, noReserve, hasBids, radius, verifiedOnly, since, view (list/grid). Radius-filter NIET in DB-where want SQLite kent geen haversine — page doet post-filter |
| `claimsale-filters.ts` | (Fase 35) **NIEUW** — `parseClaimsaleFilters`/`buildClaimsaleFilterWhere`/`countActiveClaimsaleFilters`. Filter-velden: priceMin/Max (op item-prijsrange), itemType, hasShipping, itemCountMin (5/10/25/50+ via post-filter in JS), radius, verifiedOnly, since, view |
| `expire-claims-scheduler.ts` | (Fase 35) **NIEUW** — In-process `setInterval(60_000)` voor `expireClaimedItems`, parallel aan auction-scheduler/auction-activator-scheduler. HMR-resistant via `globalThis.__expireClaimsScheduler`. Boot-init in `instrumentation.ts`, eerste tick 5s na boot. Externe cron blijft als safety-net |

### Database (`prisma/schema.prisma`)
User, Category, Series, CardSet, Card, CardPriceHistory, CardWatchlist, Auction, AuctionBid, AuctionShippingMethod, AuctionUpsell, Claimsale, ClaimsaleItem, ClaimsaleShippingMethod, Listing, ListingShippingMethod, ListingUpsell, SellerShippingMethod, ShippingBundle, Transaction, Subscription, Conversation, ConversationParticipant, Message, Proposal, Watchlist, Notification, Review, AppConfig, AutoBid, CartItem, Dispute, DisputeEvent, UsernameHistory, VerificationRequest, CosmeticBundle, CosmeticItem, OwnedItem, EmberTransaction, ActivityLog, Achievement, AchievementTier, UserAchievement, BuybackRequest, BuybackItem, BulkBuybackItem, **WithdrawalRequest** (Fase 6), **UserBlock** + **UserReport** (Fase 7), **CancellationRequest** (Fase 9), **AdminAuditLog** + **CronRun** (Fase 15), **BundleProposal** + **BundleProposalListing** + **BundleListing** + **PickupSchedule** (Fase 27), **EnterpriseRequest** (Fase 31), **AuctionRunnerUpOffer** + **PendingPlatformFee** (post-Fase-33), **AuctionLabel** (Fase 35), **Event** + **EventUpsell** + **EventReport** (Evenementenkalender, live 2026-06-18), **ImageModerationLog** (content-moderation audit-log: `userId?`/`context`/`verdict` (safe|blocked|skipped_unavailable|skipped_no_key)/`reason?`/`mimeType`/`sizeBytes`/`createdAt`).

**Auction.paymentStatus** enum-uitbreiding (post-Fase-33): `AWAITING_RUNNER_UP_DECISION` toegevoegd voor de 72u-window-fase. **User.maxRunnerUpAttempts**: default 5 → 2, validatie 1-10 → 0-3 (clamp-script in `scripts/clamp-runner-up-attempts.ts` voor bestaande users >3). **AuctionRunnerUpOffer**-model: `auctionId`/`bidderId`/`bidAmount`/`premiumAmount` (snapshot)/`deliveryChoice`/`status` (AWAITING_DECISION|ACCEPTED|DECLINED|EXPIRED)/`decisionDeadline` (createdAt+72u)/`decidedAt`. **PendingPlatformFee**-model: `userId`/`type` (BID_DEPOSIT_FORFEIT|BID_PAYMENT_FAILURE_FEE)/`amount` (open bedrag)/`originalAmount` (immutabel)/`description`/`relatedAuctionId`/`settledAt` (null = open). Nieuwe Transaction-type-string `PLATFORM_FEE_SETTLEMENT` voor settlement-rijen (geen balance-mutatie, alleen audit-trail).

User-velden toegevoegd in roadmap-werk: `maxRunnerUpAttempts` (Cluster A), `iban`/`accountHolderName`/`lastIbanChange` (Fase 5), `suspendedUntil`/`suspensionType`/`suspensionReason`/`suspensionAdminId` (Fase 8), `lastAddressChange` (Fase 27.108 — 30d cooldown na adreswijziging). Auction-velden: `runnerUpEnabled`/`failedBidderIds`/`runnerUpAttempts` (Cluster A). Listing.expiresAt + `EXPIRED` enum-waarde **verwijderd** (Cluster C). User-relatie `adminAuditLogs` (Fase 15, named `AdminAuditLogActor`). Listing-velden `pickupPostalCode`/`pickupCity` + status-uitbreiding `PAUSED/RESERVED/PARTIALLY_SOLD` + relations naar `BundleProposalListing[]`/`BundleListing?` (Fase 27; DRAFT toegevoegd in 27, weer verwijderd in 27.83). ShippingBundle-velden `paymentMode` (PLATFORM|EXTERNAL), `bundleProposalId @unique`, `pickupReservationExpiresAt` + status-uitbreiding `SCHEDULED` + relations naar `bundleListings BundleListing[]`/`pickupSchedule PickupSchedule?` (Fase 27); `autoExpiredAt DateTime?` (Fase 28 — gevuld door auto-cancel-stale-paid cron). Message-velden `bundleProposalId`/`pickupScheduleId` (Fase 27). User-relations `bundleProposalsAsBuyer/Seller` + `pickupSchedulesProposed` (Fase 27). **Fase 31 User-velden**: `accountType` enum-uitbreiding met `ENTERPRISE`, `tierRank Int @default(1)` (1-4 voor search-boost), `shopSlug String? @unique` (vanity URL), `freeUpsellsRemaining Int @default(0)` + `freeUpsellsResetAt DateTime?` (maandelijks gereset), `skipBidConfirmation Boolean @default(false)` (modal-preference), `mollieCustomerId String?` (Mollie-hook, ongebruikt tot Fase 17). **Fase 31 Subscription-velden**: `tier`-comment uitgebreid met ENTERPRISE, `mollieSubscriptionId`/`mollieCustomerId` (Mollie-hooks), `paymentStatus String @default("PAID")` (PAID/FAILED/GRACE/EXPIRED), `gracePeriodEnd DateTime?`, `nextBillingAt DateTime?`, `legacyPricing Boolean @default(false)` (gereserveerd voor toekomstige prijswijziging). **Fase 31 EnterpriseRequest**-model: `userId` (NIET @unique zodat na rejection opnieuw mag) + named relaties `EnterpriseRequester`/`EnterpriseReviewer` op User, fields `shopName`/`estimatedMonthlyRevenue`/`phone`/`motivation`/`status`/`reviewedAt`/`reviewedById`/`rejectionReason`/`approvedMonthlyPrice`. **Fase 31 Transaction-types**: nieuwe `AUCTION_PREMIUM` type-string voor de 3% buyer's premium leg (apart van `PURCHASE`/`AUCTION_WIN`). **Fase 35 AuctionLabel**-model: `auctionId`/`type` (9 stringkeys: GEEN_RESERVE/DIRECT_KOPEN/MOET_NU_WEG/HOT_ITEM/TOPSTAAT/ZELDZAAM/HOLO_FOIL/SNEL_VERZONDEN/COMPLETE_SET)/`colorKey` (uit 10-kleuren palet)/`cost` (snapshot bij creatie)/`createdAt`. `Auction.labels AuctionLabel[]`-relatie + `@@index([auctionId])`. **Fase 35 ClaimsaleItem-veld**: `checkoutLockExpiresAt DateTime?` voor 5-minuten checkout-race-lock (one-shot per claim-cycle, reset bij elke AVAILABLE-flip). **Fase 35 AuctionUpsell window-mode**: schema (startsAt/expiresAt/dailyCost/totalCost) was al window-shaped, maar UI genereerde voorheen `startsAt=now`/`expiresAt=now+days`. Nu kiest seller een window via dual-handle slider (startDay+endDay 1-indexed inclusive) en `createAuction` berekent `startsAt = auction.startTime + (startDay-1)·dayMs` / `expiresAt = auction.startTime + endDay·dayMs`.

### Evenementenkalender (live sinds 2026-06-18)
Community-kalender voor Pokémon-evenementen (beurzen + overige events) in heel Europa. Ingelogde users maken events aan → admin keurt goed (`PENDING`→`LIVE`), of `User.isTrustedEventOrganizer` publiceert direct. Routes onder `/evenementen` (overzicht met **Beurzen/Evenementen**-tab + **lijst/kalender/kaart**-views + filter-sidebar), `/evenementen/nieuw` (multi-step wizard), `/evenementen/[id]` (detail), `/dashboard/evenementen` (mijn events), admin `/dashboard/admin/event-approvals` + `/event-reports`.
- **Schema**: `Event` (organizer/status/locatie+geocode/UTC-tijden+IANA-tz [eendaags per definitie sinds audit-pass — geen endDate-invoer meer]/`earlyAccessTime` (VT)/`entryType`+`ticketTypes`-JSON/`vendorOptions`-JSON/faciliteiten-booleans/`maxVisitors`/`venueSizeM2`/`totalTables`/`coverImage`/`flyerImage`/`galleryImages`-JSON/`videoUrl`/`organizerName`+`organizerWebsite`+`socialLinks`-JSON/toernooivelden/`isOfficial`/`recurrence`), `EventUpsell` (kopie van AuctionUpsell-shape; **twee betaalde promoties**: banner `CATEGORY_HIGHLIGHT` €0,50/dag op /evenementen + `HOMEPAGE_SPOTLIGHT` €0,75/dag op de hoofd-homepage), `EventReport` (reasons MISLEADING/OFFENSIVE/SPAM/INAPPROPRIATE/OTHER), `User.isTrustedEventOrganizer`. **EventLabel is in ontwikkeling geschrapt** — geen label-systeem voor events.
- **Lib** `src/lib/events/`: `types`, `countries` (volledige Europese landenlijst, **apart van** `shipping/countries`), `timezones` (`zonedWallClockToUtc` + `formatEventDateRange`, DST-correct via `Intl`, geen tz-label want al lokale tijd), `geocoding` (Nominatim/OSM, fail-soft, 8s timeout, User-Agent), `video` (YouTube/Vimeo → `youtube-nocookie`-embed, geen eigen opslag), `format` (NL-euro `formatEuro`: €7,50 / hele getallen €5,- / 0=Gratis), `upsell-config` (banner €0,50/dag + spotlight €0,75/dag; helpers `calculateEventBannerCost`/`calculateEventSpotlightCost`/`eventUpsellDaysUntil`, constants `EVENT_BANNER_STORED_TYPE`=CATEGORY_HIGHLIGHT / `EVENT_SPOTLIGHT_STORED_TYPE`=HOMEPAGE_SPOTLIGHT). Plus `src/lib/event-filters.ts` (`parseEventFilters`/`buildEventFilterWhere`/`countActiveEventFilters` — tab+view+country+type+**datePreset** (week/weekend/month/quarter, server-side venster)+custom-date+**activities** (canPlay/canTrade/canSell)+radius+free; radius post-filtert in JS via haversine van buyer-postcode-coord → event lat/lng).
- **Actions**: `src/actions/event.ts` (`createEvent`/`updateEvent`/`deleteEvent` — guards auth→requireNotSuspended→requireEmailVerified, dup-detectie, geocode, trusted-auto-publish, banner-saldo-deduct), `event-report.ts` (`reportEvent`), `admin/events.ts` (`approveEvent`/`rejectEvent`/`setEventOfficial`/`setTrustedOrganizer`/`reviewEventReport` — `requireAdmin` + `logAdminAction`; `AdminAction`/`AdminTargetType` uitgebreid met EVENT-keys). `UploadContext` += `"event"` (gemodereerd via Haiku vision).
- **Componenten** `src/components/events/`: `multi-step-event-form` + `steps/*` (type/details/locatie/tickets/faciliteiten/foto/promotie/controleren), `event-live-preview` (sticky), `event-card`/`event-list-row`, `event-calendar-month` (maandgrid + hover quick-view klikbaar + "+N meer"→dag-in-lijst + vandaag-blauwe-rand + uitgelicht-voorrang), `event-quick-view`, `event-map` + `leaflet-map` (Leaflet + `leaflet.markercluster`, `next/dynamic` ssr:false; popup met adres + openingstijden), `event-gallery` (lightbox), `event-filter-sidebar` (gebruikt de gedeelde `FilterSection`/`CheckboxRow`/`RadioRow` uit `src/components/ui/filter-section.tsx`; secties Land/Type/Wanneer/Activiteiten/Afstand/Entree), `event-controls` (Beurzen/Evenementen-tab + Lijst/Kalender/Kaart-toggle), `event-report-button`, admin `admin-event-approval-list`/`admin-event-reports-list`. Homepage-rij `src/components/home/spotlight-events.tsx` toont events met actieve HOMEPAGE_SPOTLIGHT-upsell.
- **Tickets** (detailpagina, belangrijkste blok): ticket-vormige kaarten met `public/images/cosmetics/blank-ticket.webp` als achtergrond (vaste aspect-ratio zodat de perforatie klopt), naam+beschrijving+servicekosten links, prijs in de stub rechts, 2-koloms grid. PAID kiest **online-ticketlink** (knop "Tickets kopen", nieuwe tab) óf **"alleen aan de deur"** (geen schema-veld — lege `registrationUrl`). Capaciteit-chips: max. bezoekers + totaal tafels.
- **Header**: events-link staat **secundair** (achter de divider, label "Evenementen", geen brand-bolletje); de zoekbalk is vervangen door een **loep-icoon dat op desktop naar links uitklapt** over de nav heen (klik-buiten/Esc sluit).
- **i18n**: `common.events` = "Evenementen"/"Events"; overige event-strings grotendeels hardcoded NL in component-bodies (follow-up bij echte EN-rollout).
- **Turso-migratie**: additieve SQL in `prisma/turso-events.sql` (3 CREATE TABLE + 1 `ALTER TABLE User ADD COLUMN`). **Let op**: `prisma migrate diff` genereerde een volledige User-table-rebuild voor die ene kolom; die is **bewust vervangen door `ADD COLUMN`** om de productie-User-tabel niet te droppen. Toegepast op Turso via een `@libsql/client.executeMultiple`-script (zoals `push-to-turso.ts`).
- **Events-audit-pass (2026-07-10, branch `feature/events-verbeteringen`)** — (a) **Tickets vs standhouders gescheiden**: `TicketListEditor` heeft nu `variant="ticket"|"option"` — standhouder-opties (tafel/stoel/stroom) renderen als neutrale rijen met Table2-icoon i.p.v. ticket-kaartjes; wizard-stap heet "Tickets & stands"; `totalTables` verhuisd van Faciliteiten-stap naar het Standhouders-blok en op de detailpagina naar "Voor standhouders" (`hasVendor` verruimd met `totalTables`); server-side `parseTicketTypes(raw, allowServiceFee)` — vendors krijgen geen serviceFee. (b) **Prijs-bug op kaarten gefixt**: `getEventPriceLabel()` in `src/lib/events/format.ts` leidt "Gratis entree" / "€5,-" / "Vanaf €7,50" / "Betaald" af uit `ticketTypes`-JSON (entryPrice is altijd null sinds TIERS); `EventListItem.priceLabel` gevuld in beide mappers (evenementen/page + homepage-data). (c) **Validatie-drift**: `ticketSaleMode` wordt meegestuurd + server-side afgedwongen (PAID+ONLINE vereist registrationUrl), adres-stap geeft per-veld foutmeldingen. (d) **Flyer**: `Event.flyerImage` (staand ≈3:4) — upload in Foto-stap, `<EventFlyer>` lightbox als bovenste sidebar-kaart op detail. (e) **ICS-export**: publieke `GET /api/events/[id]/ics` (alleen LIVE, handgebouwde RFC5545) + "Zet in mijn agenda"-knop onder het datum-blok. (f) **Vroege toegang**: `Event.earlyAccessTime DateTime?` (UTC, vóór startTime, alleen PAID) — time-veld in Bezoekers-blok, "Vroege toegang vanaf 09:00 (CEST)"-regel op detail + "· VT 09:00"-hint op kaart/quick-view/wizard-preview. (g) **Edit-flow** (updateEvent had nul callers): wizard heeft `mode="create"|"edit"` + `initialForm` (via `eventToFormState` in `src/lib/events/event-to-form.ts`, bevat ook gedeelde `toLocalDate`/`toLocalTime`); edit verbergt Type- + Promotie-stappen; nieuwe page `/dashboard/evenementen/[id]/bewerken` + Bewerken-knop in MyEventRow (LIVE/PENDING/REJECTED); `updateEventSchema`/`updateEvent` op pariteit met create (lege waarde = leegmaken, booleans alleen toegepast als key in FormData zat); REJECTED → na edit terug naar PENDING, LIVE/PENDING behouden status (geen her-moderatie, bewust). (h) **Polish**: maand-groepkoppen ("Juli 2026") + "N in de komende 30 dagen"-teller in lijst-view, detail-h2 "Tickets & entree", indigo linker-rand op standhouders-kaart. **Ronde 2 (zelfde dag/branch)**: (i) VT verplaatst naar het "Wanneer?"-blok in de Details-stap (los van gratis/betaald, ook getoond bij FREE); (j) **eendaags per definitie** — endDate-veld/schema/actions volledig weg, meerdaagse beurs = één event per dag (legacy meerdaags wordt bij edit teruggebracht naar startdag); (k) datum kan niet in het verleden (min-attr + wizard-validatie + updateEvent-check); (l) lijst-kaart: banner altijd volledig 3:1, desktop = linker 2/3 rand-tot-rand; info-kolom gevuld met activiteiten-pills (Verkopen/Ruilen/Spelen) + meta-regel max-bezoekers/m²/tafels; (m) **`Event.venueSizeM2`** (Oppervlakte-veld naast max. bezoekers in Faciliteiten-stap) + **`Event.socialLinks`** (JSON, max 4 URL's, platform-detectie in `src/lib/events/socials.ts` + eigen `<SocialIcon>` inline-SVG's want lucide heeft geen brand-iconen meer) getoond in Organisator-kaart; (n) toevoegen-knop alleen ingelogd + tab-specifiek ("Beurs toevoegen" → `?type=BEURS`-preselect); kaart-view rendert ook bij 0 resultaten (overlay-pill); "Plan je route"-knop (Google Maps universal URL); "Stel een vraag"-knop via bestaande `ContactSellerButton` (kale user-to-user chat); dismissible launch-banner op /evenementen (localStorage + auto-verbergen bij ≥15 live events; **bewust géén scraping/seed** van concurrent-sites — databankenrecht + geen eigenaar). **Turso-migratie nodig bij deploy**: `prisma/turso-events-v2.sql` (4× additieve `ADD COLUMN`: flyerImage, earlyAccessTime, venueSizeM2, socialLinks).
- **Filter-redesign + events-monetisatie (live 2026-07-02)** — Collectr-geïnspireerde audit. (a) **Gedeeld filter-component** `src/components/ui/filter-section.tsx` (`FilterSection` met `description`-prop voor helper-tekst + `CheckboxRow`⬜/`RadioRow`⚪ met `accent-primary`) — was 3× gedupliceerd in marktplaats/veilingen/claimsales-sidebars, nu één bron; alle 4 overzicht-sidebars importeren het en kregen korte helper-tekst per sectie. **Bij een nieuwe filter-sidebar: altijd dit component hergebruiken, niet opnieuw dupliceren.** (b) **Events-filters**: datum-presets (Komende 7 dagen/Dit weekend/Deze maand/Komende 3 maanden, server-side venster in `presetToRange`) + custom-range + activiteiten-filter (Spelen/Ruilen/Verkopen op `canPlay/canTrade/canSell`). (c) **Featured-lek gedicht**: de "Uitgelicht"-rij op /evenementen verdween voorheen zodra je filterde; nu afgeleid uit de al-gefilterde lijst (radius-correct) en featured-styling consistent in alle 3 views — amber-rand + "Uitgelicht"-pill op `event-card`, amber `divIcon`-pin in `leaflet-map` (was alleen amber in de kalender). Banner-`featured`-flag is type-specifiek op `CATEGORY_HIGHLIGHT`. (d) **Tweede upsell**: `HOMEPAGE_SPOTLIGHT` naast de banner in `step-promotion` (twee toggle-kaarten, eigen window-datum), `createEvent` maakt beide `EventUpsell`-rijen + opgetelde saldo-deduct in één `$transaction` met anti-tamper hercheck; homepage-rij via `homepage-data.spotlightEvents` + `<SpotlightEvents>`. **Geen schema-wijziging** — `EventUpsell.type` is String, dus geen Turso-migratie. (e) **UX**: alle overzicht-navigatie (filter-sidebars, Beurzen/Evenementen-tab, Lijst/Kalender/Kaart-toggle, view-toggles + sorteer-balken van marktplaats/veilingen, zoekpagina-filters) gebruikt nu `router.push(..., { scroll: false })` zodat de pagina niet naar de top springt; paginering scrollt bewust nog wél. **Geen PRO-gating op koper-filters** (bewust besloten: filters helpen kopen → gratis houden).

### i18n (`src/i18n/`)
`routing.ts` (locales: nl default, en) · `request.ts` (translations per request) · `navigation.ts` (i18n Link/redirect) · Proxy: `src/proxy.ts`

### i18n Namespaces (`src/messages/`)
`common`, `auth`, `home`, `auction`, `claimsale`, `wallet`, `dashboard`, `profile`, `chat`, `proposal`, `listing`, `watchlist`, `notifications`, `bids`, `search`, `reputation`, `carousel`, `breadcrumbs`, `seller`, `cart`, `shipping`, `sellerClaims`, `subscription`, `verification`, `sales`, `purchases`, `disputes`, `customization`, `footer`, `withdrawal` (Fase 6), `blockReport` (Fase 7), `suspension` (Fase 8), `cancellation` (Fase 9), `admin` (Fase 15 — bevat shieldTooltip/panelTitle/sectie-namen/nav-keys/KPI-labels/quick-actions; pagina-specifieke strings nog hardcoded NL als follow-up), `bundleOffer` + `pickup` (Fase 27 — chat-bubble labels, status-keys, payment-deadlines, errors). `listing` namespace uitgebreid met `status.*`, `sections.*`, `actions.*`, `deliveryShip/Pickup/Both`, `pickupLocation.*` (Fase 27). `shipping` namespace uitgebreid met `street/houseNumber/postalCode/city/country/selectCountry` (waren MISSING — AddressForm renderde alleen key-namen) + `addressEditNote/addressLockedUntil/addressFirstEditFree/addressLastChanged/addressEditButton` voor 30d-cooldown UI (Fase 27.108).

### API Routes (`src/app/api/`)
**Alle cron-routes** zijn vanaf Fase 15 gewrapt met `withCronLogging` (productie-logs in `CronRun`-tabel) en gebruiken `resolveCronTrigger` voor auth (Bearer secret OF admin-sessie). Bij toevoegen van een nieuwe cron: ook entry in `src/lib/cron-jobs.ts` (`CRON_JOBS` + `CRON_JOB_META` + `CRON_JOB_NAMES`) zodat manual-run via admin-panel werkt.

**Alle status-flippende crons** (Fase 30) publishen vanaf Fase 30 ook real-time events via `publish()` zodat UI direct updatet wanneer een cron flipt: `cancellation-expiry` → bundle-changed, `bundle-offer-payment-deadline` → listing-changed + balance-changed, `pickup-reservation-timeout` → bundle-changed + listing-changed, `auto-cancel-stale-paid` → bundle-changed + balance-changed + listing-changed, `proposal-payment-deadline` → listing-changed + balance-changed, `expire-claims` → claimsale-item-claimed + cart-changed, `auto-confirm` → bundle-changed, `auto-resolve-disputes` → dispute-changed (via finalizeDispute), `auction-payment-deadline` → balance-changed + auction-won (rotation).

| Route | Purpose |
|-------|---------|
| `auth/[...nextauth]/route.ts` | NextAuth handlers (JWT-callback exposeert nu `accountType` op session, Fase 15) |
| `upload/route.ts` | Image upload (auth required) |
| `uploads/[filename]/route.ts` | Serve uploaded files |
| `balance/route.ts` | Get user balance (auth required) |
| `claimsales/[id]/status/route.ts` | Claimsale status check |
| `cron/auto-confirm/route.ts` | Auto-confirm shipped bundles after timeout |
| `cron/auto-resolve-disputes/route.ts` | Auto-resolve unresponded disputes |
| `cron/check-subscriptions/route.ts` | Downgrade expired subscriptions to FREE |
| `cron/expire-claims/route.ts` | Verloop CLAIMED claimsale-items >15min in cart |
| `cron/sync-pokewallet/route.ts` | Ontdekt nieuwe sets + vult lege nieuwe sets met kaarten/afbeeldingen uit TCGdex (`src/lib/pokewallet/populate-cards.ts` + `src/lib/tcgdex/client.ts`) + **stap 1c `topUpGrowingSets()`** (2026-07-08): vult GROEIENDE sets bij — promo-sets krijgen doorlopend nieuwe kaarten, max 10 sets/run, incl. imageUrl-refresh zodra TCGdex een scan heeft + refresht pricing voor alle PokeWallet-gemapte sets (heavy: ~600 API-calls) + **stap 2b beeld-mirror** (PokeWallet→R2, 1500 kaarten/nacht, gebatchte writes). Prijzen blijven 100% PokeWallet; TCGdex levert alleen de kaartlijst. `runCronManually` heeft een concurrency-guard: weigert als dezelfde job al RUNNING is (<2u; ouder = stale na container-restart). |
| `cron/cleanup-archived-chats/route.ts` | Hard-delete conversations >60 dagen ARCHIVED (destructief — manual run geblokkeerd) |
| `cron/auction-payment-deadline/route.ts` | (post-Fase-33) Pre-emptive flip AWAITING_PAYMENT → AWAITING_RUNNER_UP_DECISION (race-safe vs completeAuctionPayment), strike + 2,9%-fee + €200-borg op wanbetaler (niet-inbare deel naar PendingPlatformFee). Daarna `processRunnerUpDecision` voor offer of finalize PAYMENT_FAILED |
| `cron/auction-activate/route.ts` | (Fase 34) Flipt SCHEDULED → ACTIVE voor veilingen waarvan `startTime ≤ now`, publisht `auction-started` event, bumpt finalize-scheduler. Race-safe via conditional `updateMany`. Safety-net naast in-process activator-scheduler (sub-seconde precisie). Schedule: elke minuut |
| `cron/auction-runner-up-decision-deadline/route.ts` | (post-Fase-33) Hourly. Flips AwaitingDecision-offers waarvan 72u-window verlopen is naar EXPIRED + triggert volgende kandidaat (of finalize PAYMENT_FAILED). Geen straf voor de runner-up |
| `cron/proposal-payment-deadline/route.ts` | Mark expired ACCEPTED-AWAITING_PAYMENT proposals as PAYMENT_FAILED, listing terug op ACTIVE (A3) |
| `cron/cancellation-expiry/route.ts` | Mark PENDING `CancellationRequest`s die >7 dagen open staan als EXPIRED (Fase 9) |
| `cron/bundle-offer-expiry/route.ts` | (Fase 27) PENDING bundle-voorstellen >3 dagen → EXPIRED. Geen listing-flip nodig |
| `cron/bundle-offer-payment-deadline/route.ts` | (Fase 27) ACCEPTED+AWAITING_PAYMENT bundle-voorstellen (PLATFORM) waar paymentDeadline < now → PAYMENT_FAILED, listings RESERVED→ACTIVE, PENDING bundle weg |
| `cron/pickup-reservation-timeout/route.ts` | (Fase 27) EXTERNAL pickup-bundles waar pickupReservationExpiresAt < now en niet COMPLETED → bundle CANCELLED, listings RESERVED→ACTIVE |
| `cron/pickup-reminder/route.ts` | (Fase 27) 24-uur reminder voor ACCEPTED ophaalmomenten (proposedFor binnen 23-25u én reminderSentAt IS NULL) |
| `cron/auto-cancel-stale-paid/route.ts` | (Fase 28) PAID SHIP-bundles >14d zonder shipping → bundle CANCELLED + volledige refund naar buyer + items/listings terug op markt + autoExpiredAt-marker. Beide partijen genotificeerd. Skipt bundles met actief PENDING annuleringsverzoek |
| `cron/reset-free-upsells/route.ts` | (Fase 31) Maandelijkse reset van `User.freeUpsellsRemaining` naar tier-default (PRO=1, Unlimited=5, Enterprise+ADMIN=999). 3× updateMany per tier, schedule op 1e van de maand 00:05 |
| `cron/cleanup-sold-images/route.ts` | (Fase 41) COMPLETED-bundles met `deliveredAt` >30d → geüploade foto-**bestanden** van die verkoop verwijderd (R2 in prod via `deleteUploadedFile`, schijf lokaal) + DB-foto-velden geleegd (`imageUrls`→`"[]"`, `coverImage`/`shippingProofUrls`→null). Tekstdata blijft. Idempotent via `ShippingBundle.imagesPurgedAt`. Skipt bundles met v1/v2-dispute of OPEN/INVESTIGATING ShippingIssue. Gedeelde foto's (listing met restvoorraad via `ListingCardItem`, claimsale-cover) pas weg als die volledig terminal zijn |
| `auctions/[auctionId]/bids/route.ts` | Real-time bid polling (GET: currentBid, bidCount, highestBidderId, recentBids) |
| `events/stream/route.ts` | (Fase 30) **SSE-endpoint** voor real-time events. GET met `Content-Type: text/event-stream`, auth-required (401 anders), heartbeat 25s tegen proxy-timeouts, channel-whitelist `auction:`/`claimsale:`/`listing:` via `?channels=` query (DoS-protectie tegen user-channel sniffing). User-channel automatisch ingesteld op `user:${session.user.id}`. Cleanup via `req.signal.addEventListener("abort")` |

---

## Open Feature Fases

| Fase | Onderwerp |
|------|-----------|
| 16 | Email notificaties |
| 17 | Betaalmethoden (iDEAL/Stripe/Mollie recurring-billing voor abonnementen — Fase 31 hooks gereserveerd) |
| 18 | Veiling eindetijden pagina |
| 19 | Favoriete verkopers |
| 20 | Geavanceerd zoeken & filters |
| 21 | Mobile responsive polish |
| 22 | SEO & meta tags |
| 23 | Customization Chapter 1 — eigen cosmetic-art (banners/emblems/backgrounds) seeden + via `rewardCosmeticKey` koppelen aan achievement-tiers |

Afgerond: fases 0–12, 14 (3-tier abonnementssysteem), customization/achievement/IP-cleanup pivot, op 2026-04-29 de **9-fase audit-roadmap** (clusters A-D fundament-fixes + fases 5-9: IBAN/payouts/blocking+reports/account-suspension/cancellation-requests), op 2026-04-30 **Fase 15 — Admin panel** (volledige backoffice op `/dashboard/admin/*` met audit-logging, cron-status/run-now, user-360°-detail, catalog/AppConfig/moderation), op 2026-05-01 **Fase 13 — Premium statistieken** (dashboard-essentials voor IEDEREEN: action-items/saldo/activiteit/recente-bundles; premium-only statistieken-page polish: CSV-export per sectie, ChartEmptyState voor lege chart-blokken, month/ytd quick-ranges, mobile-polish; commission-sectie omgezet naar saved-only), op 2026-05-01 **Fase 25 — Full-width compact layout** (PageContainer met narrow/default/wide tiers, header-redesign met inline zoekbalk vanaf xl en collapse-toggle op md→xl, h-16 header-hoogte, alle pages gemigreerd, grid-pages naar 5–6 cols op brede schermen, sponsored-rows tot 6 items met progressive disclosure), op 2026-05-01 **Fase 26 — Light-mode redesign** (page-bg #ffffff → #f5f6f8 voor surface-hiërarchie, --card blijft wit, nieuwe shadow-card / shadow-card-hover utilities, glass-utilities in light naar solide card-look (dark behoudt glass), section-gradient ontblauwd, alle 15+ admin-pages/-components gestript van rauwe bg-white/border-slate-200, hover:bg-white/X overlays in forms naar hover:bg-muted), en op 2026-05-01 **Fase 27 — Marktplaats: bundle-offer + pickup-flow + listing-statussen** (DRAFT/PAUSED/RESERVED listings met PAUSED-cascade voor PENDING proposals + bundle-offers; buyer-initiated multi-listing bundle-voorstellen via chat met PLATFORM/EXTERNAL split — SHIP gebruikt escrow + 5d payment-deadline, PICKUP off-platform met 14d reservation-timeout en geen wallet-impact; uitgebreide pickup-flow met locatie op listing, ophaalmoment-voorstel via chat, 4-cijfer code-confirm met 5-attempts-lockout, dispute-blokkade voor EXTERNAL; 4 nieuwe crons en uitgebreide cleanup-archived-chats voor BundleProposal), op 2026-05-02 **Fase 27.31-27.36 — Marktplaats QoL** (pickup-code 4 cijfers + 1 hoofdletter zonder O/I via crypto.getRandomValues, 240k mogelijkheden; allowDirectBuy/acceptsOffers/suggestedPrice toggles op listing met Direct Kopen-knop voor non-stocked FIXED listings; conditionRange UI van/tot voor COLLECTION; closePartiallySoldListing actie; ActivePickupsSection prominent in /aankopen; watchlist-status-badges met opacity-styling voor non-buyable; stock-badge "X op voorraad" op listing-card voor SEALED/OTHER), op 2026-05-02 **Fase 27.39-27.43 — Marktplaats payment-modes opschoning** (allowPlatformPickup/allowExternalPickup seller-toggles voor PICKUP/BOTH listings; buyListing accepteert deliveryChoice met 3 paden — SHIP/PICKUP_PLATFORM/PICKUP_EXTERNAL; BuyNowButton + BuyQuantityForm + nieuwe PickupReserveButton voor non-stocked en stocked listings; bundle-offer 3-way keuze in form; EXTERNAL pickup naar 1-knops koper-confirm via confirmExternalPickup ipv code-input; "Markeer als verkocht" knop verwijderd want alle echte flows zetten SOLD automatisch; ShippingBundle.deliveryMethod snapshot; DeliveryChoice type in apart src/lib/listing-types.ts), op 2026-05-03 **Fase 27.81 — /marktplaats/nieuw UX-pass** (centrale checklist-helper voor alle vereiste velden, sectie-status-pills compleet/onvolledig/optioneel, char-counters, scroll-to-error, preview-validatie-summary, shippingMethodIds verplicht voor SHIP/BOTH server-side, dode step-shipping.tsx weg) op 2026-05-03 **Fase 27.83 — DRAFT verwijderd** (concept-flow opgegeven; saveDraft/publishDraft/deleteDraft + draftListingSchema + sectie + i18n-keys + status-enum verwijderd; bestaande DRAFT-rows naar DELETED), op 2026-05-04 **Fase 27.103-27.104 — Plaats + afstand op item-cards** (NL PC4 → lat/lng dataset met haversine-helper; SellerLocationLine gedeelde component voor "📍 Plaats · 12 km"; same-country only met cross-border vlaggetje; <1km privacy-floor; uitgerold op marktplaats/veilingen/claimsales/homepage hoofdgrid + sponsored-rows; getBuyerLocation breidt bestaande getBuyerCountry uit met postalCode), op 2026-05-04 **Fase 27.105-27.107 — Marktplaats list-view + filters** (Marktplaats-stijl twee-koloms layout met sticky filter-sidebar links en lijst/grid-toggle rechts; default lijst-view; filters: prijs, type, conditie, verkoopvorm, levering, gratis verzending, afstand-radius, geverifieerd, sinds-wanneer; URL-based filter state voor shareable links; mobile slide-over drawer met counter-badge; ListingListRow met conditie-pill kleur per niveau, delivery-badges sky-blue/amber, Direct kopen + Voorstel doen pills onder elkaar gestackt, watchlist-overlay op image; "Bieden"→"Voorstel doen" voor listings want bieden is veiling-jargon), op 2026-05-04 **Fase 27.108-27.109 — Profiel-page herstructureren + adres-cooldown + n.o.t.k.** (User.lastAddressChange schema-veld + ADDRESS_COOLDOWN_DAYS=30 helper; updateAddress server-side cooldown-guard, eerste keer gratis daarna 30d geblokkeerd; AddressForm display met laatst-gewijzigd-datum en lock-icoon bij actieve cooldown; /dashboard/profiel volledig herschreven naar 9 secties (profielinfo/account+login/adres/bedrijf/verzendgebied/bank/voorkeuren/personalisatie-link/geschiedenis); BannerSelector verplaatst naar /customization; 11 missing shipping i18n keys toegevoegd; prijs-fallback "Voorstel" → "n.o.t.k." op listing-card en list-row, "Voorstel doen" badge blijft voor de actie), op 2026-05-04 **Fase 28 — Orderdetails-popup + refund-flow herzien + cancel-flow symmetrisch + escrow-shipping waterdicht + auto-cancel-stale-paid** (`order-detail-modal.tsx` major rewrite voor /dashboard/aankopen + /verkopen: visuele 4-staps tijdlijn boven (SHIP=Betaald→Verzonden→Geleverd, PICKUP=Gereserveerd→Afspraak→Opgehaald, +Refund-stap als refundedAmount>0, rode Geannuleerd-stap bij CANCELLED) met `buildTimelineSteps` helper; sorteer-dropdown op item-lijst met 7 keys — sortering propageert naar pakbon; **refund-flow gesimplificeerd** naar manueel bedrag + optionele reden, geen item-checkboxes meer (loste quantity-bug op gegroepeerde items op); refund-window verlengd naar 30d na delivery (`COMPLETED_REFUND_WINDOW_DAYS`) met split tussen escrow-deduct (SHIPPED) en seller.balance-deduct (COMPLETED) in `issueSellerRefund`; **COMPLETED-pad checkt seller's `available = balance − reservedBalance`** ipv alleen balance — voorkomt dat refund auction-bid-reserves onderdekkend maakt; refund-history blok per-event via Transaction-records (page-query met description-prefix `Gedeeltelijke terugbetaling`, reden uit suffix); refund-badge op bundle-card in collapsed view (line-through op totaal + netto-prijs + groene "−€X"-pill); verzendblok met carrier+service+shippedAt; **directe-cancel verwijderd voor beide partijen** — `cancelPurchase` + `cancelOrderBySeller` actions weg; `<CancellationActions>` inline op PAID in beide views, beide partijen gebruiken altijd `requestCancellation` (mutual akkoord, 7d-deadline) — voorheen kon seller direct annuleren zonder buyer-akkoord, schond de leverplicht; PAID-hint voor seller "Refund mogelijk zodra je verzendt"; pakbon i18n-NL hardcoded; 23+ nieuwe `common.orderDetail` i18n-keys NL+EN; **shipping-escrow waterdicht** — voor Fase 28 werd shippingCost bij claimsale-checkout wel van buyer gedebiteerd maar niet aan seller's heldBalance toegevoegd; bij refund/cancel kreeg buyer het wel terug → geld uit het niets. Nu één model: heldBalance bevat altijd `bundle.totalCost` (items + ship) over alle flows (cart-claimsale, listing, proposal hadden al deels deze conventie); `releaseEscrow` heeft nieuwe optionele `commissionableAmount`-param zodat seller bij delivery items+shipping krijgt maar commissie alleen over items wordt geheven; `confirmDelivery`/`autoConfirmDeliveries`/`pickup.confirmPickup` zijn refund-aware (`releaseAmount = totalCost−refundedAmount`); `respondToCancellation` ACCEPT en `dispute.ts` (5 paden — autoResolve, mutual-proposal-accept, admin-decision BUYER/SELLER/PARTIAL) gebruiken nu eveneens totalCost-aware refund-bedragen; **auto-cancel-stale-paid cron** (dagelijks) annuleert PAID SHIP-bundles waar seller 14d niet heeft verzonden — volledige refund naar koper, items terug op markt, `ShippingBundle.autoExpiredAt`-marker zodat admin via `/dashboard/admin/seller-warnings` repeat-offenders kan identificeren (extra accent voor sellers die eerder een cancel-aanvraag afwezen — gouden ban-flag); symmetrisch met `auction-payment-deadline` — wie z'n verplichting niet nakomt verliest automatisch), op 2026-05-04 **Fase 29 — Veiling-binding + reserve-versoepeling + strike-systeem + IP-tracking** (reserve over de hele linie van 40% → 15% in `BID_RESERVE_RATE` constante; verified-eis voor bids ≥ €2500 in `placeBid` + `setAutoBid` met error-code `VERIFIED_REQUIRED_FOR_HIGH_BID`; €200 borg-forfait virtueel — uit `User.balance` gepakt op moment dat cron PAYMENT_FAILED flagt voor finalPrice ≥ 2500, geen pre-storting; strike-systeem met `User.paymentFailureCount` + `paymentFailureLastAt`, 2× = 30d auto-suspend, 3× = permanent via nieuwe `suspendUserSystem` zonder admin-guard; bedrijfsvrijstelling `isBusinessBidExempt` — admin-toggle op BUSINESS-account met valide vatNumber+cocNumber via `setBidDepositExemption` action; nieuwe crons `payment-failure-decay` (wekelijks, −1 op count na 365d zonder failure) en `prune-bid-ips` (wekelijks, NULL bidderIp >90d voor privacy); IP-tracking minimaal subset: `User.lastLoginIp/At` snapshot in `auth.ts authorize` callback, `AuctionBid.bidderIp` snapshot bij elke bid, hard-block in placeBid als bidder-IP overlap met seller's lastLoginIp binnen 7d-window (error `BID_IP_OVERLAPS_SELLER`), soft-flag via `logAdminAction({ action: "BID_IP_OVERLAP" })` bij meerdere accounts vanaf één IP; `AdminAuditLog.adminId` nullable + `actorType` veld voor system-events; admin user-detail krijgt veiling-status sectie met strike-counter, login-IP, en exemption-toggle component; KPI-tegel "Wanbetalers (geschorst)" op admin-overview; auction-payment-deadline route gerefactord naar CRON_JOBS-dispatch om duplicatie te vermijden; new constants in `src/lib/auction/bid-tiers.ts` plus pure `bidPassesVerifiedGate` helper), en op 2026-05-05 **Fase 30 — Reserve naar 10% + €2000-drempel + real-time SSE-infrastructuur over de hele site** (reserve `BID_RESERVE_RATE` 15% → 10% en `VERIFIED_BID_THRESHOLD` €2500 → €2000 — 10% × €2000 = €200 borg klopt visueel; **stale-autobid bug-fix**: in `recalculateTotalReserved` telt een autobid alleen als z'n maxAmount het volgende minimum-bod kan halen, anders functioneel dood en geen reserve meer; complete SSE-infrastructuur via `src/lib/realtime.ts` (in-memory pub/sub op `globalThis` om HMR-reloads te overleven), `src/app/api/events/stream/route.ts` (Node-runtime, heartbeat 25s, channel-whitelist `auction:`/`claimsale:`/`listing:`), `useRealtimeEvents` hook met auto-reconnect (exponential backoff capped 30s), `<RealtimeProvider>` wrap in locale-layout, single EventSource per browser-tab gedeeld door alle subscribers; **bell-based notification UX vervangt toasts** — `notification-bell` + nieuwe `message-icon` met hover-popover + shake-animatie + auto-flash bij nieuw event, klik navigeert naar overzichtspagina (toasts verwijderd uit RealtimeProvider, achievement-unlock behoudt rich Trophy-toast); event-types: `outbid`/`bid-placed`/`auction-won`/`auction-ended`/`balance-changed`/`achievement-unlocked`/`notification-created`/`new-message`/`claimsale-item-claimed`/`cart-changed`/`listing-changed`/`bundle-changed`/`dispute-changed`/`withdrawal-changed`/`verification-changed`/`suspension-changed`; **page-specifieke real-time updates** op alle relevante pages: `<UserBalance>` (header pill flash), live-auction-content (bid-placed via auction-channel), live-claimsale-items (item-claimed via claimsale-channel), `useChatPolling` (hybrid SSE+poll voor /berichten/[id]), listing-detail-page via `<ListingRealtimeWatcher>`, /aankopen + /verkopen via bundle-changed/dispute-changed, dispute-detail page filter op disputeId, /winkelwagen + cart-icon op cart-changed, /uitbetalingen + /verificatie + dashboard-suspension-banner via dedicated user-channel events, /dashboard/volglijst op listing-changed/auction-ended/bid-placed, **alle proposal/bundle-offer/pickup chat-acties** publishen `new-message` via centrale `publishNewMessageForConversation` helper zodat chat-thread elk system-message direct toont; **alle status-flippende crons publishen events**: cancellation-expiry, bundle-offer-payment-deadline, pickup-reservation-timeout, auto-cancel-stale-paid, proposal-payment-deadline, expire-claims, auction-payment-deadline, auto-confirm — UI updatet instant zodra cron flipt; nieuwe helpers `useRefreshOnRealtime([types], filter?)` voor pages, `<RealtimePageRefresh events={...}>` voor server-rendered layouts, `<ListingRealtimeWatcher listingId>`; polling blijft als fallback (notification-bell/message-icon/cart-icon op 30s, live-auction op 15s/2s laatste 30 min, live-claimsale op 15s, chat op 5s/15s adaptive); hosting-aanname Railway single-instance Node-container — voor multi-instance zou alleen `src/lib/realtime.ts` swappen naar Redis pub/sub via ioredis (~30 regels), interface ongewijzigd), en op 2026-05-05 **Fase 31 — Monetisatie-laag herontwerp** (TierKey uitgebreid met `ENTERPRISE`; nieuwe prijzen+commissies FREE 3% / PRO €14,95+€149jr / 2,5% / Unlimited €39,95+€399jr / 2% / Enterprise €749 / 0%; ADMIN-fallback nu naar ENTERPRISE-perks ipv UNLIMITED; nieuwe TierFeatures bulkUpload/vanityShopSlug/searchBoost/accountManager + TierLimits.freeHomepageSpotlightsPerMonth (0/1/5/999); break-evens in `TIER_BREAK_EVENS_MONTHLY`; abonnement-page herschreven naar 4-koloms grid met monthly/yearly billing-toggle, break-even-zin per tier, current-subscription-card; **Mollie recurring-billing uitgesteld naar Fase 17** — schema-hooks gereserveerd op `User.mollieCustomerId` + `Subscription.mollieSubscriptionId/paymentStatus/gracePeriodEnd/nextBillingAt/legacyPricing`, PRO/Unlimited-knoppen disabled "Binnenkort beschikbaar"; **free-upsell-quota** per tier maandelijks gereset via nieuwe `reset-free-upsells` cron (1e van de maand), `applyFreeUpsellsToCost`-allocator deelt quota greedy lineair over HOMEPAGE_SPOTLIGHT-entries, `User.freeUpsellsRemaining` decrement in createListing/createAuction-transactie + direct-quota bij `upgradeToTier`; **vanity shop-URL** `/winkel/<naam>` voor Unlimited+ via `User.shopSlug @unique`, `updateShopSlug` action met reserved-list (22 paden), redirect-route die naar `/verkoper/<userId>` doorvuist (notFound bij downgrade); **subtle tier-based search-boost** — `User.tierRank` denormalisatie (1-4), als SECONDARY orderBy in marktplaats/veilingen/claimsales/zoeken zodat FREE geen page-1-positie verliest; **Enterprise-aanvraag-flow** — nieuw `EnterpriseRequest`-model + `submitEnterpriseRequest`/`approveEnterpriseRequest`/`rejectEnterpriseRequest` actions, `/dashboard/abonnement/enterprise-aanvraag` aanvraag-page, `/dashboard/admin/enterprise-requests` admin-queue met PENDING/APPROVED/REJECTED filter-tabs en inline approve-form (custom monthlyPrice + billingCycle) + reject-form, AdminNav-entry, `upgradeToTier` extended met `customMonthlyPrice` param + ENTERPRISE-tier-support; **bulk-upload CSV voor PRO+** — `papaparse` + Zod BulkRowSchema in `src/lib/listing-bulk-import.ts`, `bulkImportListings(csvText)` action met tier-gate + pre-flight account-limit check + per-rij transactie (geen one-big-transaction), `/marktplaats/bulk-upload` 3-staps wizard (upload→preview met error-tabel→result), template-download knop op /dashboard/marktplaats voor PRO+; **3% buyer's premium op alle veilingen** — `AUCTION_BUYER_PREMIUM_RATE` + `calculateBidFees(bid)` in `src/lib/auction/fees.ts`, geen tier-discount, gaat 100% naar platform via `Transaction { type: "AUCTION_PREMIUM" }`; `calculateReserveAmount` intern aangepast naar 10% × `total` (bid+premium) zodat alle 8 callers automatisch correct werken; nieuwe wallet-helper `deductBidPayment(userId, bid, premium, desc, auctionId)` doet één $transaction met balance-decrement + 2 Transaction-rijen (PURCHASE + AUCTION_PREMIUM); buyNow + finalizeAuction full-payment + completeAuctionPayment gebruiken nu deductBidPayment, escrowCredit blijft op bid-deel; **AuctionBid.amount blijft het bod (zonder fee)** — publiek zichtbare bid-stream toont alleen het bod, premium wordt on-the-fly berekend voor de winnaar; nieuw `<BidConfirmationModal>` component met breakdown bod/fee/totaal/reserve, opent bij eerste bod per veiling via per-veiling localStorage-tracking + globale `User.skipBidConfirmation` toggle in /dashboard/profiel-Voorkeuren; oude inline buy-now-confirm vervangen door modal-flow; nieuw `<AuctionFeeExplainer>` collapsible disclosure-component op auction-detail page boven de bid-section met "ⓘ 3% veilingkosten" + 4 bullets (bescherming/anti-fraude/platform/support); **legacy-grandfathering geschrapt** — geen real users yet, dus geen migratie nodig; `Subscription.legacyPricing` veld blijft gereserveerd voor toekomstige prijswijziging zodra er real users zijn), en op 2026-05-05 **Fase 31 audit-fixes** (na multi-agent audit van auction/claimsale/listing-flows: **#1 premium-refund** — 3% buyer's premium werd nooit teruggegeven bij volledige cancel/dispute-BUYER, koper verloor structureel 3% per teruggedraaide auction. Nieuwe `refundAuctionPremium(buyerId, auctionId)` helper in wallet.ts (idempotent via `AUCTION_PREMIUM_REFUND`-Transaction-check) geïntegreerd in 4 paden: auto-cancel-stale-paid cron, respondToCancellation ACCEPT, dispute auto-resolve seller-no-response, admin BUYER-decision. Niet in partial-refund-paden — premium is platform-fee voor auction-faciliteit, blijft bij gedeeltelijke refund. **#2 ADMIN-mapping** — `getSubscriptionInfo` mapde ADMIN nog naar UNLIMITED, inconsistent met `getTierConfig` die nu naar ENTERPRISE mapt. ADMIN-users zagen verkeerde tier-config op /dashboard/abonnement. **#3 free-upsell quota race** — findUnique-snapshot tot decrement was niet atomic, parallelle createListing/createAuction-calls van zelfde user konden dezelfde quota dubbel claimen. createListing nu met conditional `tx.user.updateMany({ where: { freeUpsellsRemaining: { gte: freeUsed } }, data: { decrement } })` + throw "FREE_UPSELL_QUOTA_RACE" → tx-rollback → user-friendly error. createAuction met logging-only fallback (auction al aangemaakt buiten tx, volledige fix vereist refactor). **#4 bulk-upload validation gaps** — pre-flight checkListingLimit was snapshot zonder per-rij re-check (limit-bypass-vector); createSingleBulkListing bypaste createListing's "minstens één non-LETTER shipping-method"-check (broken listings). Beide nu gefixt: per-rij liveLimit-check breaks loop bij hit + non-LETTER + shippingMethodIds-required-voor-SHIP validaties in createSingleBulkListing. **#5 buyNow partial-payment $transaction** — 4 sequentiële prisma-calls (auction.update + auctionBid.create + syncReservedBalance + createPendingBundle) bij failure tussen stap 1-3 gaf orphaned auction in AWAITING_PAYMENT zonder bid-record waardoor cron-rotatie geen runner-up vond. Nu eerste 3 in atomic $transaction, syncReservedBalance buiten (idempotent). **#7 reserve-naamgeving** — `calculateReserveAmount(bidAmount)` accepteert bid maar berekent intern op total — misleidend. Prominente jsdoc-warning + alias `calculateReserveForBid` voor toekomstige callers. Nieuwe Transaction-type `AUCTION_PREMIUM_REFUND` gebruikt voor refund-tracking + idempotency.), en op 2026-05-06 **Fase 32 — Monetisatie afronden** (zes-stappen polish van het betaalsysteem zodat het productie-klaar is: **#1 buyer's premium 3% → 2,9%** — `AUCTION_BUYER_PREMIUM_RATE` constante verlaagd, `wallet.ts deductBidPayment` description nu dynamisch via `PREMIUM_RATE_LABEL` (in-line const die `.toFixed(1).replace(/\.0$/, "")` toepast), `refundAuctionPremium`-regex generieker (`/^Veilingkosten \d+(?:[.,]\d+)?%: /`) zodat oude "3%"-rows blijven matchen — backward-compat zonder migratie. UI-componenten BidConfirmationModal/AuctionFeeExplainer/BidConfirmationToggle tonen rate dynamisch met `.toFixed(1).replace(/\.0$/, "")`. NL/EN i18n `subscription.auctionPremiumNote` bijgewerkt. Comments overal gestript van hardcoded "3%" — rate-bron is nu enkel de constante. **#2 `BID_PAYMENT_FAILURE_FEE` 2,9%-boete bij PAYMENT_FAILED** — nieuwe constante `PAYMENT_FAILURE_FEE_RATE = 0.029` in `bid-tiers.ts`, geldt voor ALLE bedragen (niet alleen ≥€2000), geclampt op `User.balance` ná de bestaande €200 forfait-deduct. Beide deducts gestackt in één `prisma.$transaction` met sequentiële balance-tracking zodat Transaction-rows correcte `balanceBefore`/`After` hebben. Voorbeelden: €1000-bod → fee €29; €2500-bod → forfait €200 + fee €72,50 = €272,50; €2500-bod met €100 saldo → forfait €100 (clamped), fee €0. Notificatie-tekst toont breakdown. Strike-counter blijft onafhankelijk. Geen schema-wijziging want `Transaction.type` is `String`. Mentaal model: 10%-reserve in `User.reservedBalance` fenced de balance zolang AWAITING_PAYMENT, dus de 2,9%-boete past altijd binnen de fence (2,9% < 10%) — clamp is safety-net voor edge-cases. **#3 bundle-offer commissie-bug fix** — `respondToBundleOffer` vulde 3 ShippingBundle-creates (PAID, partial PENDING, EXTERNAL) met `totalItemCost = totalAmount` (incl. shipping) en `shippingCost = 0`. `releaseEscrow` neemt `commissionableAmount = bundle.totalItemCost`, dus seller betaalde tier-commissie ook over verzendkosten bij multi-listing bundles (FREE 3% × €5 shipping = €0,15 te veel). Fix: `acceptedShippingPrice` opslaan uit gekozen `SellerShippingMethod`, `shippingCostForBundle` = price voor SHIP / 0 voor PICKUP, `itemsTotalForBundle = max(totalAmount - shippingCost, 0)`. EXTERNAL pickup-pad behoudt `totalItemCost = totalAmount` (geen escrow/commissie + comment toegevoegd). Backward-compat sunk cost gedocumenteerd. **#4 `releaseEscrow` shortfall-clamp** — bij `heldBalance < amount` (data-corruptie of pre-Fase-28 legacy-data) gaf `releaseEscrow` seller `amount - commissie` ongeacht de werkelijke hold = "geld uit het niets". Fix: `actualReleased = safeEscrowDecrement`, `commissionBase = min(commissionableAmount ?? actualReleased, actualReleased)`, `sellerReceives = round(actualReleased - commissionAmount)`. Happy-path identiek bij `safeEscrowDecrement === amount`; alleen shortfall-pad corrigeert. `console.error` blijft loggen voor admin-zichtbaarheid. **#5 commissie-grondslag UX-tooltip** — nieuwe i18n-key `subscription.commissionBaseTooltip` ("Commissie wordt geheven over de verkoopprijs van de items, exclusief verzendkosten."). `CurrentSubscriptionCard` toont Info-icoon naast commissie-percentage; `TierGrid` ondersteunt nu een optioneel `tooltip`-veld op feature-rows en past 'm toe op de commission-feature in elke tier-card. **#6 auction-bundle premium-zichtbaarheid op /aankopen** — `aankopen/page.tsx` berekent `auctionPremium = auction.finalPrice * AUCTION_BUYER_PREMIUM_RATE` voor auction-bundles (on-the-fly, geen schema-wijziging). `PurchasesContent BundleCard` toont Info-icoon naast `totalCost` met tooltip die de splitsing en het werkelijk-betaalde-totaal toont (verwijst naar /dashboard/saldo voor de aparte AUCTION_PREMIUM-tx). Listing/claimsale-bundles hebben `auctionPremium = null` en blijven ongewijzigd. **#7 bid-section UX-pass** — `<form action={handleBid}>` veroorzaakte een React 19 form-action-reset die de input-waarde terugzette naar `defaultValue={minimumBid}` op het moment dat de bevestigingsmodal opende → vervangen door `onSubmit + e.preventDefault + new FormData(e.currentTarget)`. Balance-info-blok verwijderd (saldo zichtbaar via header), alleen "Maximaal bod" inline naast "Minimaal bod" boven de bid-input. Quick-bid chips herontworpen: "Bied"-prefix weg, secundair gestylde chips met alleen het bedrag, verplaatst naar onder de bid-input. Eerste chip = exact `minimumBid` (zodat je 'm direct kunt bieden), tweede en derde = ronde bedragen via `niceStep` afhankelijk van increment-niveau (≤€1→€1, ≤€5→€5, >€5→€10). AuctionFeeExplainer verplaatst van boven `BidSection` naar onder `AutoBidForm` (referentie-info, hoort niet boven de actie). Modal-tekst "reserve wordt vrijgegeven … of de veiling eindigt" was fout voor de winnaar — herschreven naar drie scenario's (overboden / win+saldo / win+geen saldo + 5d). Percentage-labels in modal nu als subtle pill-badges (`bg-card border-border` voor zichtbaarheid in dark mode + `leading-none` voor verticale uitlijning) i.p.v. tussen-haakjes. "Door te bieden ga je akkoord met de voorwaarden"-tekst gecentreerd. **#8 account-age restricties verwijderd** — `src/lib/account-age.ts` + 7 `checkAmountAllowed`-aanroepen in auction/listing/claimsale/cart/proposal/bundle-offer-actions weg. De caps (0-24h €50 / 1-7d €200 / 7+d €500) waren overlappend met het modernere bid-systeem (10% reserve-fence + €2000 verified-eis + strike-suspend + 2,9% payment-failure-fee) en stuurden goedwillende nieuwe users irritant af. Verificatie blijft via `User.isVerified` voor de Fase 29 verified-bid-threshold (€2000+). **#9 verificatie-uitbreiding naar 3 types** — Drie aparte trust-signals naast elkaar, schema-wijziging op User: `isIbanVerified` + `isAddressVerified` Booleans + `addressVerificationStatus` String. VerificationRequest krijgt `type` (ID|ADDRESS) + `addressDocumentType` (TAX_LETTER/UTILITY_BILL/BANK_STATEMENT/MUNICIPAL_LETTER) + `documentType` nu optional. IBAN-flow: `confirmBankTransfer` accepteert optionele `senderIban`-param, server vergelijkt genormaliseerd met `User.iban`, bij match flipt `isIbanVerified=true` + notificatie. Adres-flow: `submitAddressVerification` (zelfde patroon als ID), `adminReviewVerification` leest `request.type` en flipt het juiste user-veld. `revokeVerification(userId, type)` accepteert nu ID|IBAN|ADDRESS. `/dashboard/verificatie` herontworpen naar 3-secties layout (één per type, eigen status-card + form). Admin-verification-list toont type-pill (ID=sky / Adres=violet) + profiel-adres bij ADDRESS-aanvragen. Nieuwe `<TrustBadges>` component (alleen geverifieerde types tonen, kleuren ID=sky/IBAN=emerald/Adres=violet) gebruikt op `SellerInfoBlock` + `SellerReputationCard`. Legacy `VerifiedBadge` blijft voor compacte kaartjes (listing-card/sponsored-row) die alleen ID-only tonen. Trust-badges geven geen blocking effects — IBAN/Adres zijn pure trust-signals, ID blijft de €2000-bid-gate.), en op 2026-05-06 **Fase 33 — Verzending statisch** (volledige rewrite van het verzendsysteem naar een statische tariff-catalog. **Schema**: `SellerShippingMethod` krijgt `service` (MAILBOX_PARCEL/PARCEL_STANDARD/PARCEL_SIGNED) + `zone` (DOMESTIC/EU_NEAR/EU_FAR/EU_FAR_ISLAND) + `priceOverride` Float?, met `@@unique([sellerId, service, zone])`. Legacy-velden `serviceName/countries/shippingType/isDefault/isTracked/isSigned/price` op nullable gezet, niet gedeleted (FK-snapshots in join-tabellen blijven werken). **Tariff-catalog** in `src/lib/shipping/tariffs.ts`: twee-laags model — landspecifieke DOMESTIC voor 16 landen (NL/BE/DE/FR/AT/IE/LU/FI/IT/ES/SI/SK/HR/EE/LV/LT) + tier-fallback voor de rest (NORTHERN/SOUTHERN/EASTERN/ISLAND). Cross-border tarieven (`EU_NEAR €9.50/€13.50`, `EU_FAR €14.50/€18.50`, `EU_FAR_ISLAND €19.50/€24.50`) zijn niet origin-specifiek — pan-EU carriers prijzen vrijwel identiek per route. **Briefpost geschrapt** (untracked = risico voor TCG-disputes). **Cross-border-altijd-signed-regel vervallen**: PARCEL_STANDARD mag nu in EU_NEAR/EU_FAR onder €150. Boven €150 blijft SIGNED verplicht in alle zones. **Zones** in `src/lib/shipping/zones.ts`: symmetrische `EU_NEAR_NEIGHBORS` gegenereerd uit één-richting-array `EU_NEAR_PRIMARY_PARTNERS` om tegenrichting-vergeet-fouten uit te sluiten. Bewust conservatieve buurland-mapping (NL↔BE alleen, niet NL↔DE) — realistische handelspartners, niet geografische buren. MT/CY krijgen automatisch EU_FAR_ISLAND voor cross-border. **`User.sellingCountries`** generiek gemaakt: `DOMESTIC_ONLY | DOMESTIC_AND_NEAR | ALL_EU` (legacy `NL_ONLY/NL_BE/ALL_EU` worden via `normalizeSellingScope` gemapt). Default voor nieuwe users: DOMESTIC_AND_NEAR. **Server actions** (`src/actions/shipping-method.ts`) volledig herschreven: `createShippingMethod`/`deleteShippingMethod` verwijderd; `updateShippingSlot` accepteert alleen carrier+priceOverride+isActive (±20% server-validatie via `validatePriceOverride`); nieuwe idempotente `setupStaticShippingMethods(userId)` upsert per slot waar basePrice bestaat (max 7: 1 mailbox + 6 parcel). `updateSellingScope` triggert automatisch een `setupStaticShippingMethods`-resync zodat slots in/uit de scope correct flippen. **filter.ts** `getSellerCountryFilter` herschreven naar zone-based: per buyer-country pre-computeert het welke seller-countries DOMESTIC/NEAR/FAR zijn en bouwt een Prisma OR-where met scope-matching. **UI**: `<ShippingMethodSelector>` accepteert nu `EnrichedShippingMethod[]` (vanuit `getSellerShippingMethods` action met basePrice/effectivePrice/min/max-grenzen verrijkt). `<ShippingMethodsManager>` herschreven naar vaste-tabel met per-slot carrier-dropdown + clamped prijs-input + active-toggle. `<SellingScopeToggle>` is origin-aware: optie 2 toont werkelijke buurlanden ("Nederland + buurlanden (België)"), disabled voor IE/GR/MT/CY met tooltip "Geen EU-buurlanden voor jouw locatie". `<ShippingMethodPicker>` (checkout) filtert op `zoneFor(seller.country, buyer.country)` + SIGNED-only ≥€150. **Snapshot-pattern blijft**: `effectivePrice = priceOverride ?? basePrice` op moment van listing-create wordt naar `Listing/Auction/ClaimsaleShippingMethod.price` gesnapshot zoals voorheen. **Migratie-script** `scripts/migrate-shipping-v2.ts` zet legacy-records op `isActive=false` (niet hard-delete) en draait `setupStaticShippingMethods` voor elke user met `User.country` gevuld. Files toegevoegd: `tariffs.ts`, `zones.ts`, `static-methods.ts`. Files verwijderd: `defaults.ts`, `shipping-method-form.tsx`, `selling-countries-toggle.tsx`. **Iteratie-feedback verwerkt**: (a) bug-fix in `setupStaticShippingMethods` — `update: { isActive: inScope }` always (was `inScope ? {} : { isActive: false }`) zodat scope-upgrades automatisch zones weer activeren; (b) tolerance verhoogd 20% → 50% (`PRICE_OVERRIDE_TOLERANCE`); (c) prijs-edit naar `<PricePopover>` (UserBalance-stijl popover, click-toggle + click-outside + Escape) met amber tip-card "houd verzendkosten laag" en optionele Reset-knop; (d) eilanden volledig geschrapt — `EU_FAR_ISLAND` zone weg, MT/CY in SOUTHERN tier (DB-cleanup-deleteMany toegevoegd in `setupStaticShippingMethods` voor stale-zone-rijen); (e) headers per zone in manager met dynamische EU_NEAR-label "Naar [buurlanden]" via `formatList` helper; (f) scope-description nu dynamisch "Ik verzend alleen naar Nederland en België" via `formatScopeLabel(origin, neighbors)`; (g) **required-slot regel** — `PARCEL_SIGNED` verplicht actief in elke in-scope zone (fallback voor orders ≥€150), grijze "Verplicht"-pill met lock-icoon ipv toggle, server-guard in `toggleShippingMethod`; (h) **scope-filter UI** — out-of-scope zones volledig verborgen in manager (waren visible-but-inactive, gaf bypass-vector via toggle); (i) i18n + schema-comments overal "20%" → "50%" gemigreerd. **Open follow-ups**: cross-border tarieven en tier-fallbacks zijn baseline-schattingen — vóór productie-launch valideren met live-tests; eilanden MT/CY zitten nu in SOUTHERN-fallback (€7.50/€11.00) — bij eerste seller daar bevestigen via DHL/UPS rate calculator), en op 2026-05-10 **Fase 35 — Promotie-sectie redesign + veilingen/claimsales marktplaats-stijl + checkout-lock** (drie samenhangende delen op één branch: **Deel A — Promotie-sectie**: nieuw `AuctionLabel`-model in schema (auctionId/type/colorKey/cost/createdAt) parallel aan `AuctionUpsell`; nieuwe library `src/lib/auction/labels.ts` met `LABEL_TYPES` (9 stuks: GEEN_RESERVE/DIRECT_KOPEN/MOET_NU_WEG/HOT_ITEM/TOPSTAAT/ZELDZAAM/HOLO_FOIL/SNEL_VERZONDEN/COMPLETE_SET — laatste alleen voor COLLECTION-type), `LABEL_COLORS` (10-kleuren palet ruby/amber/emerald/sky/violet/rose/indigo/slate/orange/teal), `COLOR_CLASSES` Tailwind-mapping, `calculateLabelCost(count)` bundle-tarief (1=€0,99, 2=€1,69 met €0,29 korting), `MAX_LABELS_PER_AUCTION = 2`, `availableLabelsFor({reservePrice, buyNowPrice, condition, auctionType})` voor conditional availability + server-side anti-tamper hercheck, en `SPOTLIGHT_CTR_MULTIPLIER=15`/`CATEGORY_HIGHLIGHT_CTR_MULTIPLIER=8`/`LABELS_CTR_MULTIPLIER=3` ("tot Nx meer klikken" indicaties met disclaimer "branchegemiddelde"). **Window-mode upsells**: `AuctionUpsell` gebruikt nu `startsAt`+`expiresAt` op basis van dual-handle slider (1-indexed inclusive days, `days = endDay - startDay + 1`), berekend in `createAuction` op `auction.startTime`-basis. Speciale teksten: "op de laatste dag" (window=1d laatste dag) en "de hele veiling" (volledig venster). Surface-label dynamisch ("op de homepage" voor HOMEPAGE_SPOTLIGHT, "in de categorie-uitlichtingen" voor CATEGORY_HIGHLIGHT). **`URGENT_LABEL`-upsell opgeheven**: niet meer aangeboden in UI, bestaande records lopen vanzelf af. **Promotie als 7e step** (eigen sectie ipv inline in Bezorging) in `multi-step-auction-form.tsx` met Megaphone-icoon in `auction-form-progress.tsx`; `step-upsells.tsx` rewrite naar vertikale stack van 3 cards (Spotlight / Category / Labels) met dual-handle Slider, kleur-popover via Pencil-icoon, X-knop voor remove, counter-pill "1/2 labels gekozen", live-preview-strings, stale-state-fix via `useEffect` (filter labels bij reservePrice/buyNowPrice/condition-mutaties zodat "Geen Reserve"-label automatisch wegvalt als reserve toegevoegd wordt). Header: "Max 2 labels · €0,99 voor 1, €1,69 voor 2 — eenmalig per veiling · kleur inbegrepen". `<AuctionLabels size="sm|md">` render-component met `LABEL_TEXT`-mapping (DIRECT_KOPEN toont `formatPrice(buyNowPrice)`) op `auction-card`/`auction-list-row`/`live-auction-content`/`sponsored-row`/detail-page. **Promotie-cost overzicht** op step-review (`PromotionCostBlock`): per-spotlight rij + labels-rij + totaal. **Cancel-auction met refund**: `cancelAuction` accepteert nu SCHEDULED + ACTIVE; nieuwe `refundAuctionPromotion(auction, prismaTx)` doet pro-rata spotlight-refund (`cost × (expiresAt - now) / (expiresAt - startsAt)`) + 100% labels-refund — geen quota-return (gratis-quota is 1× claim per slot, niet refundeerbaar). Confirmation-dialog vermeldt het exacte bedrag, sonner-toast bij annuleren. **Deel B — `/veilingen` marktplaats-stijl layout**: nieuwe `src/lib/auction-filters.ts` (parseAuctionFilters/buildAuctionFilterWhere/countActiveAuctionFilters) met filters: priceMin/Max (op `currentBid`), types, conditions, durations, hasBuyNow, noReserve, hasBids, radius, verifiedOnly, since (today/week/month/all), view (list/grid). Nieuwe componenten in `src/components/auction/`: `auction-list-row.tsx` (countdown-overlay links op image — indigo voor SCHEDULED, "Geen reserve"-pill bewust subtiel via `bg-muted text-muted-foreground ring-border` in donker, labels size="md", "Bied nu"/"Bekijk" knop conditioneel op SCHEDULED), `auction-view-toggle.tsx`, `veilingen-filter-sidebar.tsx`, `veilingen-mobile-filters.tsx`. `/veilingen/page.tsx` rewrite met two-column layout, sponsored-row alleen op page-1-zonder-filters, default view = list, radius post-filter in JS. **SCHEDULED zichtbaar**: query gebruikt `status: { in: ["ACTIVE", "SCHEDULED"] }`, sort-volgorde plaatst SCHEDULED achter ACTIVE via secondary `localeCompare` op status, voor "highest bid"-sort gebruikt `nulls: 'last'` zodat SCHEDULED zonder bod onderaan komt. ScheduledStartPill (indigo, "Start over X") naast normale CountdownPill (zwart). `auction-card.tsx` aangepast met `status`/`startTime`-props, "Kopen nu: €X" inline pill verwijderd (paid label vervangt het). **Deel C — `/claimsales` marktplaats-stijl layout**: nieuwe `src/lib/claimsale-filters.ts` (filters: priceMin/Max op item-prijsrange, itemType, hasShipping, itemCountMin via post-filter in JS — 5/10/25/50+, radius, verifiedOnly, since), nieuwe componenten `claimsale-list-row.tsx` (cover-image met "X kaarten" amber-pill + amber voortgang-bar + 7 thumbnails 96×128 rounded-sm sizes=256px quality=90 + "+N meer" dashed-tile, verzending-only badge met mb-3 spacing, prijsrange + amber "Bekijk items"-knop), `claimsales-filter-sidebar.tsx`, `claimsales-mobile-filters.tsx`, `claimsale-view-toggle.tsx`. `/claimsales/page.tsx` rewrite met two-column layout + item-include voor thumbnail-strip + radius/itemCountMin post-filter. **Deel D — Race-safe checkout-lock op claimsale-items**: `ClaimsaleItem.checkoutLockExpiresAt DateTime?` schema-veld + 5-minuten lock-window. **Strict one-shot lock-pattern** in `cart.ts checkout()`: `updateMany WHERE checkoutLockExpiresAt: null AND claimedAt: { gte: now-15min }` zodat lock één keer per claim-cycle gepakt kan worden (geen renew-exploit mogelijk waarbij user de lock indefinite kon verlengen). Hard-cap totaal alive-time = 15min basic + 5min lock-grace = 20min absolute max. Visible `expiresAt` = `max(claimedAt+15min, checkoutLockExpiresAt)` zodat UI-timer altijd matcht met server-side deadline. `expireClaimedItems`-cron skipt items met actieve checkout-lock. **Lock-reset propagatie**: alle AVAILABLE-flips zetten `checkoutLockExpiresAt: null` (`expireClaimedItems`/`unclaimItem`); alle CLAIMED-flips defensief reset (`claimItem`/`claimAllItems`). **UI-pause tijdens checkout-modal**: `cart-checkout.tsx` dispatchet custom `cart-checkout-locked`/`unlocked` DOM-events op modal-open/close; `cart-item-row.tsx` listent en pauseert countdown-interval + skipt auto-refresh-on-expired + toont "Vastgezet"-pill (ShieldCheck-icoon) ipv aftellende klok. `payment-method-modal.tsx` accepteert optionele `checkoutLockMinutes`-prop voor groene ShieldCheck-info-banner "Je items zijn N minuten vastgezet zodat je rustig kan afrekenen — ook als de claim-timer in je winkelwagen verloopt"; alleen claimsale-checkout passeert de prop, direct-buy laat 'm leeg. **Deel E — In-process expire-claims-scheduler**: nieuw `src/lib/expire-claims-scheduler.ts` (parallel aan auction-scheduler/auction-activator-scheduler) doet `setInterval(60_000)` op `expireClaimedItems`, HMR-resistant via `globalThis.__expireClaimsScheduler`, eerste tick 5s na boot. Boot-init in `src/instrumentation.ts`. Externe cron blijft als safety-net. **Deel F — GIF-upload weg**: `src/lib/upload.ts ALLOWED_TYPES`, `image-uploader.tsx accept`, `claimsale-form.tsx`, `profile-form.tsx`, `profile.ts` action allemaal van GIF gestript (untracked = risico op animated avatars/listings — past niet bij TCG-marketplace tone). **Deel G — Diverse UX-polish**: `step-type.tsx` (auction + listing) gebruikt nu `RectangleVertical`-icoon ipv `CreditCard` voor SINGLE_CARD (was visueel een bankpas). `multi-step-auction-form.tsx` scrolt naar boven (`window.scrollTo({top:0})`) op preview-klik zodat user direct de validatie-summary ziet. Mobile-bottom-bar `flex-col items-center sm:flex-row` zodat warning-chips netjes gestackt staan op mobiel. **Fase 36 — Homepage onderzijde redesign + dark-mode default + section-rhythm fix** (op 2026-05-11): **Deel A — Dark als default**: `theme-provider.tsx` `defaultTheme="dark"` + `enableSystem={false}`. localStorage-key blijft default ("theme") zodat reeds-toegglede users hun keuze houden. **Deel B — `bg-muted` dark-fix**: `.dark { --muted: #1e293b → #334155 }` (slate-700) in `globals.css` — bg-muted was identiek aan --card in dark, dus onzichtbaar op `bg-card` parents (~253 usages site-breed). Single-line CSS-var-fix lost het overal in één keer op. **Deel C — Cleanup**: 8 ongebruikte componenten uit `src/components/home/` verwijderd (`marketing-hero.tsx` v1, `how-it-works-section.tsx` v1, `pricing-section.tsx`, `cta-banner.tsx`, `hero-email-form.tsx`, `hero-particles.tsx`, `hero-stat-badges.tsx`, `features-section.tsx`). `countdown-label.tsx` gehouden — wordt door `home-auction-card.tsx` gebruikt. **Deel D — Section rhythm 2-tier**: `.soft-section-bg` (sage-mint gradient) volledig geschrapt uit globals.css. Hele homepage van How-it-works tot footer staat nu op strikte `bg-card` ↔ `bg-background` alternatie, met `section-gradient` als enige accent (tier-benefits sectie). Padding gestandaardiseerd op `py-12 lg:py-16` voor carousels en `py-16 lg:py-24` voor marketing-pillars. Beide `<SectionDivider />` calls weg uit `page.tsx`. Live-auctions-zone behoudt forced-dark `bg-slate-950` in dark-mode (marketing-impact), light gebruikt `bg-background`. **Deel E — Vier nieuwe content-secties** (allemaal logged-out only behalve RecentlySold die ook logged-in toont): (1) **`<RecentlySoldSection>`** + `src/lib/home-recently-sold.ts` helper — query op COMPLETED+PLATFORM+deliveredAt bundles, 8 cards in 2-3-4-col responsive grid met verkoopprijs (groen), relative-time, kind-pill (auction/listing/bundle/claimsale), link naar verkoper-profiel. Skipt EXTERNAL pickup (off-platform, geen price-trail). Geen buyerInfo exposed. (2) **`<BuyerProtectionSection>`** — 4-staps escrow-flow (Betaal → Bewaring → Controleer → Vrijgave) met genummerde icon-circles + connecting-line, 3 stat-pills (100% escrow / <48u disputes / €0 servicekosten) bovenaan, 4 garantie-cards onderaan (refund-garantie / dispute-afhandeling / NL-support / GDPR+SSL). Visueel substantieel zonder JS-dep. (3) **`<TierComparisonHomepage>`** — client component met billing-toggle MONTHLY/YEARLY (17% korting badge), 4-tier grid, "Meest gekozen"-badge op PRO, persuasive copy + break-even-pill. CTAs: FREE → `/register`, PRO/UNLIMITED → `/register?plan=PRO|UNLIMITED`, ENTERPRISE → `/dashboard/abonnement/enterprise-aanvraag`. Hergebruikt 100% van `subscription` i18n-namespace (geen duplicatie) + `ACCOUNT_TIERS` + `TIER_BREAK_EVENS_MONTHLY`. Section gebruikt `section-gradient` als enige accent-moment in het ritme. (4) **`<FaqSection>`** — native `<details>`/`<summary>` accordion (geen extra dep, accessible). 8 substantiële Q+A pairs: claimsale-uitleg / veiling-werking / betalingssecurity / verzending+tracking / dispute-flow / commissie+tarieven / verkoper-worden / mobile. Support-CTA-card onderaan linkt naar `/contact`. (5) **`<EmptyHomeSection>`** — generic fallback voor lege carousels (claimsales/listings/sold/sellers) met dashed-border, gekleurde icon-tile, title+desc+CTA-knop. Vervangt het stille `return null` patroon dat ritme brak. **Deel F — Testimonials gating**: `testimonials-section.tsx` accepteert `fiveStarCount` prop; rendert niet als `< 3` echte 5★-reviews. Placeholder-content blijft in code voor toekomstige fase. `homepage-data.ts` voegt `fiveStarReviewCount = prisma.review.count({where:{rating:5}})` toe. **Deel G — Section-order finale**: logged-out 15-stappenflow Hero → HowItWorks (bg-card) → ScrollStory (bg-bg) → SponsoredSpotlight (bg-card) → LiveAuctions (bg-bg) → RecentClaimsales/Empty (bg-card) → RecentListings/Empty (bg-bg) → RecentlySold/Empty (bg-card) → TrustPillars (bg-bg) → BuyerProtection (bg-card) → TierComparison (section-gradient accent) → TopSellers/Empty (bg-card) → Testimonials gated (bg-bg) → PlatformStats (bg-card) → FAQ (bg-bg). Logged-in 8-stappen zonder marketing-blokken. **Deel H — i18n**: ~60 nieuwe keys onder `home` namespace (NL+EN parity): empty-states (12), recently-sold (7), buyer-protection (23), tier-benefits-homepage (6), faq (20). Plus nieuwe `subscription.featureItemsPerClaimsaleUnlimited` voor de Unlimited-tier feature-row. **Open follow-ups**: (a) `Intl.NumberFormat("nl-NL")` hardcoded in hero-componenten — EN-users zien NL number-format. Bewust niet aangeraakt omdat hero buiten scope viel; eenvoudige `useLocale()`-pass-through wanneer aan de orde. (b) Testimonials staan nu in dev verborgen door 5★<3 — placeholder content blijft tot toekomstige echte-reviews-fase. (c) `/contact` route bestaat nog niet — FAQ-support-CTA is voorlopig een `mailto:support@cardscenter.nl` (hardcoded constant in `faq-section.tsx`); bij echte /contact-pagina vervangen door `<Link href="/contact">`. (d) RecentlySold mobile-grid is 2-col op <md — kan eventueel naar 3-col-scroll-carousel als 4 items te smal voelen op tablet. (e) Section-divider component blijft in `src/components/home/` voor mogelijke andere pages; ongebruikt op homepage. **Fase 36 audit-fixes** (post-merge): (i) `recently-sold-section.tsx` gemarkeerd als `"use client"` — `useFormatter` werkt in server components maar het component heeft hover-transitions en cards die hoe dan ook client-rendered zijn. (ii) Dode `bg-gradient-radial` Tailwind-class verwijderd uit `trust-pillars-section.tsx` — de radial-glow rendert via inline `style={{backgroundImage: "radial-gradient(circle, var(--tw-gradient-stops))"}}`, de `from-*/via-*/to-*` utilities zetten de CSS-variabelen die de inline style consumeert. Tailwind heeft geen native `bg-gradient-radial` utility. (iii) FAQ-support-CTA `<Link href="/contact">` → `<a href="mailto:support@cardscenter.nl">` (omdat /contact niet bestaat) — `SUPPORT_EMAIL` const inline in `faq-section.tsx`. (iv) **TierComparison nu ook zichtbaar voor logged-in FREE-users**: `showTierComparison = !isLoggedIn || accountType === "FREE" || !accountType` in page.tsx; nieuwe `isLoggedIn` prop op `TierComparisonHomepage`. Voor logged-in FREE: FREE-CTA → `/dashboard/abonnement` met label "Jouw huidige plan", PRO/UNLIMITED → `/dashboard/abonnement` met "Upgrade naar X", ENTERPRISE → `/dashboard/abonnement/enterprise-aanvraag`. Logged-out gedrag ongewijzigd (alle CTAs naar `/register?plan=...`). 2 nieuwe i18n-keys onder `home`: `tierCtaCurrentPlan`, `tierCtaUpgradeTo`. (v) **FAQ nu zichtbaar voor iedereen** (gate `!isLoggedIn` weggehaald) — onboarding-info als koper/verkoper-tarieven en uitbetalingen is ook nuttig voor ingelogde users. (vi) **Bekend issue**: logged-in non-FREE users zien 3 opeenvolgende `bg-card` secties (RecentlySold → TopSellers → PlatformStats) omdat TrustPillars/BuyerProtection/Testimonials weggevallen zijn. Visueel net acceptabel door distincte content per card, maar breekt strikt het 2-tier ritme. Indien hinderlijk: PlatformStats → `bg-background` flippen accepteert dan 2 opeenvolgende bg in logged-out flow (Testimonials → PlatformStats). Op 2026-05-15 **Claimsale-bundeling met seller-lock** (geen eigen fase, tussen het claimsale-wizard-werk door): `cart.ts checkout()` deed `findFirst` op een bestaande PAID `ShippingBundle` voor het buyer-seller-paar en hergebruikte die — daardoor werden claimsale-items in een eerdere marktplaats-/veiling-bestelling van dezelfde verkoper gedumpt, mét behoud van die bundle's `listingId`/`auctionId`-link. Default-gedrag nu: elke checkout maakt een verse bundle. Cross-checkout merge alleen via expliciete buyer-keuze (radio in /winkelwagen) + alleen tussen claimsales onderling (zie Claimsale-merge in de architectuur). Schema-toevoegingen `ShippingBundle.lockedForPackingAt` + `appendHistory` (JSON-log). Server: `getCombinableClaimsaleBundles` helper, `lockBundleForPacking` seller-action, race-safe claim via conditional `updateMany`. UI: merge-radio in winkelwagen, amber lock-banner + slate gelockt-pill + append-tijdlijn in OrderDetailModal. Tegelijkertijd ook 2 kleinere fixes: `ShippingMethodPicker` kreeg geen `itemTotal` mee → MAILBOX_PARCEL bleef ≥€150 zichtbaar (UI-only; server weigerde al correct); en de claimsale-detailpagina toont nu `reference`/`sellerNote`/`itemDescription` per item + kolom-label schakelt op claimsale-type (CARDS→Kaartnaam, ITEMS→Itemnaam) + beschrijving gecapt op 1000 tekens + owner-acties publishen realtime `claimsale-item-claimed` (status uitgebreid met `DELETED`). en op 2026-05-15 **Fase 37 — Vlag-icons + Live Hub + Marktplaats-wizard uniform + Autobid bug-fix** (vijf samenhangende delen op één sessie): **Deel A — `flag-icons` library** (`npm install flag-icons` + `@import "flag-icons/css/flag-icons.min.css"` in `globals.css`; nieuwe `<CountryFlag code="NL|EU|...">`-component in `src/components/ui/country-flag.tsx` met xs/sm/md/lg sizes + optionele `rounded`-mode + EU-fallback. Uitgerold op: seller-location-line (replaced emoji-flags, `crossBorderCountry` helper in distance.ts — flag toont nu altijd if seller has country, niet alleen cross-border), seller-info-block, address-form display, buyer-shipping-info, shipping-methods-manager zone-headers (origin-flag bij DOMESTIC, neighbor-flags bij EU_NEAR, EU-vlag bij EU_FAR), selling-scope-toggle (3 scope-cards renderen vlaggen ipv lucide-icons — origin/origin-stack/EU), home-auction-card SpecRow MapPin-row, ShippingMethodDisplay zone-headers in alle 3 aanmaak-flows (listing/auction/claimsale-form prop-threading via `originCountry`+`neighbors` van `getEuNearNeighbors(user.country)`). **Deel B — Marktplaats-wizard uniform**: nieuwe `ListingLabel` Prisma-model (parallel aan `AuctionLabel`, `onDelete: Cascade`), nieuwe `src/lib/listing/labels.ts` met 7 LABEL_TYPES (geen GEEN_RESERVE/DIRECT_KOPEN — auction-jargon), zelfde 10-kleurpalet + bundle-tarief €0,99/€1,69, conditional `availableLabelsFor({condition, listingType})` (TOPSTAAT-only-NM, COMPLETE_SET-only-COLLECTION). Nieuwe `<ListingLabels>` render-component. `multi-step-listing-form.tsx` complete rewrite naar two-column lg-layout: sticky `<ListingFormProgress>` chip-nav met IntersectionObserver (6 stappen Type/Foto's/Details/Prijs/Bezorging/Promotie), sticky `<ListingFormSummary>` preview-sidebar 360px, chip-array missing-fields (max 6 zichtbaar) i.p.v. "next-required" string, `data-section + scroll-mt-32` per sectie, SectionHeader-pills verwijderd. `step-upsells.tsx` rewrite naar vertikale stack (Spotlight + Category Highlight, URGENT_LABEL geschrapt) + `LabelsBlock` mirror van auction met kleur-popover + remove-X + counter-pill + stale-state-fix. `listing-form-progress.tsx` + `listing-form-summary.tsx` nieuw, parallel aan auction. `step-review.tsx` rendert nu `<ListingLabels size="md">` onder titel + kosten-block inclusief labels. `createListing` action accepteert labels-payload (apart parsen, anti-tamper hercheck), combined balance-check, één gecombineerde Transaction-deduct voor upsells+labels. Labels meegestuurd in queries op `/marktplaats` + detail-page, gerenderd op listing-card en listing-list-row. **`LISTING_UPSELL_TYPES_OFFERED`** constant in upsell-config.ts (alleen HOMEPAGE_SPOTLIGHT + CATEGORY_HIGHLIGHT). **Pickup-payment-UX**: i18n badges toegevoegd — `pickupPayment.platform.badge="Aanbevolen"` (emerald) + `pickupPayment.external.badge="Eigen risico"` (amber). Default `allowExternalPickup=false` in INITIAL_STATE (was `true` — sellers moesten bewust kiezen voor off-platform). freeShipping toggle verborgen + state-clear bij switch naar PICKUP-only. Typo's: "publiek zichtbaar" → "publiekelijk zichtbaar" in beide pickup-privacy hints. `freeShippingNote` ingekort tot "De koper betaalt geen verzendkosten." (zin over methode-selectie verwijderd, was redundant). **Deel C — Klikbare verwijzingen**: 4 i18n-strings die `Dashboard → X` als platte tekst hadden zijn omgezet naar `<link>...</link>`-markup met `t.rich()`-rendering in de componenten: `shippingDisplay.lockedNote` (ShippingMethodDisplay → `/dashboard/verzending`), `pickupLocation.noCityWarning` (listing-form), `acceptShipping.noMethods` (bundle-offer-message), FAQ items `faqA10/A11/A12` (homepage FaqSection → saldo/uitbetalingen/verzending) met optional `linkHref` per item. Volgt het bestaande `t.rich({link})`-patroon uit live-auction-content/bid-section. **Deel D — Admin ReserveBreakdownCard + setAutoBid bug-fix + auto-publish**: nieuwe helper `getReserveBreakdown(userId)` in balance-check.ts retourneert per-auction-rijen met reason (`highest-bidder | autobid-armed | awaiting-payment`) + baseAmount + reserveAmount + dbReserved vs liveReserved + drift. Nieuwe `<ReserveBreakdownCard>` + `<ReserveSyncButton>` in `src/components/admin/` voor `/dashboard/admin/users/[userId]?tab=wallet`: amber drift-banner met "Sync nu"-knop alleen zichtbaar bij drift, groene "Klopt" anders, plain-language explainer per veiling-rij, `10% × €X × 1,029` formule-uitleg. Nieuwe admin-action `syncUserReservedBalance(userId)` met `requireAdmin` + `logAdminAction({action:"SYNC_RESERVED_BALANCE",...})`. **Bug-fix** in `setAutoBid` (auction.ts:1199-1215): wanneer user al hoogste bieder is werd alleen de AutoBid-row geüpserted maar `User.reservedBalance` niet bijgewerkt (drift €4-30 mogelijk afhankelijk van max). Nu `syncReservedBalance(userId)` in de else-tak. **Architectuur-verbetering**: `syncReservedBalance` zelf publishet nu `publish(userChannel(userId), {type:"balance-changed"})` direct na de DB-update — alle 13 callsites (placeBid, resolveAutoBids, cancelAutoBid, setAutoBid, finalizeAuction, completeAuctionPayment, cron auction-payment-deadline, runner-up flow, admin sync, ...) krijgen automatisch live-broadcast naar `<UserBalance>` header-pill, geen handmatige publish-call meer nodig per locatie. **Deel E — Live Hub**: `/dashboard/biedingen` → "Live Hub" hernoemd (i18n `bids.title` + `dashboard.myBids` aangepast); page is nu tracker-driven (Watchlist ∪ bid-history) i.p.v. alleen bid-history. ACTIVE auctions tonen 2-koloms op lg+; ENDED auctions waar user op gebod heeft tonen als compacte rows onderaan. Nieuwe `<LiveBidCard>` client-component met 4 status-pills (`highest`=emerald-Trophy / `autobid-armed`=violet-Zap / `outbid`=amber-AlertCircle / `tracking-only`=sky-Heart), prominent groot huidig-bod (groen als highest), meta-regel met jouw bod + reservering + autobid-max-zap, inline quick-bid form (€ input + Bied-knop) met `min-w-[14rem]` + `flex-wrap`-fallback voor smalle viewports, autobid expand-panel met info-explainer ("Geef het maximumbedrag op dat je bereid bent te betalen..."), bottom-right `<MiniCountdown>` met seconden onder 24u voor live-feel (d/u/m/s format, rood < 1u), untrack-hartje bij gevolgde-auctions. Sortering: hoogste-bieder → autobid-armed → tracking-only → snelst-eindigende. `<PastBidRow>` compact voor Won/Lost. `<BiedingenRealtimeWatcher>` registreert `auction:<id>`-channels voor alle live cards + subscribet op bid-placed/outbid/auction-ended/auction-won/balance-changed → `router.refresh()` triggert herfetch. `WatchlistButton` aangepast met `e.preventDefault() + e.stopPropagation()` zodat hartje veilig in nested `<Link>` op auction-card werkt (ook voor listing-list-row). Tooltip context-aware: `auctionId` → "Live Hub", listing/claimsale → "Volglijst" (zelfde Watchlist-tabel onder de motorkap, twee pages in dashboard). Pulserend rood live-bolletje (rose-500 + animate-ping outer + solid inner) op page-heading naast "Live veilingen" + in dashboard-nav vervangt het HandCoins-icoon voor de Live Hub-link (universele streaming-signal — `NavItem.liveIndicator?: boolean` optional flag). Tracker-hartje overlay top-left op auction-card grid-view in `/veilingen?view=grid` (showWatchlist+initialWatched-props vanaf page-query naar AuctionCard), en op 2026-05-17 **Fase 38 — Mobile UX-polish + Prijs-helper wizard + bid-ladder overhaul + iOS Safari hardening**: substantiële UX-polish-sessie over de hele site, niet één feature maar een cluster van mobile-first verbeteringen en infra-fixes. **Deel A — Bid-ladder + min startbod**: `getMinimumIncrement` in [src/lib/auction/bid-increments.ts](src/lib/auction/bid-increments.ts) volledig herzien naar granulaire ladder met 14 tiers tot €250 max-stap (was €25 cap bij ≥€500). Nieuwe constante `MIN_STARTING_BID = 5` in [src/lib/validations/auction.ts](src/lib/validations/auction.ts) — Zod-schema weigert lagere startbiedingen, UI-laag in step-pricing toont amber-callout met "Kan beter via een claimsale →"-link bij invoer <€5, min-attr op de input + onKeyDown blokkeert negatief teken + e/+, clamp in onChange als laatste vangnet. Wizard-progress markeert pricing als incompleet en publish-knop disabled tot ≥€5. **Deel B — Prijs-helper modal** ([src/components/auction/pricing-helper-modal.tsx](src/components/auction/pricing-helper-modal.tsx) NIEUW): 4-stappen wizard die seller per veiling adviseert over startbod/reserve/buyNow/looptijd. Centrale `STRATEGY_CONFIG`-tabel met 4 tiers — `flash` (3d, 10% startbod, geen reserve, +5% BN), `fast` (5d, 15%, geen reserve, +8% BN), `standard` (7d, 30%, neutraal, +12% BN), `patient` (14d, 50%, reserve aan, +20% BN). Auto-coupling: tier-keuze zet looptijd + reserve-toggle + BN-toggle direct naar aanbevolen defaults (override mogelijk per stap). `<RecommendationBanner>` toont alleen positieve "Aanbevolen voor de strategie [name]"-confirmatie wanneer feature past — niet-passende combinaties krijgen géén waarschuwing (zou kopers afschrikken), gebruiker beslist op basis van neutrale pros/cons-cards. Tier-cards 1×4 gestapeld met icon (Zap/Rocket/Scale/ShieldCheck) + looptijd-badge. Marktwaarde-input pre-fillt uit `pricing.avg` van TCGdex-snapshot (afgerond op hele euro's, null als geen snapshot). Helper-trigger als chip-knop bovenaan step-pricing. Modal via `createPortal` in `document.body` om elke parent stacking-context te ontsnappen, fullscreen op mobile (`bg-background` solid, `fixed inset-0`) + centered dialog op desktop (`sm:my-8 sm:max-w-lg sm:rounded-2xl`). Body-scroll-lock via `position:fixed top:-scrollY` op `<body>` zolang modal open is (iOS-safe), scroll-positie hersteld bij sluiten. Compute-logica respecteert server-rules (reserve ≥ startbod, buyNow > beide) met +5 fallback voor lage-waarde edge-cases. **Deel C — Claimsale list-row mobile rewrite** ([src/components/claimsale/claimsale-list-row.tsx](src/components/claimsale/claimsale-list-row.tsx)): /claimsales-page heeft geen grid-view meer (te druk visueel), alleen list. Article `flex flex-col sm:flex-row` met inner-wrapper `flex flex-row sm:contents` zodat image+body op mobile in flex-row staan maar op desktop oplossen tot direct article-children naast de prijs-kolom. Mobile-only items-carousel (5 thumbs 80×112 + "+N meer"-tile, horizontaal scroll) onder de image+body. Mobile-only seller-footer met watchlist links + seller-info rechts via `justify-between`. Cards zonder cover renderen géén placeholder-tile meer (Tag-icoon weg) — body neemt vrijgekomen ruimte. `sm:min-h-[14rem]` op article + `mt-auto` op desktop-footer + `justify-end` op seller-row zorgt dat seller+location op alle cards op exact dezelfde y- én x-positie staat ongeacht of er een cover-thumbnail is. Verzending-pill op mobile `text-[10px]` → `text-xs` met `font-medium`. Typo `weeken` → `weken` gefixt. **Deel D — Mobile header refresh** ([src/components/layout/header.tsx](src/components/layout/header.tsx)): UserBalance + CartIcon uit `hidden md:flex`-block gehaald → altijd zichtbaar in header-banner (mobile + desktop). Desktop-iconen (notifs, admin, msgs, dashboard-avatar) blijven `md:flex`-only. Slide-out menu volledig herontworpen: quick-action-rij met 2 icon-buttons (Berichten + Meldingen, ieder 48×48 met unread-badge — primary kleur voor msgs, rose voor notifs, ring-2 ring-slate-950 voor "float"-effect, max "9+") + Dashboard-pill met gradient + avatar + chevron als hoofd-CTA. Section-labels "CATEGORIEËN" / "OVERIG" in uppercase slate-500. Primary nav als rounded-xl cards met gekleurde bolletjes (blue/amber/emerald) + active-state highlight (zelfde tinted bg + pulserend ping-animatie op de dot). Unread-counts realtime via SSE (`new-message` + `notification-created` events) + 60s polling fallback. **Deel E — iOS Safari hardening**: globale CSS-rule in [globals.css](src/app/globals.css) `font-size: 16px !important` voor `input/textarea/select` onder 640px — voorkomt iOS auto-zoom bij focus op kleinere font-size inputs. Tailwind utilities `text-sm` etc. worden op mobile overruled. Excluded: `checkbox/radio/range/color/file` (niet zoom-triggers). Aanvullend zijn de search-bars (header inline, mobile-menu, header-toggle, card-search) direct op `text-base` gezet voor extra zekerheid. Card-search-select dropdown gerefactord naar `createPortal` in `document.body` + `position: fixed` met dynamisch berekende top/left/width o.b.v. wrapper `getBoundingClientRect()` — escape uit alle parent stacking-contexts inclusief de sticky form-progress-nav die op iOS Safari `backdrop-filter` + `sticky` + `z-index` "atomic"-stacking veroorzaakte. Scroll+resize-listeners updaten dropdown-positie zodat 'ie blijft kleven aan de input. Click-outside-handler check zowel wrapper als dropdown-ref. **Deel F — Card-search-select polish**: 3-koloms grid op lg+ (was 2-koloms), thumbnails 96×134 → 112×156, "Normal"-variant verborgen uit chip-rij (alle kaarten hebben een Normal-print, vervuilt resultaten), `FETCH_LIMIT=60` initial fetch + `INITIAL_VISIBLE=24` met "Meer weergeven (+N)" pill onder de grid die client-side de rest onthult, dropdown-breedte matched de search-input-breedte (geen vaste width-override). Selected-state preview-card vergroot van 60×84 naar 140×196 met high-res `imageUrl` (na detail-fetch), uitgebreide info-rij (set + release-year, series-naam, rarity-chip, variant-chips). **Deel G — Homepage tweaks**: "EINDIGT NU"-label drempel van `URGENT_HOURS=3` → `URGENT_MINUTES=5` zodat 'ie alleen in laatste 5 min (incl. anti-snipe-extensies) verschijnt. Auction-card "Startbod" + "Huidig bod" 2-koloms vervangen door één gecentreerde "Huidig bod"-block. `<RecentlySoldSection>` van 2-rij-grid naar `HomeCarousel` (1 rij desktop, scrollbaar). Subtitle ingekort ("een eerlijke prijs-indicatie"-zin weg). Top-Sellers en Platform-Stats secties verborgen op homepage (in beginfase geen real-data, componenten blijven bestaan voor toekomstige re-enable). **Deel H — Misc polish**: sticky bottom-bar in auction-wizard toont op mobile alleen 1ste error + amber `+N`-counter (was alle errors gestackt). Autofocus weggehaald van Prijs-helper marktwaarde-input op mobile (geen ongevraagde keyboard pop-up). DOM-event `pricing-helper-open/close` voor sticky-bar-verberg-coupling (belt-and-braces — modal covert visueel al fullscreen op mobile). Onbenutte placeholder "bv. 250" weg uit marktwaarde-input. Claimsales-grid-view + `ClaimsaleViewToggle` weggehaald uit `/claimsales` (alleen lijst-rows, ClaimsaleCard nog gebruikt op homepage). Backdrop-blur weggehaald van `auction-form-progress`-sticky (iOS-quirk met "atomic" stacking). en op 2026-05-17 **Fase 39 — Mijn Veilingen dashboard-redesign + nav count-badges + status-aware auction-edit**: drie samenhangende delen. **Deel A — `/dashboard/veilingen` rewrite**: van platte lijst naar Live Hub-stijl two-section layout. Boven: "Live veilingen" met pulserend rose-bolletje + count-pill, 2-col `lg:grid-cols-2` `<SellerAuctionCard>`-grid voor ACTIVE+SCHEDULED. Onder: "Afgelopen veilingen" met compacte `<EndedAuctionRow>` (status-pill rechts + eindprijs + image). Sort live "hot first": bidCount≥5 → has-bids → no-bids → SCHEDULED, tie-break op endTime-asc. SellerAuctionCard heeft 4 status-pills (Wacht op start sky / Geen biedingen amber / Verkocht bij eind emerald / Onder reserve rose) + optionele "Hot" violet-pill bovenop (bidCount≥5), hoogste-bieder-block met avatar + naam klikbaar naar `/verkoper/[id]` + bedrag, `<AuctionLabels size="sm">` als labels actief, action-strip met "Bekijk biedingen" / "Pas aan" (opent `<EditAuctionDrawer>`) / `<AuctionOwnerActions variant="card">` (cancel-with-refund, ongewijzigd) / `<MiniCountdown>` bottom-right (target=startTime voor SCHEDULED, endTime voor ACTIVE, urgent<1u). EndedAuctionRow mapt 8 statussen (ENDED_SOLD/BOUGHT_NOW emerald, ENDED_RESERVE_NOT_MET/AWAITING_PAYMENT/AWAITING_RUNNER_UP_DECISION amber, ENDED_NO_BIDS/CANCELLED slate, PAYMENT_FAILED rose). `<VeilingenRealtimeWatcher>` registreert `auction:<id>`-channels per live-card + subscribet op bid-placed/auction-ended/auction-won/balance-changed/auction-updated → `router.refresh()`. Geen `outbid` (seller bid niet op eigen veilingen). `MiniCountdown` ge-extract van inline in `live-bid-card.tsx` naar `src/components/dashboard/mini-countdown.tsx` (reused door SellerAuctionCard). **Deel B — Dashboard-nav reorder + count-badges**: `offer`-groep volgorde nu Veilingen → Claimsales → Marktplaats. `NavItem` uitgebreid met `badge?: number`-veld; render in `renderItem()` rechts naast label-span met tier-aware kleur (`bg-primary/20 text-primary` active, `bg-muted text-muted-foreground` inactive), hidden bij undefined/0. Counts in dashboard-layout opgehaald via parallelle `Promise.all([getSellerStats, fetchActiveActivity])` en als `counts={auctions, listings, claimsales}` prop doorgegeven aan `<DashboardNav>`. `fetchActiveActivity` ([src/lib/dashboard-queries.ts](src/lib/dashboard-queries.ts)) telt nu ook SCHEDULED-status voor auctions + claimsales (listings ongewijzigd want geen SCHEDULED-state). **Deel C — `updateAuction` server-action + EditAuctionDrawer**: nieuwe gedeelde helper `computeEditScope(status, bidCount)` in `src/lib/auction/edit-scope.ts` retourneert `EditScope = FULL | TIMING_LOCKED | DESCRIPTION_ONLY | NONE`. Matrix: SCHEDULED+0 bids = FULL (alles), ACTIVE+0 bids = TIMING_LOCKED (alles behalve startDate/duration/endTimeOfDay), ACTIVE+≥1 bid = DESCRIPTION_ONLY (alleen description + image-append + labels-add), alle ended/awaiting = NONE. Nieuwe `updateAuctionSchema` in `src/lib/validations/auction.ts` met alle velden optional + cross-field superRefine voor buyNow > reserve > startingBid. Server-action `updateAuction(auctionId, formData)` in `src/actions/auction.ts`: per-veld scope-validatie buiten tx (user-friendly errors), cross-field re-check tegen DB-waarden (seller kan reservePrice los muteren zonder buyNow), `deriveAuctionWindow()` voor timing-recompute met SCHEDULED→ACTIVE auto-flip als `startTime ≤ now + SCHEDULED_THRESHOLD_MS`, atomic `$transaction` met fresh-scope-recheck + race-gated `updateMany WHERE status IN allowedStatuses` zodat een tussentijdse bid de update faalt met user-friendly bericht. Labels-add (alle non-NONE scopes) met `availableLabelsFor()` + bundle-cost-delta (`calculateLabelCost(newTotal) - existingCost`) + balance-check + `deductBalance` buiten tx. Publiseert `auction-updated` event (nieuw in `realtime.ts` event-union, payload `{auctionId}` — watcher hoeft alleen `router.refresh()` te triggeren) naar `auctionChannel(auctionId)` + `userChannel(sellerId)`. Drawer `<EditAuctionDrawer>` opent via "Pas aan"-knop op SellerAuctionCard: scope-aware form met banner-tone per scope (sky=FULL, sky=TIMING_LOCKED, amber=DESCRIPTION_ONLY, rose=NONE), `<LockedField>` met lock-icoon + tooltip voor velden buiten huidige scope, additive-image-mode in DESCRIPTION_ONLY (bestaande foto's lock-pill, aparte `<ImageUploader>` voor toevoeging), full-replace in TIMING_LOCKED/FULL. Diff-aware submit: alleen gewijzigde velden in FormData. Submit-state via `useTransition` + sonner-toast + `router.refresh()`. v1-beperkingen: type-specific velden (cardItems/productType/itemCategory/conditionRange) niet bewerkbaar via drawer — follow-up; shippingMethodIds niet verwerkt — seller moet annuleren-en-opnieuw als verzending fout zit; upsell-toevoegen vanuit drawer deferred (pro-rata cost-interactie met `refundAuctionPromotion` vereist apart ontwerp), en op 2026-05-17 **Fase 40 — Aankoop-flow audit: race-fixes + Dispute v2 + Tracking-stuck + UX-polish** (8 deelgebieden in één branch op basis van 3-agent audit-rapport). **Schema**: nieuwe `DisputeV2` + `DisputeV2Event` + `ShippingIssue`-modellen naast bestaande v1-Dispute; `ClaimsaleItem.lastUnclaimAt`+`lastUnclaimedById` voor anti-renewal-exploit; `CancellationRequest.escalatedShippingIssueId` voor auto-escalatie bij EXPIRED. **A. Race-fixes**: `buyNow` volledig atomic via `$transaction` met `updateMany WHERE status="ACTIVE" AND endTime > now AND buyNowPrice NOT NULL`-gate. Wallet-mutaties via nieuwe tx-bewuste helpers in `src/lib/wallet-internal.ts` (`deductBidPaymentInTx` + `escrowCreditInTx`) zodat full + partial-payment paden niet meer kunnen orphan'en bij wallet-fail. `resolveAutoBids` recursion depth-guard 50. `cancelAuction` orphan-cleanup binnen tx: AwaitingDecision RunnerUpOffers → EXPIRED, PENDING bundles → deleteMany. Claim-lock-renewal-exploit gefixt door `unclaimItem` lock niet meer te resetten + `lastUnclaim*`-tracking; `claimItem` + `claimAllItems` two-path (fresh-claim reset lock, same-user-reclaim behoudt). **B. Buyer-dashboard**: stale-PAID amber banner >7d met "Vraag annulering aan"-CTA + countdown naar 14d auto-cancel (lost user's klacht "ik zie geen annuleer-knop" op — knop was er, alleen verstopt achter expand). autoExpiredAt-banner met heldere uitleg voor cron-cancelled bundles. SHIPPED-bundles >14d krijgen "Trackingprobleem melden"-knop met directe link naar ShippingIssue-form. **C. Seller-dashboard**: ship-deadline countdown pill in collapsed view met tone-coding (blauw >7d / amber 2-7d / rood <2d). AWAITING_PAYMENT-tab info-banner "Wachten op betaling — geen actie nodig". Bulk-mark-shipped als follow-up gedeferred. **D. Dispute v2 greenfield**: 6 structured reden-categorieën (NOT_RECEIVED/NOT_AS_DESCRIBED/DAMAGED/WRONG_ITEM/COUNTERFEIT_SUSPECTED/OTHER) ipv free-text; expliciete MEDIATION-fase voor proposal-loop; 5d admin-SLA-deadline bij ESCALATED; foto-upload UI bestaat echt (v1 had dead `evidenceUrls`-params); werkt voor SHIPPED + EXTERNAL-pickup-COMPLETED-bundles; 4 admin-resoluties (FULL_REFUND → bundle CANCELLED + escrow + premium terug / PARTIAL_REFUND → bundle COMPLETED + split / NO_REFUND → escrow naar seller / RETURN_AND_REFUND admin-only); cron `auto-resolve-disputes-v2` (OPEN>14d → 100% buyer + premium / SELLER_RESPONDED>14d → escrow naar seller / ESCALATED wacht op admin); pages `/dashboard/geschillen-v2/[id]` role-aware detail (buyer/seller/admin in één component) + `/dashboard/admin/disputes-v2` admin-queue met SLA-tone-pills; v1-Dispute blijft ongewijzigd voor lopende cases. **E. Shipping-proof**: sterke aanbeveling-banner in `ShipBundleForm` voor bundles ≥€50; `UploadContext` uitgebreid met "shipping" + "dispute" (beide gemodereerd via Claude Haiku vision); admin v1-disputes-page toont nu `bundle.shippingProofUrls` als aparte sectie zodat admin complete bewijs-pakket ziet. **F. Tracking-validatie**: `src/lib/shipping/tracking-validation.ts` met permissieve per-carrier regex (POSTNL / DHL / DPD / GLS / UPS / BPOST / DEUTSCHE_POST / LA_POSTE) + generic floor (≥6 alphanumeric/dashes); `markAsShipped` doet server-side validatie met user-friendly error; PostNL postcode-guard weigert verzending zonder buyer-postcode; "OTHER"-carriers krijgen alleen generic-check. **G. ShippingIssue**: lightweight admin-ticket voor tracking-problemen; `openShippingIssue` (buyer >14d sinds SHIPPED of seller voor WRONG_DELIVERY), `startInvestigatingShippingIssue`, `resolveShippingIssueGoodwill` (platform-credit tot €50 via Transaction GOODWILL_REFUND, bundle blijft SHIPPED — seller-escrow niet geraakt), `resolveShippingIssueNoAction`, `escalateShippingIssueToDispute` (maakt DisputeV2 met escalated_from_shipping_issue_*-subcategorie); 4 types (TRACKING_STUCK / NO_SCAN_UPDATE / WRONG_DELIVERY / OTHER); `autoConfirmDeliveries` skipt nu ook bundles met disputeV2 of active ShippingIssue zodat escrow niet vrijkomt tijdens admin-onderzoek. AdminAction "RESOLVE_SHIPPING_ISSUE" + AdminTargetType "SHIPPING_ISSUE". AdminNav uitgebreid met navDisputesV2 + navShippingIssues. **H. Misc**: `scripts/fix-pre-fase28-heldbalance.ts` eenmalige migratie voor claimsale-bundles van vóór Fase 28 waar shippingCost wel van buyer afgeschreven werd maar niet aan seller.heldBalance toegevoegd (default dry-run, --apply, idempotent via Transaction-description-marker); `cancellation-expiry`-cron escaleert nu PAID-bundles met EXPIRED-cancel automatisch naar OPEN ShippingIssue zodat admin eerder kan ingrijpen ipv 14d-stille-limbo. **Open follow-ups Fase 40**: (a) bulk-mark-shipped voor seller-dashboard (top-sellers met 20+ bundles/dag — al Fase 28 follow-up, opnieuw gedeferred); (b) user-overview-page `/dashboard/geschillen-v2` — alleen detail-pages bestaan nu, geen lijst; gebruikers vinden v2-disputes via notificaties; (c) migratie van openstaande v1-disputes naar v2 zodra die resolved zijn; (d) carrier-verzekerings-integratie (DHL/PostNL claim API's); (e) admin-knop in v1-disputes om handmatig naar v2 te escaleren — huidige flow: nieuwe disputes gaan automatisch v2, v1 blijft alleen voor bestaande open cases; (f) i18n EN-vertaling van het hele `disputes-v2` namespace + de admin shipping-issues page — momenteel grotendeels hardcoded NL in component-bodies; (g) goodwill-pot-budgettering KPI op /dashboard/admin (hoeveel hebben we deze maand uitgegeven aan goodwill-refunds?); (h) pre-Fase28 heldBalance-migratie nog niet uitgevoerd in prod — dev-test toonde 12 bundles met €71.56 totale correctie. Update deze tabel na elke afgeronde fase.

### Performance-pass (2026-06-13, live gedeployed)
Optimalisatie-sessie n.a.v. trage laadtijden. **Belangrijkste les: de hele site rendert dynamisch** omdat `[locale]/layout.tsx` `auth()` aanroept (header login-status) → page-level `revalidate`/ISR wordt genegeerd, élke pagina draait per request. Empirisch bevestigd via `Cache-Control: no-store`. Daarom: **cache zware reads op query-niveau met `unstable_cache`** (werkt óók op een dynamische pagina), niet via page-ISR. Gedaan:
- **Kaart-detailpagina 15s-fix**: de blokkerende `syncSingleCard` PokeWallet-fetch (3 retries + backoff, geen timeout) volledig uit de render gehaald — prijzen komen puur uit de nachtelijke `pokewallet-scheduler` (boot + 03:00 UTC). `lookupCard` in React `cache()` (dedup generateMetadata/page), `relatedCards` in de grote `Promise.all`, dubbele `Card.findUnique` weg (`card = initialCard`), `viewCount`-write naar `after()`. PokeWallet-client kreeg 8s `AbortSignal.timeout`.
- **Indexen**: `Card` losse `@@index([cardSetId])` → composite `@@index([cardSetId, localId])` (lokaal `db push` + handmatig op Turso via libsql-script `DROP INDEX Card_cardSetId_idx` + `CREATE INDEX Card_cardSetId_localId_idx`).
- **`unstable_cache`** op `getHomepageData` (60s) en de `/kaarten`-lijst-data (~9 zware aggregate/groupBy-queries over 20k kaarten, 1u). Catalogus-detail (set/pokedex) had al ISR.
- **Loading-UX**: `loading.tsx` voor de hele `kaarten`-sectie (was er niet) met `<RouteLoadingIndicator>` (`src/components/ui/route-loading-indicator.tsx`): draaiend logo (`public/images/logo-loading.png`, plain `<img>` — Next image-optimizer faalt op Railway, vandaar overal `unoptimized`/`<img>`) + **puur-CSS percentage & balk** via `@property --loading-pct` + counter in globals.css (animeert vóór JS-hydratie; JS-tellers staan stil tijdens cold load) + roterende NL Pokémon-weetjes (JS, degradeert netjes).
- **Turso-latency**: gemeten met `scripts/measure-turso-latency.ts`. Railway (Amsterdam) + Turso (aws-eu-west-1) al gecolokeerd (~15ms) → embedded replica niet nodig. TTFB is ~70ms; traagheid = render-tijd + cold start na deploy (1e hit per pagina koud).
- **Asset-valkuil**: een ontbrekend/untracked bestand in `public/` geeft op live een 404 → bij `<img>` een ronddraaiende alt-tekst. `logo-loading.png` was nooit `git add`'d. Check bij nieuwe assets: `git ls-files`.
- **Open follow-ups**: (a) hoofd-kaartafbeelding (high-res `unoptimized`) laadt ~4s — apart onderzoeken; (b) écht instant pagina's vereist de **layout static maken** (header-auth client-side of PPR) → site-breed project, starter geparkeerd op branch `perf-card-static-experiment`.

### Beeld-weerbaarheid + kaart-prijstrends (live sinds 2026-07-07/08)
- **Beeld-mirror PokeWallet→R2**: `Card.imageMirrorKey` / `CardSet.logoMirrorKey` bevatten een stem (bv. `card-base1-4`); render bouwt `/api/uploads/{stem}-{low|high}.jpg`. Voorkeursvolgorde in `src/lib/card-image.ts getCardImageUrl`: **mirror > imageUrlFull (behalve PriceCharting-240px-thumbnails) > imageUrl (TCGdex) > PriceCharting als laatste redmiddel**. Bulk-lading via `scripts/pw-mirror-images.ts` (gehard: lockfile, health-gate op live API, gebatchte `$transaction`-writes, limit-cap, `--set=<tcgdexSetId>`-filter; live-runs via de regio-loze Fly-hostname `libsql://cardscenter-cardscenter.turso.io` voor edge-isolatie van de app). Nachtelijke cron mirrort 1500/nacht. **Bulk-jobs op live Turso: altijd klein+gepaced+gebatcht+foreground met health-gate** — een verkeersstorm kreeg 2026-07-04 het Railway-IP geblokkeerd bij Turso's edge (3 dagen down; fix = Railway-regio-switch of hostname-wissel).
- **Trend-percentages** (`src/lib/price-trend.ts`): alle prijs-delta's (stijgers/dalers op /kaarten = 30d-venster "afgelopen maand", percentage onder Marktwaarde op kaart-detail) komen uit een **kwadratische trendfit met Theil-Sen-uitschieterdetectie** over de snapshot-reeks — echte timestamps als x-as (gaten vertekenen niet), begin→eind van de fit als percentage. Losse snapshot-delta's alleen nog als fallback bij <5 punten. Kaart-detail hecht de live Marktprijs als "vandaag"-punt aan als het laatste snapshot ouder is, zodat grafiek/trendlijn/percentage tot nú lopen en met /kaarten matchen. Trendlijn bij 7d/14d lichtgrijs, vanaf 30d amber.
- **Snapshot-gat 5-7 juli 2026** (storing) is op live gevuld met lineaire interpolatie tussen 4 en 8 juli (`scripts/backfill-price-gap.ts`, idempotent via `bkfl-`-id's + INSERT OR IGNORE). Bij een toekomstig gat: zelfde script kopiëren/aanpassen.

### Bekende follow-ups (klein onderhoud, niet eigen fase)
- (Fase 39) `EditAuctionDrawer` v1 dekt description/title/photos/pricing/pickup-city/timing. **Type-specific velden** (cardItems voor MULTI_CARD, productType voor SEALED_PRODUCT, itemCategory voor OTHER, conditionRange voor COLLECTION, estimatedCardCount) zijn nog niet bewerkbaar — sellers die hier een fout in zien moeten annuleren-en-opnieuw (alleen mogelijk zonder biedingen). Vereist eigen sub-form per auctionType in de drawer.
- (Fase 39) `shippingMethodIds` zit in `updateAuctionSchema` maar wordt server-side nog niet verwerkt — comment in `updateAuction` documenteert dit. Bij implementatie: mirror `createAuction` met `deriveListingShippingMethodIds` of accepteer `allowMailbox` boolean ipv expliciete IDs.
- (Fase 39) Upsell-toevoegen vanuit `EditAuctionDrawer` deferred. Vereist pro-rata cost-rebuild + interactie met `refundAuctionPromotion` (welke pad refundt bij latere cancel? Apart ontwerp).
- (Fase 39) `dashboard-nav` count-badges tonen `auctions = ACTIVE+SCHEDULED`, `claimsales = LIVE+SCHEDULED`, `listings = ACTIVE`. Listings hebben geen SCHEDULED-state. Bij later toevoegen van listing-SCHEDULED ook badge-query bijwerken.
- (Fase 39) i18n: nieuwe seller-card / ended-row / edit-drawer strings zijn hardcoded NL in component-bodies. Bij echte EN-rollout migreren naar `dashboard.sellerCard.*` / `dashboard.endedRow.*` / `dashboard.editDrawer.*` namespaces. Volgt het patroon van admin-pages die ook nog hardcoded NL bevatten.
- Search/homepage/recommendations filteren nog niet op `UserBlock` (alleen marktplaats/veilingen/claimsales/conversations doen dat). Helper `getBlockedUserIds()` bestaat al — hoeven alleen toe te passen.
- PENDING `ShippingBundle`s zijn niet zichtbaar in `/dashboard/aankopen`/`verkopen` — daar wordt PENDING uitgefilterd. Pending auctions hebben aparte sectie, pending listing-proposals worden via chat afgehandeld. Eventueel een "Wachten op betaling"-sectie toevoegen.
- Admin-panel pagina's (`/dashboard/admin/bank-transfers`, `/users`, `/catalog`, `/config`, `/crons`, `/moderation`, `/audit`) gebruiken hardcoded NL-strings; alleen het `admin` i18n-namespace voor sectie/nav/KPI is gevuld. Pagina-strings progressief migreren wanneer EN echt gebruikt gaat worden.
- Admin user-detail "manuele verificatie" knop ontbreekt nog — bestaande `adminReviewVerification` werkt alleen op een bestaande `VerificationRequest`. Voor "verifieer zonder upload" zou een aparte action nodig zijn.
- (Fase 27) `PurchasesContent` + `SalesContent` hebben geen dedicated "bundle" sourceType — multi-listing bundles worden nu gerenderd als `sourceType="listing"` met titel "Bundel: N advertenties". Volledige multi-listing rendering (lijstje van alle items, per-item shipping-status) is een follow-up.
- (Fase 27) `counterBundleOffer` action niet geïmplementeerd. Schema heeft `parentProposalId` ready voor counter-chain (max 5 ronden volgens design). Buyer kan nu alleen withdrawen + nieuwe offer maken.
- (Fase 27) Bestaande PICKUP-listings van vóór Fase 27 hebben geen `pickupPostalCode`/`pickupCity` ingevuld. Bij eerste edit is het verplicht; geen migratie/banner-prompt geïmplementeerd.
- (Fase 27) Commission-omzeiling via PICKUP — sellers kunnen alle listings als PICKUP-only zetten om de 1.5–3% commissie te ontwijken. Bewust open business-issue. Mogelijke oplossingen later: platform-fee per pickup-confirm, of pickup uitsluiten van seller-tier-quotas.
- (Fase 27.36) Stock-badge op listing-card alleen geactiveerd op /marktplaats main grid. Sponsored row, recommendations, watchlist en seller-profile tonen de badge nog niet — `availableStock` prop is undefined daar. Follow-up: count-query toevoegen aan die queries.
- (Fase 27.39) Stocked + EXTERNAL pickup is op MVP-niveau: `PickupReserveButton` voor stocked listings reserveert quantity=1. Koper die meer stuks wil ophalen moet via chat regelen. Volledige stocked + quantity-stepper + EXTERNAL pickup is een follow-up.
- (Fase 27.43) PICKUP_PLATFORM bundle-offer gebruikt de bestaande PLATFORM-branch in `respondToBundleOffer`. Dispute-blokkade in `dispute.ts` checkt nog op `paymentMode === "EXTERNAL"`; voor PICKUP_PLATFORM bundles is dispute mogelijk via SHIP-pad maar de UI/flow is geen prioriteit getest. Cancellation/refund-paden voor PICKUP_PLATFORM bundles werken via PLATFORM-flow.
- (Fase 27) Reviews + seller-stats voor EXTERNAL bundles: huidige queries werken op `ShippingBundle.status="COMPLETED"` ongeacht paymentMode. EXTERNAL telt dus mee in seller-stats; in dashboards apart markeren ("buiten platform afgehandeld") is wenselijk maar niet geïmplementeerd.
- (Fase 27.103) `src/lib/data/postcodes-nl.json` is werkbare seed (~150 entries voor majeure NL plaatsen). Voor productie vervangen door volledige PDOK PC4-set (~4000 entries — open data via PDOK/CBS). Bestand-formaat ongewijzigd, code werkt direct na vervanging.
- (Fase 27.103) Distance werkt momenteel alleen op marktplaats / veilingen / claimsales / homepage hoofdgrid + sponsored-rows. ItemCarousel (recommendations op detail-pages: "andere items van seller", "vergelijkbare items") krijgt geen distance — kleine 192px-kaarten, te krap. Watchlist (`/dashboard/volglijst`) en seller-profile (`/verkoper/[id]`) gebruiken geen ListingCard maar single-line-rows; zou aparte refactor zijn.
- ~~(Fase 27.105) Veilingen + claimsales hebben nog geen filter-sidebar / list-view.~~ **Opgelost in Fase 35** — `auction-filters.ts` + `claimsale-filters.ts` aangelegd, beide pages rewrite naar two-column layout met dezelfde architectuur.
- (Fase 35) `availableLabelsFor()` heeft nu 9 label-types — bij toevoegen van een 10e moet je `LABEL_TEXT`-mapping in `auction-labels.tsx` + i18n-keys + (zonodig) conditional-rule in `availableLabelsFor` updaten. Geen DB-migratie want `AuctionLabel.type` is `String`.
- (Fase 35) Spotlight-quota refund: bij `cancelAuction` worden gratis-upsell-quota's NIET teruggestort (1× claim per slot, niet refundeerbaar). Bewust gekozen om quota-grinden te voorkomen, maar betekent dat een seller die per ongeluk publisht en direct annuleert z'n maandelijkse free-spotlight kwijt is. Mogelijk later een 5-minuten "regret window" toevoegen waarin quota wel terugkomt.
- (Fase 35) Checkout-lock werkt alleen voor claimsale-cart. Listing-bundle-checkouts en auction-payment-flows hebben geen vergelijkbare lock want daar is de race-window kleiner (geen achtergrond-cron die items kan terughalen tijdens betaal-modal). Mocht ooit nodig zijn voor listing-bundles: zelfde patroon (lockExpiresAt + AVAILABLE-flip-reset) toepasbaar.
- (Fase 35) `auction-card.tsx` accepteert nu `status` + `startTime` velden in `AuctionCardData` voor SCHEDULED-rendering. Bestaande callers buiten /veilingen (homepage hero-row, sponsored-row, /verkoper/[id]) tonen `status` mogelijk niet — fallback naar normale CountdownPill als `status === "ACTIVE"` of undefined. Voor productie: callers checken en SCHEDULED-zichtbaarheid eventueel uitbreiden.
- (Fase 27.108) `/dashboard/profiel` "Wachtwoord wijzigen" sectie is placeholder ("Wijzigen via support"). Echte password-change action vereist Auth.js v5 credentials-provider integratie + email-confirmatie flow — out-of-scope voor deze fase.
- (Fase 27.108) Adres-cooldown is niet retroactief: bestaande users waar `lastAddressChange` null is kunnen nog één keer gratis wijzigen. Voor users die ooit adres invulden bij registratie wordt dit gezien als "eerste vrije edit" — bewust, omdat we anders bestaande users zouden straffen voor een veld dat altijd al bewerkbaar was.
- (Fase 27.108) Bedrijfsgegevens-sectie (companyName/vatNumber/cocNumber) is read-only display. Editable wijzigingen vereisen aparte action met validatie + mogelijke admin-review (BTW-nummer is identiteitsbewijs voor zakelijke accounts).
- (Fase 28) Pakbon-template (printable HTML in `order-detail-modal.tsx handlePrint()`) is hardcoded NL — de visible UI is wel volledig vertaald via `common.orderDetail`. Pakbon-template-i18n is een follow-up wanneer EN echt gebruikt gaat worden.
- (Fase 28) Bulk-mark-shipped en CSV-export per bundle ontbreken nog. Top-sellers met 20+ bundles per dag zouden veel hebben aan multi-select op /dashboard/verkopen om in één keer tracking-codes in te voeren. Eigen fase (28.bulk).
- (Fase 28) Pick-list-checkboxes (afvinken tijdens packen, lokaal in browser) bewust uit deze fase gehaald. Zou `localStorage[bundleId] = packedItemIds[]` zijn — geen schema-werk. Volgt als verkopers vragen om monitor-pack-flow.
- (Fase 28) Pre-Fase-28 refunds zonder Transaction-event-record vallen in de modal terug op één samengevatte regel "Refund op bundle €X.XX" ipv per-event lijst. Reden: oudere bundles hebben wel `bundle.refundedAmount > 0` maar geen matchende Transaction-rij met description-prefix `Gedeeltelijke terugbetaling`. Geen migratie nodig — de fallback rendert correct.
- (Fase 28) Seller heeft geen dedicated "Annulering aanvragen"-formulier op SHIPPED of later. Voor problemen na verzending blijft `dispute.ts` de juiste route. CancellationActions toont alleen op PAID.
- (Fase 28) `cancelOrderBySeller` action is verwijderd — als andere code (legacy seeders, tests) ernaar verwijst zal die breken. Cleanup volgt indien gevonden.
- (Fase 28) **Pre-Fase-28 data-migratie nodig voor productie**: PAID/SHIPPED claimsale-bundles aangemaakt vóór de escrow-shipping fix hebben `seller.heldBalance += totalItemCost` (zonder shipping). Bij delivery zal `releaseEscrow(seller, totalCost, totalItemCost)` willen decrementeren met `totalCost`, terwijl er minder dan `totalCost` in heldBalance zit voor die bundles. De `safeEscrowDeduction`-clamp in `partialRefundEscrow` voorkomt negatief, maar `releaseEscrow` heeft die clamp niet — zou negatief kunnen worden. Voor productie eerst een eenmalig script draaien dat `seller.heldBalance += bundle.shippingCost` voor alle PAID/SHIPPED claimsale-bundles, gefinanceerd uit een platform-correctie-rij (geld kwam destijds nooit ergens binnen). Listing/proposal/auction/bundle-offer waren al consistent en hebben geen migratie nodig.
- (Fase 29) Privacy-policy text voor IP-tracking ontbreekt nog — er is geen `/privacy` page in het project. Voor productie: maak `src/app/[locale]/privacy/page.tsx` met de tekst "We verwerken IP-adressen voor anti-shill-bidding-detectie en bewaren ze maximaal 90 dagen, daarna geanonimiseerd."
- (Fase 29) Reserve-migratie voor lopende ACTIVE bids: bestaande `User.reservedBalance` is op 40% berekend. Bij eerste bid-mutatie of overbid wordt `syncReservedBalance` automatisch aangeroepen en herrekend naar 15%. Voor users met lange-lopende ACTIVE bids zonder activiteit blijft 40% staan tot ze of hun tegenstander bewegen. Eenmalige `syncReservedBalance(userId)` call voor alle users met `reservedBalance > 0` zou dit oplossen — script niet geschreven.
- (Fase 29) Borg-forfait clamp telemetry: als forfait < €200 (door clamp op te lage balance) zou je willen flaggen voor admin (mogelijk fraud-pattern: balance leegtrekken vóór payment-deadline). Eerst data verzamelen, dan dashboard-tegel toevoegen.
- (Fase 29) Geen automatische unsuspend bij strike-decay. Een gesuspende user blijft suspended tot admin handmatig `liftSuspension` doet, ook als hun count terugzakt naar 0. Bewust — automatic unsuspend zou dorpsmolen-effect kunnen geven (3× failure → permanent → na 365d ineens weer actief). Admin moet expliciet beslissen.
- (Fase 29) `kpiBidDefaulters` link gaat naar `/dashboard/admin/users?filter=bid-defaulters` — die query-param wordt door de users-page nog niet gehonoreerd. Filter werkt voor nu als shareable URL maar voor stricte filtering is een aanpassing in de admin/users page nodig.
- (Fase 29) Bestaande `src/app/api/cron/auction-payment-deadline/route.ts` is gerefactord naar CRON_JOBS-dispatch (was eerst dubbele logica). Andere oude cron-routes met inline logica zijn niet aangeraakt — werken nog steeds maar duplicatie blijft (zie comparison met `auto-cancel-stale-paid` en `payment-failure-decay` patroon).
- (Fase 31) Mollie recurring-billing volledig uit de fase getild — wachten op Fase 17. Hooks gereserveerd in schema (`User.mollieCustomerId`, `Subscription.mollieSubscriptionId/paymentStatus/gracePeriodEnd/nextBillingAt`). PRO/Unlimited upgrade-knoppen op /dashboard/abonnement zijn disabled met "Binnenkort beschikbaar". Bij implementatie: nieuwe `src/lib/mollie.ts` wrapper + `/dashboard/abonnement/upgrade/[tier]` route + `/api/mollie/webhook` + nieuwe `subscription-grace-expiry` cron + Mollie-cancel in `cancelSubscription`. De bestaande `upgradeToTier` is al de juiste insertion-point — Mollie-action hoeft alleen na geslaagde first-payment `upgradeToTier(userId, tier, cycle)` aan te roepen.
- (Fase 31) Bulk-upload CSV is MVP — geen image-upload, geen multi-card-items-JSON-expansion, geen tcgdex-resolution. Sellers vullen die per listing aan via standaard edit-flow. Voor productie kan een uitgebreidere variant met image-URL-kolom + per-rij prefetch nuttig zijn.
- (Fase 31) Tier-search-boost werkt alleen op marktplaats/veilingen/claimsales/zoeken hoofd-queries als SECONDARY orderBy. Sponsored-rows + recommendations + radius-post-filter zijn ongewijzigd. Voor "Aanbevolen"-sort die echt PRIMARY tier-boost zou doen is een extra UI-toggle nodig — niet in deze fase.
- (Fase 31) Reserve-migratie voor lopende ACTIVE bids: bestaande `User.reservedBalance` werd op `bid` berekend; nieuwe formule rekent op `bid + premium`. Bij eerste bid-mutatie of overbid wordt `syncReservedBalance` automatisch aangeroepen en herrekent. Eenmalige `syncReservedBalance(userId)` voor alle users met `reservedBalance > 0` zou de stale waardes goed zetten — script niet geschreven omdat de gebruiker expliciet aangaf dat er geen real users zijn.
- (Fase 31) Bid-confirmation modal-state per veiling staat in browser-`localStorage["bid-confirmed-auctions"]`. Reset bij browser-clear of wisselen van device → user ziet modal opnieuw. Server-side syncen zou een `BidConfirmationSeen { userId, auctionId }`-tabel vereisen — bewust als overhead skipped voor MVP.
- (Fase 31) Vanity-slug rate-limiting / nakijk-policy ontbreekt: een Unlimited-user kan willekeurig vaak slug wijzigen + alle gewenste namen claimen. Voor productie wellicht een 30d-cooldown analog aan IBAN/adres + admin-revoke voor offensive slugs.
- (Fase 31) Bulk-upload heeft geen rollback bij partial failure: als rij 7 van 10 faalt staan rijen 1-6 al gepubliceerd. Dit is bewust (per-rij transactie ipv one-big) om timeout-risico te voorkomen, maar betekent dat seller na error een aantal listings handmatig moet corrigeren of verwijderen. UI-summary toont wel duidelijk welke rijen geslaagd vs gefaald zijn.
- (Fase 31 audit) **`deductBidPayment` race tussen findUnique en $transaction** — bestaand patroon van `deductBalance` (sinds begin van het project), niet acuter geworden door Fase 31. Twee parallelle aanroepen kunnen beide de "balance >= total"-check passen → negatief saldo mogelijk. Vereist een wallet-systeem refactor naar conditional `updateMany({ where: { balance: { gte: amount } }, data: { decrement } })` met throw bij count===0. Geldt voor ALLE deduct-paden (deductBalance, deductBidPayment), niet alleen auction.
- (Fase 31 audit) **`createAuction` free-upsell race** — anders dan `createListing` (waar quota-decrement IN de listing-create-transaction zit + rollback bij race) staat de upsell-flow van `createAuction` BUITEN de auction-create. Bij quota-race krijgt seller dubbel gratis quota — minor financial impact (~€0.75/dag x N dagen). Volledige fix vereist refactor van `createAuction` naar één $transaction-wrap rond auction + shipping-methods + upsells + balance-deduct. Voor nu: conditional updateMany met `console.warn` als detection.
- (Fase 31 audit) **`EnterpriseRequest` REJECTED-records cleanup-policy ontbreekt** — een user die 100× wordt afgewezen accumuleert 100 REJECTED-rijen. `submitEnterpriseRequest` checkt alleen op PENDING-uniqueness, niet op recente afwijzingen. Niet kritiek maar DB-bloat-vector + theoretische DOS van het admin-panel als één user honderden afgewezen rijen heeft. Mogelijke fixes: cron die REJECTED-records >180d hard-delete, OR cooldown-policy "max 1 aanvraag per 30 dagen na rejection".
- (Fase 32) Backward-compat sunk cost voor pre-Fase-32 bundle-offer-bundles: `totalItemCost` bevat daar nog `totalAmount` (incl. shipping), dus releaseEscrow heft te veel commissie bij hun delivery-confirm. Geen migratie omdat er nog geen real users zijn — als dat wel ooit het geval is, eenmalig `UPDATE ShippingBundle SET totalItemCost = totalCost - shippingCost WHERE bundleProposalId IS NOT NULL AND createdAt < '2026-05-06'`.

## Workflow
- Na elke grote verandering: herstart de dev server (`npm run dev`) en controleer of alles correct werkt voordat je verdergaat.
- **Branch-strategie:** één branch per fase/cluster (`fase-N-onderwerp` of `cluster-X-onderwerp`), commit-per-logische-stap, fast-forward merge naar main. Géén force-push, géén destructieve git-commando's.
- **Commit per logische stap:** voor grote features split je in `<fase>.schema`, `<fase>.cron`, `<fase>.ui`, `<fase>.actions`, etc. — commit-message dekt scope per stap. Heel handig om later te herleiden.

### Handige shortcuts (uit ervaring)
- **Compile-check zonder build:** `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/<path>` — Turbopack compileert lazy bij eerste request, zo zie je TS/runtime errors zonder `npm run build` (die de DB lockt).
- **Find dev server PID + kill:**
  ```
  netstat -ano | grep ":3000" | head -2
  taskkill //PID <pid> //F
  ```
- **Watch dev log voor errors zonder noise:**
  ```
  tail -50 <dev-server-log> | grep -iE "error|fail" | grep -v queryCommand | grep -v cz-shortcut
  ```
  (`queryCommandState` en `cz-shortcut-listen` zijn pre-existing browser-extensie noise — negeren)
- **Mod-97 IBAN sanity-test:** zie `src/lib/validations/iban.ts`; test met canonical NL91ABNA0417164300 + bewust-foute checksum NL91ABNA0417164301.

### Action guards in volgorde van toepassen
Bij een nieuwe write-action die door een gebruiker getriggerd wordt, voeg deze guards toe in deze volgorde (bovenaan de functie):
1. `await auth()` + check `session?.user?.id`
2. `await requireNotSuspended(userId)` (Fase 8) — slaat over voor: payouts, dispute-respondes, markAsShipped
3. Resource-check: bestaat het, ben ik geautoriseerd?
4. State-check: is het in de juiste status?
5. Balance/saldo-check
6. Race-safe write via `updateMany` met status-filter, of expliciete `prisma.$transaction`
7. **Real-time `publish(...)` calls** (Fase 30) na succesvolle schrijfactie — kies juiste channel-type op basis van wat er verandert:
   - **Eigen-state events** (balance/cart/withdrawal/verification/suspension/notification) → `userChannel(userId)`
   - **Bundle/dispute** events die beide partijen raken → publish naar **buyer + seller user-channels**
   - **Publieke broadcasts** (auction/claimsale/listing status) → resource-channel (`auctionChannel`/`claimsaleChannel`/`listingChannel`)
   - **Chat-systeem-messages** (proposal/bundle-offer/pickup-flows die direct via `prisma.message.create` lopen ipv `sendMessage`) → roep `publishNewMessageForConversation(convId, senderId, preview)` aan na de message-create. Anders blijft chat-thread stale.
   - Patroon: `import { publish, userChannel } from "@/lib/realtime"` en publish AAN HET EIND van de action, na `revalidatePath` indien aanwezig. Fire-and-forget — geen `await` of error-throw bij geen subscribers.

### Action guards voor **admin-actions** (Fase 15)
Admin-only schrijfacties hebben een ander patroon — de eindgebruiker is hier geen "user", maar een admin die op iemand anders' state acteert:
1. `const { adminId } = await requireAdmin();` (uit `@/lib/admin`) — werpt bij niet-ADMIN. Page-variant: `requireAdminPage()` doet redirect.
2. Resource-check + state-check (existentie + correcte status)
3. **De eigenlijke schrijfactie** (transactie of single update)
4. **`await logAdminAction({ adminId, action, targetType, targetId?, metadata? })`** uit `@/lib/admin-audit` — direct na succes, niet bij faalpaden. Het `action`-string moet bestaan in de `AdminAction` const-union (uitbreiden in `admin-audit.ts`).
5. Optionele notificatie naar de getroffen user (zoals in `suspendUser`, `confirmBankTransfer`).
6. `revalidatePath()` voor de admin-page die het toont.

Voorbeeld: zie `src/actions/admin-suspension.ts:suspendUser` of `src/actions/withdrawal.ts:approveWithdrawal`.

### Nieuwe admin-feature toevoegen — checklist
Bij elk verzoek dat raakt aan "iets dat alleen admins doen" (queue, bulk-actie, instelling, log-view):
1. **Pagina** komt onder `/dashboard/admin/<feature>/page.tsx`. Géén nieuwe `*/admin/` paden ergens anders. `dashboard/admin/layout.tsx` doet de role-guard al — geen inline `accountType !== "ADMIN"` checks.
2. **Server actions** komen in `src/actions/admin/<feature>.ts`. Elke action begint met `await requireAdmin()` en eindigt met `await logAdminAction(...)` (zie patroon hierboven).
3. **AdminAction-union** in `src/lib/admin-audit.ts` uitbreiden met de nieuwe action-keys voordat je `logAdminAction()` aanroept.
4. **Nav-entry** in `src/components/admin/admin-nav.tsx` toevoegen onder de juiste sectie (Overview / Queues / Users & Content / System).
5. **i18n-keys** in het `admin` namespace (`src/messages/nl.json` + `en.json`) — minstens label + section, page-strings mogen hardcoded NL als de feature admin-only is.
6. **Overview-tegel** op `/dashboard/admin/page.tsx`? Alleen als het een queue is met "openstaand werk". Voeg een KPI-tegel + count-query toe.
7. **Audit-page filters** — als de nieuwe action veel zal voorkomen, voeg de action-key toe aan de `ACTIONS` const in `src/app/[locale]/dashboard/admin/audit/page.tsx` (en `targetType` aan `TARGET_TYPES` indien nieuw).

### Nieuwe cron toevoegen — checklist (Fase 15)
1. **Route** in `src/app/api/cron/<name>/route.ts` — gebruik `resolveCronTrigger(request)` voor auth en wrap business logic in `withCronLogging("<name>", async (run) => { ... }, trigger)`. `run.setItemsProcessed(n)` aanroepen voor zinvolle stats.
2. **Registry-entry** in `src/lib/cron-jobs.ts`:
   - Toevoegen aan `CRON_JOB_NAMES` const-tuple
   - Runner-functie in `CRON_JOBS` (zelfde shape als de andere — `{ itemsProcessed, result }` returnen)
   - Meta-entry in `CRON_JOB_META`: `description` (1-2 zinnen), `schedule`, `allowManualRun` (default true tenzij destructief), optionele `runWarning` voor heavy/risky jobs.
3. **Mag deze cron manueel?** Default ja. Zet `allowManualRun: false` voor:
   - Destructieve jobs (hard-delete, geen reversal mogelijk)
   - Jobs waarbij scheduling-frequentie precies belangrijk is en handmatig draaien de logica breekt
4. Heavy/risky maar wel manueel? Vul `runWarning` — die wordt zowel in de UI getoond als in de browser-confirm-prompt verwerkt.
5. CLAUDE.md API Routes-tabel hierboven bijwerken met de nieuwe route.

### Schema-velden die moeten meereizen bij nieuwe queries op publieke lijsten
- **Country filter:** `getSellerCountryFilter(buyerCountry)` (`src/lib/shipping/filter.ts`)
- **Block filter:** `sellerNotInBlockedFilter(await getBlockedUserIds(session?.user?.id))` (`src/lib/blocking.ts`)
- Deze twee samen → `where: { ...countryFilter, ...blockingFilter, status: "ACTIVE" }`

### Wat te doen als nieuwe Prisma-relatie circulair lijkt
Bij models met meerdere relaties naar dezelfde User (zoals `WithdrawalRequest.user` + `WithdrawalRequest.reviewedBy`), gebruik named relations:
```prisma
user        User @relation("WithdrawalUser", ...)
reviewedBy  User? @relation("WithdrawalReviewer", ...)
```
En in `User`:
```prisma
withdrawalRequests         WithdrawalRequest[] @relation("WithdrawalUser")
reviewedWithdrawalRequests WithdrawalRequest[] @relation("WithdrawalReviewer")
```
